# R16 — Model Feature Backlog Refinement (P0/P1 results)

Canonical home for completed R16 phase results. Scope, recipe rationale, and
the as-built P1 contract live in
`docs/ai-research/scoping/r16-model-feature-backlog-refinement.md`; this file
holds the evidence and verdicts. Other state files point here.

Cap: ≤500 lines. Roll older blocks to a sibling archive once exceeded.

---

## R16-P0 — MCTS self-play v3 embedding support — DONE

Data-path parity fix (commit `b38de0e`): MCTS-distill now exercises the v3
card-embedding branch instead of training it inert. Confirmed working by the
R110 W6 reproduction (real monotone `0.527→0.561→0.604` vs the inert
confounded run). Canonical reproduction evidence:
`docs/ai-research/progress/r110.md`.

---

## R16-P1 — Temporal / turn-state v3.1 (164-d) — IMPLEMENTED, ABLATION = NO-GO

### 1. Verdict: NO-GO — v3.1 ≈ v3.0, no Wilson-lower win, production stays 96-d

The 164-d temporal/turn-state vertical was implemented end-to-end and ablated
faithfully against the v3.0 baseline. Promote criterion: a Wilson-lower WIN of
the v3.1 best-promoted aggregate over the v3.0 R110-W6-repro best-promoted
baseline of **0.6042**.

v3.1 best-promoted is **0.5955 (iter-3)** — **0.0087 BELOW 0.6042**, within
gate noise (no gain, slight underperformance). v3.1 never clears the v3.0
peak and oscillates in a ~0.52–0.60 band. **v3.1 is NOT promoted; production
stays pinned 96-d.** The temporal/turn-state representation produced a **null
strength signal**.

### 2. Implementation status — IMPLEMENTED

- **v3.1 vertical** (commit `357f0d6`): TS observation schemaVersion 2→3
  (`temporal` block + per-side/per-Uma `turnState`, counts-only ability
  locks, derived `effectiveRetreatCostReduction`) → NEW Python
  `observation_to_features_v3_1` (164-d; frozen v3.0 builder verbatim for
  slots 0–109 byte-identical, +54 temporal slots) → serve_onnx `164→v3.1`
  schema entry → 164-d ONNX export. `STATE_DIM` stays a frozen alias of
  `STATE_DIM_V3=110`; v3.1 is opt-in via dim-keyed selectors.
- **Trainer wiring** (commit `3ce1404`): `--state-dim` threaded through
  `selfplay_dataset.py` / `train_bc.py` / `r12_orchestrator.py`, plus
  `make_v31_additive_init.py` — a 164-d additive-tail warm-start init proven
  output-identical to the v3.0 init (**logit/value delta = 0.0**), the
  cleanest possible A/B init.

As-built P1 contract and the two resolved scope omissions
(`globalRetreatCostReduction` → derived effective field; `ownIsFirstTurn` =
energy/setup-phase flag `turnsTakenBySide===0`):
`docs/ai-research/scoping/r16-model-feature-backlog-refinement.md` § P1.

### 3. Trajectory + reasoning (rolled to archive)

v3.1 6-iter Wilson-lower: 0.5189/0.5869/0.5442/**0.5955**/0.5527/0.5527
vs v3.0 R110-W6-repro best iter-2 **0.6042**: Δ=−0.0087, within-noise,
no Wilson win → **NO-GO**. Oscillates in ~0.52-0.60 band; +2-iter
extension confirmed no late climb (iters 4-5 flat at 0.5527).
Schema-independent W6 pattern (canonical: `r110.md §3` — intrinsic
W6-recipe property, not a representation effect). Full trajectory
table + reasoning rolled to `r16-archive.md` § "R16-P1 v3.1 trajectory".

### 5. Methodology / harness facts / residual gaps (rolled to archive)

- Faithful R110-W6-repro mirror (rollout leaf, 100 sims, CRN 3, 200
  rollout steps, n=120 side-balanced gate at `--eval-seed-start 9000`,
  unregularized; 164-d additive-tail init delta=0.0; faithful
  `--resume-state` for iters 4-5).
- Durable harness fact: `serve_onnx.py` TCP backlog 5→128
  (`request_queue_size=128`) under 24-worker gate fan-in; commit
  `e846881`; 0 connection errors across all subsequent runs.
- Benign residual: `training/r14_value_crossover_probe.py` is not
  wired for 164-d; logged-only diagnostic, zero effect on gated
  checkpoints.
- Two corrected recipe-confounds (process lessons): W6-fix flags
  defaulted ON (corrected with `--no-w6-fix-*`); `--eval-games 120`
  was wrong (the real gate is `--games 60`). Lesson: verify effective
  config in `events.jsonl`, never infer flag defaults; verify recipe
  args against the real baseline `gate.log`.

Full prose (methodology + recipe-confound walk-through) rolled to
`docs/ai-research/progress/r16-archive.md`.

### 8. Cross-references

- Scope / as-built P1 contract:
  `docs/ai-research/scoping/r16-model-feature-backlog-refinement.md`
- Schema-independent W6 mechanism + v3.0 baseline:
  `docs/ai-research/progress/r110.md`
- Forward-looking backlog: `docs/ai-research-backlog.md` (item 2 / 2b)

---

## R16-P2 — Per-Uma Slot Tokens v3.2 — C8 GATE NO-GO, +6.8pp iter-0 LIFT

### 1. Verdict — NO-GO at acceptance gate; iter-0 lift opens new forward line

Three v3.2 experiments executed 2026-05-21, all killed pre-iter-5. Best v3.2
Wilson-lower is **0.5955** (C8 rollout-leaf iter-0), **0.0087 BELOW** the
0.6042 R110-W6-repro production ceiling — within gate noise, no Wilson win.
**C8 gate result: NO-GO. 96-d production pin unchanged.** BUT C8 iter-0
produced a **+6.8pp Wilson-lower lift over R110 iter-0** (0.5955 vs
0.5273) — bit-exact init delta=0.0 confirms this is real learning from
slot-token state representation, not variance. The lift did not survive
W6 loop dynamics (iter-1 regressed bit-identically to R110 iter-0).
**NEW POSITIVE DATAPOINT** that re-opens `w6-loop-anti-degradation` as the
highest-leverage forward line: v3.2 + W6-fix-ON may preserve the iter-0
lift across iters.

### 2. Implementation status — IMPLEMENTED (C1-C7 bit-exact)

Autonomous chain C1-C7 landed across two /work cycles, all delta=0.0 parity
invariants verified bit-exact:

- **C1** `features.py` F=23 frozen slot-token tensor (commit `c46b02f`).
- **C2** `model.py` `uma_slot_encoder` + zero-output `Linear` head,
  `delta_logits` 0.000e+00 vs v3.0 (commit `0d7d6a6`).
- **C3** verified NO-OP.
- **C4** dataset / selfplay packing for the new `uma_slot_*` tensors
  (commit `43db81b`).
- **C5** LANDMINE `serve_onnx` `_SCHEMA_TABLE` refactor + 10 cross-input
  fail-fast guards + v3.2 ONNX roundtrip 2.38e-07 (commit `212ecde`).
- **C6** `train_bc` + `r12_orchestrator` `--uma-slot-tokens` flag,
  1-iter v3.2 smoke produced 7-input ONNX graph with schema 3.2 sidecar
  (commit `dc3d23b`).
- **C7** `make_v32_slot_token_init.py` + delta=0.0 parity smoke,
  `logits`/`value` 0.000e+00 bit-exact vs v3.0 source (commit `103e2ce`).

Chunk-plan landing commit `7330f0a`; queue refreshes `c475d30` / `7c3ca87`.
Full scope and chunk plan: `docs/ai-research/scoping/r16-model-feature-backlog-refinement.md`
§ "P2 - Per-Uma Slot Tokens" + § "P2 - Chunk Plan (kickoff 2026-05-21)".

### 3. Trajectories (n=120 side-balanced, Wilson-lower vs rule-bot)

**C8** — faithful R110-W6-repro mirror, `--mcts-leaf rollout`, v3.2 init from
R110 iter-2 0.6042 ckpt. Launch: `runs/R16-P2-c8-ablation/launch.sh`.

| Iter | Wilson-lower | Win-rate | vs R110 iter-N | Promoted? | Notes |
|---|---|---|---|---|---|
| 0 | **0.5955** | 0.6833 | **+6.8pp** vs 0.5273 | yes | best v3.2 ckpt overall |
| 1 | 0.5273 | 0.6167 | bit-identical to R110 iter-0 | no | regress to v3.0 trajectory |
| 2 | 0.5612 | 0.65 | — | yes (tolerance) | still below iter-0 floor |
| 3 | — | — | — | — | KILLED at selfplay per user instruction |

R14 crossover at iter-1: `val_mse` 0.81 / `pearson` 0.47 / ratio 1.42, NOT
crossed. R14 crossover at iter-2: `val_mse` 1.09 / `pearson` 0.29 / ratio
1.92, NOT crossed.

**C8b** — same recipe but `--mcts-leaf value-head` from raw v3.2 init (C7
builder output). Launch: `runs/R16-P2-c8b-vhleaf/launch.sh`.

| Iter | Wilson-lower | Win-rate | Promoted? |
|---|---|---|---|
| 0 | 0.3798 | 0.4667 | yes (iter-0 floor squeak) |
| 1 | 0.3090 | 0.3917 | no |

R14 crossover at iter-1: `val_mse` 0.79 / `pearson` 0.42 / ratio 1.39, NOT
crossed. KILLED after iter-1.

**C8c** — `--mcts-leaf value-head` from C8 iter-0 ckpt (the 0.5955
best-promoted v3.2 rollout-leaf ckpt). Launch:
`runs/R16-P2-c8c-vhleaf-from-c8iter0/launch.sh`.

| Iter | Wilson-lower | Win-rate | Promoted? |
|---|---|---|---|
| 0 | 0.3481 | 0.4333 | yes (iter-0 floor squeak) |

KILLED after iter-0.

### 4. Load-bearing interpretations

1. **C8 iter-0 +6.8pp is REAL learning, not variance.** C7's
   `make_v32_slot_token_init.py` produced `delta_logits=0.000e+00` and
   `delta_value=0.000e+00` bit-exact against the v3.0 source (the
   `uma_slot_encoder.2.weight` zero-init invariant guarantees additive-tail
   output equivalence at t=0). The lift came entirely from the v3.2 vertical
   training pulling signal through the new `uma_slot_encoder` branch — the
   slot-token state representation can be learned.

2. **W6 loop dynamics broke the iter-0 lift.** C8 iter-1 regressed −6.8pp to
   0.5273, bit-identical to R110 iter-0; iter-2 only partially recovered to
   0.5612. The faithful R110 mirror uses
   `--no-w6-fix-cross-iter-replay --no-w6-fix-fixed-kl-anchor` (R110 pre-dated
   the W6 recipe-fix flag system). The iter-0 lift is exactly the kind of
   signal W6-fix-ON was designed to preserve — and was not tested on this
   recipe.

3. **Value-head-leaf is decisively broken for v3.2.** C8b iter-0=0.3798 and
   C8c iter-0=0.3481 both well below C8 rollout-leaf's 0.5955. C8c (init from
   stronger ckpt) being WORSE than C8b (init from raw ckpt) falsifies the
   "trained-ckpt unlocks vhleaf" hypothesis. Consistent with R13.W8 history
   (v3.0 vhleaf regressed every iter: 0.452→0.404→0.340→0.380). The
   remaining vhleaf hypothesis "value head needs better calibration" is what
   `value-head-data-program` Stage 1 tests.

4. **C8 acceptance gate result: NO-GO.** Best v3.2 Wilson-lower = 0.5955 (C8
   iter-0), 0.0087 below the 0.6042 production ceiling. 96-d production pin
   unchanged.

5. **NEW FORWARD LINE OPENED.** The C8 iter-0 +6.8pp lift is the strongest
   slot-tokens-or-anything signal in months. It re-opens
   `w6-loop-anti-degradation` (queue item, Ceiling Path A) as the natural
   next experiment: re-run the C8 recipe but DROP the `--no-w6-fix-*` flags
   (let W6-fix defaults to ON: cross-iter replay + fixed KL anchor). If
   W6-fix-ON over-damps (per R111 history), tune
   `--kl-anchor-weight` (currently 0.05 default; consider 0.02 or anneal),
   `--w6-replay-window`, `--w6-replay-old-fraction`. The 0b queue entry's
   scope already covers this exact tuning.

### 5. Cross-references

- Scope / P2 contract + chunk plan:
  `docs/ai-research/scoping/r16-model-feature-backlog-refinement.md`
  § "P2 - Per-Uma Slot Tokens" + § "P2 - Chunk Plan (kickoff 2026-05-21)"
- v3.0 R110-W6-repro baseline (0.6042 ceiling, schema-independent W6
  pattern): `docs/ai-research/progress/r110.md`
- Forward line: queue item `w6-loop-anti-degradation`
- vhleaf fallout: queue item `value-head-leaf-recipe-axis` (vhleaf-needs-
  better-state-rep hypothesis falsified; remaining hypothesis owned by
  `value-head-data-program` Stage 1)

---

## R16 Training-Data — Contested-Coverage Pilot

**Verdict: POSITIVE cheap slope at n=300/side, FALSIFIED at n=1000/side
confirmation gate (chunk 5j).** The pilot's monotone slope was a
sampling-noise artifact; the documented "*falsifies* the contested-coverage
bottleneck" outcome per
`scoping/r16-training-data-backlog-refinement.md:100-107` triggered.

### Mechanisms as built (write scope: `training/uma_ai/dataset.py`)

Two independently-selectable knobs, both holding total retained-row count
fixed, both defaulting OFF (bit-identical no-op when unset — verified):

- **Option 1 — contested resampling.** `contested_resample_fraction`
  (`None` = no-op) buffers retained stream, splits on the ≥4-legal threshold,
  draws with replacement from each group to hit target fraction at fixed
  total count (deterministic, RNG seeded by `--seed`).
- **Option 3 — legal-action-count loss weighting.** `contested_loss_weight`
  (×1.0 = no-op) multiplies `_sample_weight` for rows with
  `≥ contested_min_legal` (default 4) legal actions. No corpus/row-count
  change.

Threaded into `train_bc.py` (bc mode only) as `--contested-loss-weight`,
`--contested-min-legal`, `--contested-resample-fraction`. Pilot sweep
exercised Option 1 only. Full mechanism prose + pilot methodology rolled
to `r16-archive.md` § "Contested-Coverage Pilot — Mechanisms +
Methodology". Pre-registered design canonical:
`scoping/r16-training-data-backlog-refinement.md` § "Fork A".

### Pilot sweep (n=300/side, Option 1)

Corpus `runs/R7-multi-teacher-warmstart/iter-000/mixed-v3.jsonl` (3789
retained rows); 8 epochs, h64 d3, seed 7, v3.0/110-d (capacity fixed);
gate `backend/src/sim/evaluateModelVsHeuristic.ts`, rule-bot,
side-balanced, 300/side, seed-start 9000.

| Coverage point | `legal_action_count` | side-bal Wilson-lower (n=300) |
| --- | ---: | ---: |
| baseline | 0.2022 | **0.2416** |
| resample ~30% | 0.3001 | **0.2572** |
| resample ~45% | 0.4500 | **0.2635** |

Monotone increase +2.2pp across full span at n=300; pre-registered cheap
slope acceptance MET — unlocked (did not perform) a larger-n confirmation.

#### Chunk 5j — n=1000 confirmation gate FALSIFIES cheap-slope

Pre-registered n=1000/side confirmation gate run against the same three
swept points (Option 1 contested resampling, same training recipe, same
gate, side-balanced Wilson-lower). Setup-error rule-outs both passed:

- **Sanity check PASSED**: n=300 baseline reproduces pilot 0.2416 within
  tolerance (0.2447, Δ +0.003, well inside ±0.04 Wilson noise).
- **Coverage smoke MATCHED pilot exactly**: `legal_action_count`
  0.2022 / 0.3001 / 0.4500 (data path identical to pilot).

**Confirmation gate at n=1000/side**:

| Coverage point | side-bal Wilson-lower (n=1000) | pilot (n=300) | Δ |
| --- | ---: | ---: | ---: |
| baseline | **0.2840** | 0.2416 | +0.0424 |
| frac030 | **0.2860** | 0.2572 | +0.0288 |
| frac045 | **0.2656** | 0.2635 | +0.0021 |

- **Monotonicity broken**: 0.2840 < 0.2860 > 0.2656 — frac045 regressed
  below baseline.
- **Margin (frac045 − baseline) SIGN-FLIPPED**: −0.0184 (n=1000) vs
  +0.0219 (pilot n=300).
- All three pilot point estimates fall BELOW the n=1000 95% CIs — the
  pilot was on the low tail of sampling noise.
- Worse side stays `player` across all swept points (consistent w/ pilot).

**Verdict.** Contested-coverage bottleneck in its Option-1
(fixed-retained-count) form is FALSIFIED. The pilot's "positive cheap
slope" was a sampling artifact; the n=300 noise band was wider than the
real signal. No remaining autonomous experiment from this line.
Option 2 (oversampling) and Option 3 (loss weighting) untested but the
Option-1 falsification weakens the broader hypothesis. Artifacts:
`runs/R16-TD-coverage-pilot-confirmation/{verdict.json,launch.sh,
baseline,frac030,frac045}`.

## R16 Training-Data — Online MCTS Relabel Infra (3a) — rolled

The entire 3a/3b arm — emit-path, multi-worker harness, 3-recipe production
corpus (11798 rows, Audit-v2 100% PASS), pair-builder + DPO wiring + offline
floor (`pair_accuracy` 0.6981 ref), chunks 5a-5e DPO probe series (best Δ
+0.0043 pair_acc, within ±0.022 noise band), chunk 5f BC-on-relabel
(FALSIFIES candidate-3), chunk 5g candidate-1 kill-test (Case B labels
confidently wrong), chunk 5h ladder scoping, chunk 5i 400-sim argmax-flip
verdict (candidate-1 DIES at 17.3% flip rate) — is DONE-NEGATIVE with no
remaining autonomous action. The active forward direction from this arm is
the user-gated W6 HP-sweep (Ceiling Path A), tracked at queue item
`w6-loop-anti-degradation`. Full prose rolled to
`docs/ai-research/progress/r16-archive.md` § "R16 Training-Data — Online MCTS
Relabel Infra (3a emit-path) — full prose". Secondary infra finding
(relabel-MCTS RNG bleed at 400-sim A/B) tracked at queue item
`mcts-relabel-rng-bleed` (P3, scoped, not on critical path).
