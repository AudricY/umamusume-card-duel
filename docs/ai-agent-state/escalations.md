# Claude Harness Escalations

## Open

Use this file for blockers, unsafe stop lines, missing local artifacts, or human decisions needed before useful autonomous work can continue. ≤500 chars per bullet — longer rationale belongs in a scoping doc.

- **2026-05-15 R14.E user-verified closed — remaining open caveat is the 96-dim/110-dim deployment skew.** R14.E is closed: user manually exercised it in-browser at production SHA `bc6db85`, confirmed both MCTS configs functional and rollout-leaf visibly beating the rule bot; webapp opponent default now the production rollout-leaf config (Wilson lower 0.6479). Numeric latency criterion (median <3s, fallback <5%) NOT captured — closed on user acceptance + prior gate evidence. The `r14-e-manual-ui-exercise` queue item and the prior "autonomous queue exhausted" milestone are resolved. **Remaining real open caveat:** the verified-working stack is the 96-dim production code state (`bc6db85`) + the 96-dim model `runs/R13-W6-phase-d/iter-2/policy.onnx`; `feat/ai` HEAD's feature extractor is 110-dim (R7.b.2) and cannot serve the 96-dim model (`Got: 110 Expected: 96`), and no 110-dim production-grade search model exists (the 110-dim F1 line closed ~0.30). Deployment must ship from the 96-dim code state, pin serving to the 96-dim schema, or build a 110-dim production-grade model (does not exist). This is a human-owned deployment decision, not autonomous /work. Detail: `docs/ai-research/progress/r15.md` § R14.E closure; `docs/r14-sprint-plan.md` § E.

## Resolved

- **2026-05-15 MCTS-distill v1 closeout (FAIL).** Iter-0 SL gate Wilson lower **0.1470** (n=1000 side-balanced, rule-bot, seed-start 9000), point 0.169, on `runs/mcts-distill-v1/iter-000/`. <0.30 → scoping § 6 FAIL + § 5(c): **state-coverage mismatch binding** (mcts-vs-mcts corpus vs raw-policy rule-bot gate) — the pre-registered biggest risk. Soft-visit label shape is the worst F1 iter-0 axis (-15 to -18pp vs R7/R8/R7.b.2). 4 F1 axes exhausted (labels/objective/repr/label-shape). MCTS-distill SL branch closed. Repro-from-item17 blocked by post-R7.b.2 arch incompat (run README); canonical gate verdict intact (commit 73c2d8c). Detail: `progress/r15.md` § MCTS-distill v1. Next-direction: see self-directed bullet below.

- **2026-05-15 self-directed: F1 raw-policy SL line closed across 4 axes → consolidate on search-wrapped path.** With mcts-distill FAIL (0.1470), all four raw-policy SL axes are exhausted: labels (R7 0.2921), objective (R8 0.3318), representation (R7.b.2 0.3045), label-shape (0.1470). Best-ever 0.3318 never approached the 0.40 gate; the cross-axis pattern is a structural raw-policy cap ~0.30 while search-wrapped rollout-leaf MCTS reaches 0.6479 (already the elevated production claim, R14.A footnote). Pick: **do NOT spend compute on `mcts-distill-followup-direction`** — both variants face a ~25pp gap no SL axis has approached; low prior. Demoted P2→P4 deferred-revisit (if ever revisited, §5(c) indicates the rule-bot-mixed-corpus variant, not warm-start). **Next actionable: strategic refinement** reframing the backlog around the working search-wrapped path (R14.E acceptance + MCTS robustness), not raw-policy SL. User feedback basis: make these research-direction calls self-directed. Detail: `progress/r15.md` § MCTS-distill v1; `ai-research-backlog.md` (refinement to follow).

- **2026-05-14 R7.b.2 closeout FAIL + self-directed pick.** Iter-0 Wilson **0.3045** (n=1000) < 0.37 → R7.b closed. F1 axes exhausted: labels 0.2921, objective 0.3318, repr 0.3045. +1.24pp wash vs R7; -9.55pp vs gate. Embedding (+3.5% params) dropped val_acc -3.4pp — input info wasn't binding. Next: **MCTS-distillation** — R12 rollout-leaf MCTS at 0.6479, gap is search→model. `train_bc.py --data-mode mcts-distill` wired. Queued `mcts-distill-scoping` P2. Detail: `progress/r15.md` § R7.b.2.

- **2026-05-14 R8 v1 closeout (FAIL).** Iter-0 Wilson lower **0.3318** (n=1000 side-balanced, β=0.1, item17 ref, 568 kept pairs τ=0.3005). +3.97pp over R7, +2.08pp over item17 — real objective-family lift but -3.59pp vs R15.S3 ceiling 0.3677, -6.82pp vs pre-reg ≥0.40 gate. Wilson < 0.37 → close-out path (scoping § 4); marginal-band β=0.3 follow-up NOT triggered. Both labels (R7) + objective (R8) axes exhausted. **Next pick: R7.b.2 card-embedding feature pass** — feature axis is the surviving F1 lever; promoted P2 ready. Result block: `progress/r15.md` § R8.

- **2026-05-14 self-directed: R15.S3 closeout → R7 picked over R8.** Reward-shape branch closed across both axes (obs-delta saturated 0.368 ±0.001, value-head tempo regressed at coefs 0.05 + 0.01). User feedback: "be self-directed for these kinds of decisions." Pick: **R7 multi-teacher BC blend.** Reasoning: R15.S1 already closed FAIL (compute-scaled single-teacher), so the open SL question is teacher diversity; R7 reuses PPO infra and has cheaper SL-side falsification than R8's pure-DPO bet. Full detail: `docs/ai-research-backlog.md` § R7. Queue: `r7-multi-teacher-warmstart-scoping` P2 ready.

- **Resolved 2026-05-14 self-directed (premature framing).** Prior R15.S3 closeout had only tested axis 1; axis 2 (value-head tempo, Phases O + P) later regressed at coefs 0.05 + 0.01. Both axes now confirmed exhausted. Full per-phase detail at `docs/ai-performance-research-progress.md` §§ Phase L / M / N / O' / O / P.

- **Resolved 2026-05-14 self-directed: R14 cheap-inference claim stance.** Side-asymmetry gate landed at Wilson lower 0.394 — below the 0.40 bar at independent seeds. Three options scoped (CI re-targeting / larger-n re-run / accept FAIL + route to rollout-leaf). Action: (a) docs re-targeted to "Wilson 0.39–0.45 across seed ranges"; (c) rollout-leaf MCTS Wilson 0.6479 elevated as primary production claim; (b) deferred — re-run at larger n didn't change qualitative picture. Full writeup: `docs/r14-sprint-plan.md` (R14.A.footnote) and `docs/ai-research-backlog.md` (R14.A.footnote Result section).
