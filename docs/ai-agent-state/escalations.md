# Claude Harness Escalations

## Open

Use this file for blockers, unsafe stop lines, missing local artifacts, ambiguous research direction, or human decisions needed before useful autonomous work can continue.

- **2026-05-14 — R15.S3 reward-shape branch GENUINELY CLOSED across both axes; surviving F1-line candidates R7 vs R8 need human ranking. (Re-opened from `## Resolved` — see "Resolved 2026-05-14 self-directed: path (a) executed; the prior 'half-day TS change' framing was an over-cautious estimate. Phase O launched." entry below for the prior closeout context.)** Resolved 2026-05-14 self-directed: **path (a) value-head tempo signal was executed and exhausted** (Phases O + P both regressed at coefs 0.05 and 0.01). **Re-opening for human ranking of remaining surviving candidates: (b) R7 multi-teacher labels (SL warm-start rebuild) and (c) R8 DPO (PPO replacement).** The reward-shape branch is fully exhausted. Total R15.S3 branch cost ~36 min compute across 6 phases (L 5m54s + M 6m22s + N 5m25s + O' 5m28s + O 5m22s + P 5m23s). **Six sweeps total covered both axes:**

  - **Axis 1 — hand-engineered observation-delta signals.** 4 phases (L decay 1.0×, M decay 1.75×, N constant 1.0×, O' reduced-mix constant 1.0×). Capped at iter-2 Wilson **0.368 ± 0.001**. Coef magnitude (L vs M, Δ +0.002), schedule (L vs N, Δ +0.012), signal mix (N vs O', Δ +0.000) — all single-axis moves saturated.

  - **Axis 2 — per-step value-head-delta tempo signal.** 2 phases (O coef 0.05, P coef 0.01). Phase O iter-2 **0.3502** (-1.8pp vs Phase N 0.3677, with iter-1 rejected at 0.2768 < 0.2845 floor — over-shape signature). Phase P iter-2 **0.3463** (-2.1pp vs Phase N, AND -0.4pp vs Phase O; clean trajectory, no rejections). **Both magnitudes regressed.** Dimensional check passes (iter-0 shape_attribution.value_head ratio 5.17× matches the 5× coef ratio — signal correctly wired). **Lowering the coef did not help — the value-head-delta signal mechanism is wrong-shape, not wrong-magnitude.** At coef 0.05 the signal was loud and over-shaped the policy; at coef 0.01 the signal is quiet but noisy and contributes random variance without informational gain.

  All six phases PPO-healthy in non-reward dimensions (importance ratios off 1.0, KL bounded, entropy stable, `numerical_anomalies = 0`, WR tracks Wilson). **F1 post-mortem framing — final update.** Prior (`87e9e77`, premature): "gap to 0.40 is categorical — needs a different information source, not more tuning." Phases O + P tested *exactly* that — a different information source — and that source actively regressed. **New: "The F1 reward-shape mechanism cannot break 0.368 from this warm-start. Per-step shaping from any observation-derived signal saturates at 0.368, and per-step shaping from the policy's own value-head delta actively regresses. The remaining F1 moves must change either the warm-start (R7 multi-teacher labels rebuild) or the optimization objective (R8 DPO replacement). The reward-shape branch is closed."**

  **Two surviving F1-line candidates need human ranking, both autonomous-launch-INELIGIBLE (meaningfully different research directions):**
  - **(b) R7 multi-teacher labels** — retrain the DAgger SL warm-start with multiple expert teachers so the policy that PPO inherits is a different starting point; doesn't touch reward, changes SL pipeline. Cheaper to scope; bigger compute commitment if it lifts ceiling.
  - **(c) R8 DPO** — replace PPO with Direct Preference Optimization; doesn't depend on per-step hand-shaped reward signal at all; bigger pivot, more uncertain leverage.

  Path (a-prime) — non-per-step reward-shaping framework (turn-based or game-phase aggregate shaping) — is a fourth, untested possibility but ranks below R7/R8 (still operates on the now-exhausted reward axis). The autonomous loop should NOT pick R7 vs R8 — surface to human. Full synthesis at `docs/ai-agent-state/notes.md` `## F1 reward shaping — scoping (2026-05-14)` Final R15.S3 branch summary block; per-phase writeups at `docs/ai-performance-research-progress.md` §§ Phase L / M / N / O' / O / "Phase P — F1 PPO + value-head tempo signal, coef 0.01 (R15.S3 GENUINE BRANCH CLOSEOUT)"; backlog R15.S3 status flipped to "DONE / EXHAUSTED (both axes closed)"; queue item `r15-s3-branch-synthesis-and-next-pick` re-added P2 ready autonomous-launch-ineligible.

## Resolved

- **Resolved 2026-05-14 self-directed: path (a) executed; the prior "half-day TS change" framing was an over-cautious estimate. Phase O launched.** *(NB this entry was the prior premature R15.S3 closeout — only axis 1 had been tested at the time. Phases O + P then tested axis 2 and both regressed; the genuine cross-axis closeout has been re-filed under `## Open` above. Retained here as historical record of the pre-axis-2 framing.)*

  - **2026-05-14 — R15.S3 observation-delta reward-shaping branch CLOSED at iter-2 Wilson 0.368; surviving F1-line candidates need human ranking.** Four sweeps (Phase L coef 1.0× linear-decay, Phase M coef 1.75× linear-decay, Phase N coef 1.0× constant, Phase O' reduced-mix constant) covered the three single-axis moves available inside the 5-signal mix: coef magnitude moved iter-2 by +0.002 (L→M), schedule by +0.012 (L→N), signal mix by **0.000** (N→O', both at 0.3677 to 4dp). PPO healthy in all four (importance ratios off 1.0, KL bounded, entropy stable, `numerical_anomalies = 0`, WR tracks Wilson). The hand-engineered observation-delta family encodes ~+5pp Wilson over the unshaped Phase H baseline (0.311 → 0.368) but is now fully exhausted — the binding constraint is the *information content* of the signal set, not the signals' weights, schedule, or which subset is active. **Three surviving F1-line candidates need human ranking before further autonomous work:** (a) **value-head tempo signal** — per-step delta of the policy's own value estimate; highest-leverage but blocked on the queued `r15-s3-value-head-trace-instrumentation` (TS-side ONNX call-site + trace schema extension + ONNX export audit, multi-file change crossing TS/Python); (b) **R7 multi-teacher labels** — retrain DAgger with multiple teachers so PPO inherits a different starting policy; doesn't touch reward, changes SL pipeline; (c) **R8 DPO** — replace PPO with a different objective that doesn't depend on hand-shaped reward signal; bigger pivot. F1 post-mortem framing now updated from "0.40 not yet reached; gap is quantitative" to "**reward-shape axis fully explored at 0.368; the gap to 0.40 is categorical — requires a different information source, not more tuning**." Full synthesis in `docs/ai-agent-state/notes.md` `## F1 reward shaping — scoping (2026-05-14)` Phase O' Closeout + 4-axis synthesis block; closeout writeup at `docs/ai-performance-research-progress.md` § "Phase O' — F1 PPO + reduced signal-mix (R15.S3 branch closeout)"; backlog R15.S3 status flipped to DONE / SATURATED-BRANCH.

- **Resolved 2026-05-14 self-directed:** (a) docs re-targeted to "Wilson 0.39-0.45 across seed ranges"; (c) rollout-leaf MCTS Wilson 0.6479 elevated as primary production claim. (b) deferred — re-run at larger n didn't change the qualitative picture.

  - **2026-05-14 — R14 cheap-inference production claim is contradicted at independent seeds; human owns the research-stance decision.** The side-asymmetry confirmation gate (`runs/R14-A-side-asymmetry-seed900000/`) closed FAIL: gate1 (vs rule-bot, seeds 900000+, value-head leaf + c_puct=1.5, n=100) landed at **Wilson lower 0.394** — 0.6pp **below** the 0.40 production bar. Combined with F-iter-2 (0.443 at seeds 820000+) and R14.A (0.404 at seeds 800000+), the empirical range across three independent seed ranges is **Wilson lower 0.39–0.45**, with one-of-three failing the 0.40 bar. The published "Wilson 0.452 at value-head leaf + ratio=1.5" headline (sprint-plan line 121) was the upper end of that range, not a stable point estimate. The R14.A side-asymmetry "partly seed-clustered" caveat is also **refuted** — the player/opponent gap (+0.11–0.30pp Wilson) replicates across four gates and two independent seed ranges. **Three options for the human research-stance decision:** (a) re-target the cheap-inference production claim to a CI phrasing ("Wilson lower 0.39–0.45 across seed ranges"); (b) re-run R14.A at larger n (e.g. n=500/gate) to widen empirical confidence on the 0.452 number; (c) accept the cheap-inference FAIL and route deployment attention to the rollout-leaf iter-2 production pick (Wilson 0.6479), which this gate did NOT test and which remains the unchanged max-strength deployment claim. Full writeup at `docs/r14-sprint-plan.md` (R14.A.footnote) and `docs/ai-research-backlog.md` (R14.A.footnote Result section). This is a research-stance decision, not a launch-and-watch decision.
