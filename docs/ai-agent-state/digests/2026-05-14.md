# 2026-05-14 — R14 closeout + backlog refresh

R14 sprint closed (commit `1eae24c`, "R14.F: re-target sweep on iter-2"). All code-side
workstreams (A, B, E, F, G, I) DONE; only the manual 20-game UI exercise from R14.E remains.
Production picks: rollout-leaf MCTS at iter-2 (Wilson 0.6479) for max strength, value-head leaf
+ adaptive ratio=1.5 (Wilson 0.452, ~0.5s/decision) for cheap-inference deployment. See
`docs/r14-sprint-plan.md` Progress section and `docs/ai-research-backlog.md` for full evidence.

F1 PPO post-mortem at the tail of `docs/ai-performance-research-progress.md` declared the 0.40
Wilson-lower target **unreachable from the current item17 warm-start** and ranked four next moves:
better warm-start, PFSP self-play, richer reward shaping, sampling-temperature gate. All four are
now itemized in `docs/ai-research-backlog.md` under "Post-R14 / post-F1 follow-ups" as R15.S1-S4.

Queue refreshed. New top items: (1) R14.E manual 20-game UI exercise (P1, only open R14
acceptance), (2) F1 self-play readiness scoping (P2), (3) F1 better-warm-start scoping (P2),
(4) side-asymmetry confirmation gate (P3). Bootstrap-loop meta item removed; loop is exercising
itself.

## Slots 2-28 — compressed (canonical detail in progress/notes/backlog)

- **2** F1 self-play scoping GO; queued `f1-self-play-sweep` P2. `notes.md` "F1 self-play readiness".
- **3** F1 better-warm-start scoping GO; queued `f1-warmstart-sweep` P2. `notes.md` "F1 better warm-start".
- **4** `npm run build` PASS ~6s.
- **5** `TMPDIR=/tmp npm run test:train` PASS ~8s (5 smokes green).
- **6** f1-self-play-sweep LAUNCHED — `--rollout-vs-pool` vs W6/iter-{0,1,2}, PID 1755515 → `runs/R14-f1-self-play-sweep/`. Blanket user green-light established. Caveat: opponent sampled uniformly once per run, not per-game PFSP.
- **7** Progress check — iter-0 done at 1:51 elapsed, baseline Wilson 0.145.
- **8** R15.S2 FAIL — iter-2 Wilson **0.2188**, missed 0.40 gate by 18pp; co-adaptation regression. `progress.md` § "Phase J".
- **9** f1-warmstart-sweep LAUNCHED — DAgger 3× games/epochs at 64/2, PID 1876596 → `runs/R15-S1-warmstart-sweep/`.
- **10** Interim — iter-0 gate starting at ~4m elapsed, clean.
- **11** R15.S1 FAIL — iter-2 Wilson **0.3269** inside falsification band [0.291, 0.331]; pre-reg val_acc plateau-then-overfit secondary check fired. `progress.md` § "Phase K".
- **12** side-asymmetry-confirmation-gate LAUNCHED — third seed range 900000/950000, PID 1934035 → `runs/R14-A-side-asymmetry-seed900000/`.
- **13** R15.S3 reward-shaping scoping — GO conditional on ~50-80 LOC ppo_orchestrator diff; 5 signals (Δactive-energy, Δbench-energy, retreat, Δthroughput, Δhp-diff). `notes.md` "F1 reward shaping — scoping".
- **14** gate1 FAIL @ **0.394** (0.6pp below 0.40); gate2 0.482 PASS. R14 cheap-inference range now 0.39-0.45 across 3 seed ranges (1-of-3 fails 0.40). Player/opponent gap real. Escalation filed.
- **15** R14 cheap-inference escalation RESOLVED self-directed (a)+(c). Sprint-plan + backlog re-cited. Zero compute.
- **16** R15.S3 sweep LAUNCHED — orchestrator-only ~100 LOC (5 `--reward-*-coef` args, `--reward-shape-start/end`, `shape_attribution` event). PPO smoke PASS. PID 2398458 → `runs/R15-S3-reward-shaping-sweep/`. Coefs 0.02/0.02/0.03/0.02/0.05, decay 1.0→0.0.
- **17** Phase L PARTIAL @ **0.3560** — +4.5pp over phase H 0.3109, first F1 to exceed SL cap. Missed 0.40 by 4.4pp. ratio_max 19/33/11. `progress.md` § "Phase L".
- **18** r15-s3-followup-tune-shaping LAUNCHED — 1.75× coefs, PID 2543462 → `runs/R15-S3-followup-tune/`. Pidfile race first observed.
- **19** Phase M SATURATION — iter-2 Wilson **0.3580** (Δ +0.002 vs L). Iter-1 REJECTED. Coef-magnitude axis closed. `progress.md` § "Phase M".
- **20** Phase N constant-shape LAUNCHED — `--reward-shape-end 1.0`, PID 2663413 → `runs/R15-S3-constant-shape/`.
- **21** Phase N new F1 ceiling @ **0.3677** (+1.2pp over L). Three configs land 0.356/0.358/0.368 → binding constraint is signal set itself. `progress.md` § "Phase N".
- **22** Phase O original BLOCKED on TS-side trace instrumentation (placeholder `value_pred=0.0`); filed P3 `r15-s3-value-head-trace-instrumentation`. Pivoted to Phase O' reduced-mix (drop bench-energy + retreat, scale active-energy + throughput), PID 2782486 → `runs/R15-S3-reduced-mix/`.
- **23** Phase O' axis-1 CLOSED @ **0.3677** (Δ +0.0000pp vs N). 4-axis synthesis: coef +0.002, schedule +0.012, signal-mix +0.000. Obs-delta family saturated at 0.368 ± 0.001. ~23 min axis-1 cost across 4 phases. `progress.md` § "Phase O'".
- **24** VH trace instrumentation TS scoping — ~4-6 LOC across `evaluateModelVsHeuristic.ts`/`relabelDecisionTrace.ts`; ONNX/server already correct. `notes.md` "F1 reward shaping value-head tempo — TS instrumentation scoping".
- **25** Phase O end-to-end execution — TS diff (~7 LOC prod + 13 LOC smoke) + orchestrator ~30 LOC additive (`--reward-value-head-coef`, `value_head` attribution, per-step delta). `test:train` + `test:ppo-smoke` PASS. Phase O launched PID 2904732 → `runs/R15-S3-value-head-tempo/` at coef 0.05.
- **26** Phase O REGRESSED — iter-2 Wilson **0.3502** (Δ -0.018 vs N). iter-1 rejected at 0.2768 < 0.2845 floor. Mechanism: VH Tanh delta ±2/step at coef 0.05 = ±6/game vs ±0.3 budget (20× too loud); `shape_attribution.value_head 26.01→9.28→8.25`. `progress.md` § "Phase O". Phase P launched at coef 0.01 (5× smaller), PID 3017314 → `runs/R15-S3-value-head-tempo-low/`.
- **28** Phase P REGRESSED — iter-2 Wilson **0.3463** (Δ -0.021 vs N, Δ -0.004 vs O). Trajectory clean (no rejections), trained 5m23s. Mechanism check: iter-0 `shape_attribution.value_head 5.03` vs Phase O 26.01 = 5.17× matches 5× coef ratio → signal correctly wired; VH-delta mechanism is wrong-shape, not wrong-magnitude. **R15.S3 BRANCH CLOSED across both axes** (axis 1 obs-delta saturated 0.368 ± 0.001 over L/M/N/O'; axis 2 VH tempo regressed at coefs 0.05 + 0.01). Total branch ~36 min compute over 6 phases. Surviving F1 moves: change warm-start (R7) or objective (R8). Full detail: `progress.md` § Phase P (~L1412), `notes.md` "Final R15.S3 branch summary" (~L676), `escalations.md` `## Resolved` 2026-05-14 self-directed bullets, `backlog.md` R15.S3 DONE/EXHAUSTED (~L2129).

Pidfile race recurred at slots 18/20/22/25/26 — `nohup ... &` captures wrapper PID not orchestrator's real PID. Open follow-up; not yet a queue item.

## Slot 29 — digest rollforward + self-directed R7 pick (2026-05-14)

- Implementer compressed digest 832 → 102 lines per new CLAUDE.md ≤150 cap; queue.json `updated` `"2026-05-14T28"` → `"2026-05-14"`.
- **Self-directed: R7 (multi-teacher BC blend) > R8 (DPO).** Reason: R15.S1 closed FAIL (single-teacher compute scaling); R7 tests teacher diversity, reuses PPO infra, cheaper SL-side falsification. Full reasoning: `escalations.md` `## Resolved`, queue item `r7-multi-teacher-warmstart-scoping`.
- User feedback: don't escalate research-direction picks; pick + reason. Saved as durable memory.
- Pidfile race observed across slots 18/20/22/25/26 — open follow-up; not yet a queue item.

## Slot 30 — R7 scoping doc authored (2026-05-14)

- New scoping doc: `docs/ai-research/scoping/r7-multi-teacher-warmstart.md` (113 lines). First file in the new `docs/ai-research/scoping/` directory per `1d084b3` CLAUDE.md structure.
- **Cost-reducer found:** `training/train_bc.py:361-369` already implements soft-target CE (added in R12 phase C). R7 build = relabel + dataset loader only, no trainer change. Cost estimate revised: ~2h code + ~30min SL train + ~5min PPO sweep.
- **Three teachers already exist** as trace selectors in `backend/src/sim/evaluateModelVsHeuristic.ts:709/754/773` (rollout-CRN, search, planner). Step 1 of execution plan: pre-flight pairwise-teacher-agreement probe as cheap go/no-go before full trace-gen. Self-directed mitigation for risk (b) — kept in-scope, not surfaced.
- Queue: `r7-multi-teacher-warmstart-scoping` → done; added `r7-pre-flight-teacher-agreement-probe` P2 ready autonomous-launch-eligible and `r7-backlog-shrink-to-pointer` P3 ready (doc-hygiene followup).

## Slot 31 — R7 pre-flight teacher-agreement probe GO (2026-05-14)

- New probe: `backend/src/sim/r7TeacherAgreementProbe.ts` (~424 lines). Exports added on three selectors (`chooseRolloutAction`/`chooseSearchAction`/`choosePlannerAction`), no behaviour change. Output JSON: `runs/R7-pre-flight-teacher-agreement/probe-300-20260514T061913Z.json` (N=300, 1m 46s wall-clock).
- **Verdict: GO.** rollout↔search 0.800, rollout↔planner 0.180, search↔planner 0.230, all-three-agree 0.133, mean normalised mixture entropy 0.618. Rule: rollout↔search ∈ [0.30, 0.85] AND all-three-agree < 0.50.
- **Diversity source: planner specifically.** Rollout and search agree at 0.80 (same rollout-heuristic leaf, differ on search depth/CRN). Planner enumerates turn-bundles — structurally different rule — and disagrees with both at 0.18/0.23. R7 hypothesis confirmed.
- **Performance note:** planner ~14× slower per state than rollout (315ms vs 13.4ms). Projects +5–6 min trace-gen wall-clock per iter at R15.S1's row budget. Keep `plannerMaxSequences=64` cap.
- **R7.b backlog entry added** (implementer-authored during investigation): feature-representation expansion (card embeddings + recent-action history) as contingent structural lever distinct from R7 (labels) and R8 (objective). Catalogs latent progress-doc pointers at L362/L1547/L1625/L1727. Queued behind R7.
- Validation: `npm run build` PASS, `TMPDIR=/tmp npm run test:train` PASS. Queue: probe → done, `r7-step-2-schema-change` P2 ready.

## Slot 32 — R7 step 2 multi-teacher trace schema landed (2026-05-14)

- `DecisionTraceRow.teacher` → `teachers: Array<...>` (length 1–3); back-compat preserved. `chooseTraceTeacher` calls all requested selectors with per-teacher sub-seeds (`${baseSeed}:rollout|search|planner`). `parseTraceTeacher` accepts comma-separated CLI. Relabel emits `policyTargets: number[]` with uniform 1/K weights; `selectedActionIndex` becomes the argmax.
- New smoke `backend/src/tests/multiTeacherTraceSmoke.ts` (~230 lines) asserts: 3-teacher path has `teachers.length === 3`, all selections present, `policyTargets` sums to 1±1e-6 with nonzero mass on each teacher's bucket; single-teacher path produces length-1 array + one-hot. Registered in `test:train`.
- Coverage check ran 60 multi-teacher rows; **29 had >1 legal action** — single-action rows correctly short-circuit (no teacher emission). Step 3 must treat absent-`policyTargets` rows the same.
- Validation: `npm run build` PASS, `TMPDIR=/tmp npm run test:train` PASS (all 6 smokes, ~60s). Queue: step 2 → done, `r7-step-3-python-dataset-loader` P2 ready.
- Step-3 tactical note from implementer: mirror the existing MCTS-visits `policyTargets` pattern at `training/uma_ai/selfplay_dataset.py:125-135`; `train_bc.py:361-369` already has the soft-CE branch.

## Slot 33 — R7 step 3 Python dataset loader landed (2026-05-14)

- `PolicySample.policy_target: np.ndarray | None` added to `training/uma_ai/dataset.py`. `_policy_target()` parses TS-side `policyTargets` field, validates shape + sum, raises on malformed (fail loud, not silent corruption).
- `collate_policy_batch` emits padded `(B, max_actions)` `policy_targets` tensor only when EVERY sample in the batch is soft. **Mixed batches fall back to hard-CE** — synthesising a fake one-hot for legacy rows would change loss surface; corpus mixer (skipped in v1 per scoping § 3) is responsible for batch homogeneity.
- New smoke `training/smoke_e2e.py::assert_multi_teacher_dataset_loader`: covers all-soft batch (rows with mass on 2+ indices), all-hard batch (legacy `examples.jsonl` → `policy_target is None`), mixed batch (key omitted), and one-hot-equals-hard-CE numerical equivalence (`torch.allclose`, atol=1e-5).
- Validation: `TMPDIR=/tmp npm run test:python-train` PASS ~6 min (includes new R7 smoke; multi-teacher trace produced 51 rows, all `teachersPerRowHistogram: {3: 51}`). `TMPDIR=/tmp npm run test:train` PASS ~2 min.
- Queue: step 3 → done, `r7-step-4-trace-gen-and-sl-train` P2 ready autonomous-launch-eligible.
- User/linter added a refined R7.b plan to the queue (step 0 trace-reencodability spike, step 1 hygiene wire-unused-fields, step 2 card-embedding pass parked behind R7) — referenced scoping doc `docs/ai-research/scoping/r7b-feature-representation.md`. Out-of-band R7.b work; R7 line continues unchanged.

## Slot 34 — R7 step 4 LAUNCHED (2026-05-14)

- Background launch: PID **3211541**, run dir `runs/R7-multi-teacher-warmstart/`, start 2026-05-14T06:45:23Z. ETA 15–25 min (less than the conservative ~1hr in last slot).
- **R15.S1 budget matched byte-for-byte** (games=90, epochs=75, hidden=64/depth=2, max-steps=500, replay-games=100, rollout-steps=500, rollout-crn=3, eval-games=250). Rule-bot replay produced 4432 examples — identical to R15.S1 iter-1. Only axis that differs: `--trace-teachers rollout,search,planner` (vs R15.S1's single rollout).
- **Orchestrator fix:** added `--trace-teachers` as a separate free-form flag in `training/dagger_orchestrator.py` (the existing `--teacher` was `choices=[rollout,search,planner]` argparse-bound, would have rejected the comma list). `--teacher rollout` still controls iter-0 selection; `--trace-teachers` flows through to TS-side `parseTraceTeacherList`.
- Sanity check at 1m32s: orchestrator alive, `events.jsonl` shows `run_started` + `rule-bot-replay completed (4432 rows)`, trace-gen running game 21/180.
- Queue: step 4 → running; `r7-step-4-monitor` P2 ready follow-up. Next /loop slot polls PID + reads `iter-000/gate.manifest.json` for `wilson_lower`.

## Slot 35 — doc-hygiene during R7 step 4 wait (2026-05-14)

- R7 step 4 progress check at 5m25s: PID 3211541 alive, eval 68/180 (ETA 8.8 min trace-gen), no failures.
- **Digest trim before append:** slot 28 verbatim block (53 lines, R15.S3 closeout) compressed to one-liner under "Slots 2-28" header — canonical detail lives in `progress.md` § Phase P, `notes.md`, `escalations.md` `## Resolved`, `backlog.md` R15.S3. Digest 150 → 96 lines.
- **R7 backlog shrink-to-pointer** (P3 done): `docs/ai-research-backlog.md` § R7 forward brief (18 lines) collapsed to one-line pointer mirroring R7.b's compact form. Summary line at L93-97 also updated (status DEFERRED → RUNNING with launch-time + PID). R8 entry left expanded (no scoping doc yet, per queue item's pair-with-R8 note). Backlog 256 → 238 lines.
- No code touched; doc-hygiene-only coherent workstream. Next /loop slot still picks up `r7-step-4-monitor`.

## Slots 36-37 — R7.b.0 spike + pidfile-race filed (compressed)

- **36** R7.b.0 trace-reencodability spike CLOSED — VERDICT YES. R14/R15.S1 trace JSONLs schemaVersion=1 carry raw `observation` (full PublicObservation) + `legalActions[i].{id,kind,phase,payload,features}` with payload `targetUid`/`handIndex`/`attackIndex`. Every R7.b schema bump (v2.1 → v3 → v3.x) costs only feature re-extraction, not DAgger regen. Result at `docs/ai-research/scoping/r7b-feature-representation.md` § 9.
- **37** Pidfile-race tracking item filed (`harness-pidfile-race-fix` P4). Working hypothesis: `nohup × npm run` wrapper-PID race; slot-34 direct-python invocation didn't trigger across 4 checks. Low-priority workaround: direct-python.

## Slots 38-39 — R7 step 4 verdict-path + event-grep prep (compressed)

- **38** Verdict-path correction in `r7-step-4-monitor`: Wilson lower at `orchestrator-state.json::iterations[0].wilson_lower` and `gate.manifest.json::summary.wilson95.lower` (not top-level). R15.S1 iter-0 0.2844 as anchor. FAIL-case writes to new `progress/r15.md`.
- **39** Event-grep tightened: orchestrator completion is `{stage:orchestrator,event_type:run_completed,iteration:-1}` (NOT iter=0). Per-iter sequence trace-gen → relabel → mix → train → gate → decision → completed. `summary` carries modelWinRate, byModelSide, terminalReasons.

## Slots 40-41 — R7 closeout + R8 scoping (compressed)

- **40** R7 step 4 CLOSEOUT FAIL: iter-0 Wilson **0.2921** (n=500, rule-bot), Δ +0.0077 vs R15.S1 0.2844 — wash at Wilson half-width, pre-reg SL gate ≥0.35 missed by ~4pp. Plateau-then-overfit signature replicated. Result block at `progress/r15.md` § R7. Self-directed next pick: R8 (DPO) > R7.b.2 (cheaper-to-falsify, ~3h vs ~2d).
- **41** R8 scoping doc landed at `scoping/r8-dpo.md` (144 lines). Trainer = new `train_dpo.py` sibling (per-action gather + frozen-ref); β=0.1; reference = item17 (Wilson 0.311). CRITICAL: dagger-trace JSONLs lack margin — must use `exportOutcomeTrainingExamples.ts` corpus. Risk (c): BT support constraint may bound DPO from above.

## Slots 42-44 — R8 DPO end-to-end (compressed)

- **42** R8 step 1 trainer scaffold + corpus regen: `training/pair_corpus.py` 257 + `train_dpo.py` 332 + `smoke_e2e.py +151`; corpus `training/runs/r8-outcome-crn/examples.jsonl` 4094 rows; τ probe 0.229 → 568 kept pairs (13.9%). Smoke PASS 22.9s.
- **43** R8 step 2a TRAIN: ~30s on CUDA, 568 kept pairs (τ=0.3005); loss 0.683→0.615; val_acc 0.628→0.681; policy margin 1.47→3.02. ONNX roundtrip PASS. Gate split off as 2b.
- **44** R8 v1 CLOSEOUT FAIL: iter-0 Wilson **0.3318** (n=1000, side-balanced) < pre-reg 0.40 gate AND < 0.37 marginal-band trigger. +3.97pp vs R7, +2.08pp vs item17, -3.59pp vs R15.S3 ceiling. New driver `training/r8_gate_eval.py` (~150 LOC). R7.b.2 promoted as surviving lever. Detail: `progress/r15.md` § R8 (lines 143-278); `escalations.md` `## Resolved` 2026-05-14 R8 closeout bullet.

## Slot 45 — R7.b.1 hygiene wire-unused-fields LANDED (2026-05-14)

- Schema v2.1 additive: STATE_DIM **96→110** (+14 slots: [96] firstPlayer polarity, [97:100] pendingChoiceKind one-hot, [100:110] per-uma toolCardId hash). `STATE_FEATURE_SCHEMA_VERSION` 2→2.1. TS-side already emits all three fields; zero TS edits.
- Files: `training/uma_ai/features.py` +63 (helpers + ablation `state_hygiene_v21`), `training/train_bc.py` +1 (ablate choice). All STATE_DIM consumers auto-pick via constant. Old 96-d checkpoints fail loud (strict load + `export_onnx.py:17-21`).
- Smokes (TMPDIR=/tmp): `npm run build` PASS, `npm run test:train` PASS (6/6 TS), `npm run test:python-train` PASS — manifest reports `state_dim=110`, `state_feature_schema_version=2.1`, ONNX roundtrip PASS. Result block at `scoping/r7b-feature-representation.md` § 10.
- Followup: SL gate retrain on R7 corpus deferred — R7.b.2 reuses the same re-extracted corpus. Queue `r7b-hygiene-wire-unused-fields` → done.

## Slot 46 — R7.b.2 execution plan refined (2026-05-14)

- Concrete pre-flight plan for bundled #1+#2 (~515 LOC over 6 phases: TS schema 80, Python encoder 190, ONNX 65, re-extractor 120, retrain 0/~30 min, gate 0). Cost-cutters: train_bc.py ~5 LOC; ONNX opset 17 native Gather; `r8_gate_eval.py` reuses as-is; TS `/predict` unchanged.
- Corpus pick: R7's 12387-row mixed over item17's 3870-row (apples-to-apples vs prior best on this schema). Result at `scoping/r7b-feature-representation.md` § 11.

## Slots 47-50 — R7.b.2 Phases 1-4 LANDED (compressed)

- **47-48** Phase 1 TS schema bump (`PublicObservation.schemaVersion 1→2`, `cardIdsByZone`, per-action src/tgt idx; new `shared/src/cardVocab.ts`; +193 LOC); Phase 2 Python encoder (`nn.Embedding(108,32,padding_idx=0)` additive on state, src+tgt concat on action, `STATE_FEATURE_SCHEMA_VERSION 2.1→3.0`, model kwargs OPTIONAL; +556/-10 LOC).
- **49** Phase 3 ONNX export: two new int64 inputs (`card_ids_by_zone: [B,8,30]` + `action_card_idx: [B,A,2]`); opset 17 native `Gather`. Roundtrip `max_logit_diff=2.38e-7`. +273/-24 LOC across 4 files.
- **50** Phase 4 re-extractor `training/r7b2_extract_features.py` (335 LOC) injects `cardIdsByZone` + per-action src/tgt idx; new smoke `assert_r7b2_extractor_roundtrip` (+166 LOC in `smoke_e2e.py`). All PASS.
- Full detail slots 47-50: `scoping/r7b-feature-representation.md` §§ 12-15.

## Slot 51 — R7.b.2 CLOSEOUT FAIL + MCTS-distill picked (2026-05-14)

- R7.b.2 Phase 5+6 LANDED. Iter-0 Wilson **0.3045** (n=1000 side-balanced, seed-start 9000) on `runs/R7b2-card-embed/iter-000/`. +1.24pp wash vs R7 0.2921; -6.32pp vs R15.S3 ceiling 0.3677; -9.55pp vs pre-reg 0.40 gate. Wilson < 0.37 → branch closed (scoping § 7). Train: train_acc 0.971 / val_acc 0.713 vs R7 baseline 0.965 / 0.747 — embedding (+3.5% params) made the overfit *worse*. Surprise: 12387-row corpus filters to 3789 trainer samples (same as R7 baseline at v2.1 schema; apples-to-apples).
- **F1 axes all exhausted**: labels (R7 0.2921), objective (R8 0.3318), repr (R7.b.2 0.3045). Self-directed next pick: **MCTS-distillation** — R12 iter-2 rollout-leaf MCTS at Wilson 0.6479; gap is search→model, not architecture/labels/objective. `train_bc.py --data-mode mcts-distill` (line 887) already wired. Pre-reg gate consistent: Wilson ≥ 0.40 at iter-0 SL from phase-H-scale retrain. Scoping queued `mcts-distill-scoping` P2; investigator first locates label source.
- Detail: `progress/r15.md` § R7.b.2 (lines 280-...); `escalations.md` `## Resolved` 2026-05-14 R7.b.2 bullet; backlog R7.b collapsed to one-line pointer. Queue: R7.b.2 → done; R7.b.3 + R7.b.4/5 → superseded; new P3 `model-py-amp-overflow-fix` filed (fp16 overflow at model.py:199 during first R7.b.2 launch attempt; workaround was drop --amp).
