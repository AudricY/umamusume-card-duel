# 2026-05-14 — R14 closeout + backlog refresh

R14 sprint closed (commit `1eae24c`, "R14.F: re-target sweep on iter-2"). All code-side
workstreams (A, B, E, F, G, I) DONE; only the manual 20-game UI exercise from R14.E remains.
Production picks: rollout-leaf MCTS at iter-2 (Wilson 0.6479) for max strength, value-head leaf
+ adaptive ratio=1.5 (Wilson 0.452, ~0.5s/decision) for cheap-inference deployment. See
`docs/r14-sprint-plan.md` Progress section and `docs/ai-research-backlog.md` for full evidence.

F1 PPO post-mortem at the tail of `docs/ai-performance-research-progress.md` declared the 0.40
Wilson-lower target **unreachable from the current item17 warm-start** and ranked four next moves:
better warm-start, PFSP self-play, richer reward shaping, sampling-temperature gate. All four are
now itemized in `docs/ai-research-backlog.md` under "Post-R14 / post-F1 follow-ups" as R15.S1-S4.

Queue refreshed. New top items: (1) R14.E manual 20-game UI exercise (P1, only open R14
acceptance), (2) F1 self-play readiness scoping (P2), (3) F1 better-warm-start scoping (P2),
(4) side-asymmetry confirmation gate (P3). Bootstrap-loop meta item removed; loop is exercising
itself.

**F1 self-play scoping run (2026-05-14, second slot of day).** Verdict: **GO** on one cheap PPO
sweep with --rollout-vs-pool against W6/iter-{0,1,2}. Load-bearing distinction: R5 used the
item17 DAgger pool (Wilson ~0.31), W6/iter-2 is Wilson 0.6479 — ~30pp stronger opponent set, a
genuinely different experiment. Confirmed `--rollout-vs-pool` plumbing exists
(`ppo_orchestrator.py:829`, `_select_pool_opponent:836`) but picks one opponent per run
uniformly, not per-game PFSP — acceptable for v1 and flagged in notes. Queue: replaced
`f1-followup-self-play-readiness` with `f1-self-play-sweep` (P2, ready). Config spec in
notes.md "F1 self-play readiness — scoping". Not run.

**F1 better-warm-start scoping run (2026-05-14, third slot of day).** Pre-registered hypothesis:
the DAgger warm-start at hidden=64/depth=2 is compute-starved, not capacity-starved — scaling
games + epochs ~3× at fixed capacity should lift iter-2 greedy Wilson lower from item17's 0.311
to >= 0.45. Proposed config (one line): `dagger_orchestrator.py --games 90 --epochs 75
--iterations 3 --hidden-dim 64 --depth 2 --kl-anchor-weights "0.0,0.1,0.5" --eval-games 250`,
rest unchanged from item17-2026-05-11 take-2. Exit: any iter Wilson lower >= 0.45 at n>=500
side-balanced (success) or 0.311 ± 2pp (falsification, closes branch). Expected cost ~1.5h
compute. Genuinely new relative to R3 (entropy), R4 (value-head), R6 (capacity) — those covaried
other axes; this isolates SL compute. Queue: replaced `f1-followup-warmstart-scoping` with
`f1-warmstart-sweep` (P2, ready, warn-before-launch). Not run.

**Fast repo confidence (2026-05-14, fourth slot of day).** Queue exhausted of autonomous bounded
work — P1 blocked on human, both P2s need compute green-light, P3 is a real evaluation. Picked
the validation tier instead: `npm run build` passes clean in ~6s (TypeScript compile + vite
production build, 129 modules, 14 chunks, no warnings). Repo healthy post-R14 closeout. **Loop
will hit the same exhaustion next firing** unless the human unblocks one of the queued items
or `CronDelete 6f926118` is run to stop the 60s `/loop`.

**Fast repo confidence pt.2 (2026-05-14, fifth slot of day).** Completed the CLAUDE.md fast tier:
`TMPDIR=/tmp npm run test:train` passes in ~8s. All 5 training smokes green:
stateFingerprintSmoke (15 cases), outcomeExportSmoke (4 examples), evalGateSmoke (model served),
daggerRoundSmoke (157 trace rows / 157 relabeled / 196 mixed / 95 rule-bot), aiTrainingSmoke
(8 runs, loss 0.92 → 0.13, top-1 acc 1.0). Training pipeline healthy. Loop still exhausted —
same status as previous firing.

**f1-self-play-sweep LAUNCHED (2026-05-14, sixth slot of day).** User gave blanket green-light
on AI training runs (overrides warn-before-launch). Launched the queued P2 sweep against the
W6/iter-{0,1,2} pool. Command (verbatim, using `training/.venv/bin/python` — system `python3`
lacks torch):

```
nohup training/.venv/bin/python -u training/ppo_orchestrator.py \
  --out-dir runs/R14-f1-self-play-sweep \
  --init-from runs/R13-W6-phase-d/iter-2/checkpoint.pt \
  --rollout-vs-pool \
  --pool-path runs/R14-f1-self-play-sweep/opponent-pool.json \
  --iterations 3 --games-per-update 800 \
  --lr 3e-4 --clip-epsilon 0.3 --entropy-coef 0.01 --ppo-epochs 4 \
  > runs/R14-f1-self-play-sweep/launch.log 2>&1 &
```

Flag corrections vs brief: `--out-dir` (no `--run-name` exists), `--clip-epsilon` (not `--clip`).
Pool composition confirmed: three entries iter-0/1/2 from R13-W6-phase-d, schema matches
`OpponentPool.from_json` (parsed clean via probe). PID **1755515**, log
`runs/R14-f1-self-play-sweep/launch.log`. Startup banner clean: iter-0 rollout running
(~160/1600 traces collected after ~12s, ETA ~1.3 min for iter-0 rollout). Expected wall-clock
~30-45 min for 3 iters per notes. Exit gate: Wilson lower ≥ 0.40 at any iter (R15.S2). Queue
item flipped to `in_progress` with `started_at`, `run_dir`, `pid` fields so the 60s cron
(`6f926118`) doesn't double-launch. Monitor via
`tail -f runs/R14-f1-self-play-sweep/launch.log`, `runs/R14-f1-self-play-sweep/events.jsonl`,
and `runs/R14-f1-self-play-sweep/orchestrator-state.json` as iterations finalize. Caveat
flagged in notes: opponent is sampled uniformly once per run (`_select_pool_opponent`,
`ppo_orchestrator.py:836-863`), NOT per-game PFSP — this is the v1 experiment as scoped.

**f1-self-play-sweep progress check (2026-05-14, seventh slot of day).** At 1:51 elapsed, iter-0
is DONE and iter-1 is STARTING. Much faster than the 30-45 min notes estimate — wall-clock looks
more like ~5 min for the full 3-iter sweep at this machine's speed. Iter-0 (warm-start eval inside
ppo_orchestrator's gate): Wilson lower **0.145**, `promoted=True` (just baseline marker; not a
0.40-gate pass). This is much lower than the W6/iter-2 checkpoint's R14 headline numbers (0.6479
under rollout-leaf MCTS, 0.452 under value-head leaf + ratio=1.5) because the F1 gate here is
presumably greedy-argmax vs rule-bot — directly comparable to F1 phase H's 0.31 number, not the
R14 production numbers. Will wait for the run to finish, then read all three iters at once.

**f1-self-play-sweep closeout (2026-05-14, eighth slot of day).** Run finished 5m32s wall-clock.
Final result: iter-0 (warm-start eval) Wilson **0.1455** (WR 30%) → iter-1 **0.2993** (WR 50%) →
iter-2 **0.2188** (WR 40%), all n=20. **R15.S2 gate (Wilson >= 0.40) missed by 18pp at final and
10pp at the best mid-run iter.** Verdict FAIL. Load-bearing detail: the v1 `_select_pool_opponent`
sampled `R13-W6-phase-d/iter-1` for both iter-0 and iter-1 (uniform-once-per-run is stable across
iters in the current plumbing), then rolled the **just-promoted iter-1 from this very run** as
iter-2's opponent — creating a co-adaptation regression. Mechanism narrative: stronger pool broke
phases 2/G/H's "ratios ≈ 1.0" stasis (iter-0 ratio range 0.05-4.6 vs phase H's ~1.00),
mean_return marched -0.236 → -0.173 → -0.153, entropy contracted 0.293 → 0.267 — the policy
genuinely moved, the largest single PPO step in any F1 phase. But iter-2 against own-recent-self
overfit (ratio max spiked to 21.3) and gate fell back. Comparison: worse than R5 final 0.3109
(weaker pool, didn't move policy as much but didn't regress) and worse than phase H final 0.3109
(no pool, also didn't move policy but landed at the SL cap). What this closes: the strong-pool
self-play branch of the F1 next-moves. Surviving F1 candidates: R15.S1 (better warm-start) and
R15.S3 (richer reward shaping). Writeup at `docs/ai-performance-research-progress.md` § "Phase J
— F1 PPO + strong-pool self-play"; backlog R15.S2 marked DONE / FAIL with Result block. Queue:
removed `f1-self-play-sweep`; new head after the human-blocked R14.E remains `f1-warmstart-sweep`
(P2, user's blanket AI-launch permission allows autonomous launch); `side-asymmetry-confirmation-gate`
promoted to P2.

**f1-warmstart-sweep LAUNCHED (2026-05-14, ninth slot of day).** Now the load-bearing surviving
F1 candidate after R15.S2 (self-play) closed FAIL. Hypothesis: DAgger warm-start at 64/2 is
compute-starved — 3x games / 3x epochs at fixed capacity should lift iter-2 greedy Wilson lower
from item17's 0.311 to >= 0.45. Command (verbatim, using `training/.venv/bin/python` — system
`python3` lacks torch):

```
nohup training/.venv/bin/python -u training/dagger_orchestrator.py \
  --out-dir runs/R15-S1-warmstart-sweep \
  --iterations 3 --games 90 --max-steps 500 \
  --teacher rollout --rollout-steps 500 --rollout-crn-samples 3 \
  --replay-games 100 --relabeled-weight 0.6 --replay-weight 0.4 \
  --epochs 75 --batch-size 32 --hidden-dim 64 --depth 2 \
  --eval-games 250 \
  --kl-anchor-weight 0.1 --kl-anchor-weights "0.0,0.1,0.5" \
  > runs/R15-S1-warmstart-sweep/launch.log 2>&1 &
```

All spec flags mapped cleanly to orchestrator argparse (`dagger_orchestrator.py:1020-1056`); no
flags dropped. `train_bc.py` defaults (lr 3e-4, dropout 0.05, value-weight 0.1, entropy-bonus
0.0) flow through automatically — scoping explicitly said keep defaults so no passthrough
needed. PID **1876596**, log `runs/R15-S1-warmstart-sweep/launch.log`. Startup banner clean:
rule-bot replay generated (100 games / 4432 examples), iter-0 rollout-CRN trace collection
running at ~1.5 games/iter pace (180 trace evals = 90 games x 2 sides; ETA ~4 min for iter-0
rollout phase alone). Expected wall-clock ~1.5h per scoping (n=500 eval x 3 iters adds ~20-30
min, dominant cost is rollout-CRN x3 teacher invocations). Exit gate: any iter Wilson lower
>= 0.45 at n>=500 side-balanced (success) or 0.311 +/- 2pp (falsification, closes
compute-starved branch). Queue: `f1-warmstart-sweep` flipped to `in_progress` with
`started_at`, `run_dir`, `pid` so 60s cron `6f926118` doesn't double-launch. Monitor via
`tail -f runs/R15-S1-warmstart-sweep/launch.log`,
`runs/R15-S1-warmstart-sweep/events.jsonl`, and
`runs/R15-S1-warmstart-sweep/orchestrator-state.json` as iter-0/1/2 finalize.

**f1-warmstart-sweep interim status (2026-05-14, tenth slot of day).** At ~4m04s elapsed (run
start 11:08:36, check at 11:12:40, PID 1876596 alive), iter-0 is past trace-gen, relabel, mix,
and SL train. `[eval N/180]` is the **trace-gen rollout-teacher loop** inside
`evaluateModelVsHeuristic.ts` (180 = 90 games x both sides), not a model gate eval — confirmed by
launch.log L18 showing the `--decision-trace-out` invocation precedes any checkpoint. Trace-gen
completed at events.jsonl ts +207s (8917 trace rows). SL train completed at +254s with final
train acc 0.958 / val acc 0.738 (per-epoch val plateaued ~0.74 around epoch 7 then overfit, as
expected at 75 epochs / 3757 samples). `iter-000/model/checkpoint.pt` (1.2 MB) now exists; the
gate stage **just started** (events.jsonl tail: `stage=gate event=started mode=policy
min_games=500` — confirms `2 * --eval-games / 2` math, side-balanced). **Signal call:** the
0.594 trailing WR is rollout-teacher-vs-rule-bot (no model in the loop), so it tells us only that
the teacher is decisive — NOT a 30-pp lift over item17. Confidence: high (banner + checkpoint
timing). **ETA:** gate at rollout-CRN=3 over 500 games ≈ 12-17 min (trace-gen ran 1.15 s/game,
gate adds model-serve overhead); iter-0 total ~19 min, per-iter ~25 min, full 3-iter sweep
**~75-90 min remaining wall-clock** — on the 1.5h scoping estimate. **Health:** no NaN / OOM /
error lines in launch.log (one benign torch lr_scheduler deprecation warning at L329); event
stream chronological, no skipped stages. No commits since slot 9. **First real signal lands
when the iter-0 gate emits its `completed` event with summary.modelWinRate** — that is the
first apples-to-apples comparison to item17's 0.311.

**f1-warmstart-sweep closeout (2026-05-14, eleventh slot of day).** Run finished 11m 51s wall-clock
(~8x faster than scoping's ~1.5h estimate). Final result: iter-0 Wilson **0.2845** (WR 32.4%) →
iter-1 **0.2883** (WR 32.8%) → iter-2 **0.3269** (WR 36.8%), all n=500 side-balanced vs rule-bot.
**Verdict FAIL — pre-registered falsification confirmed.** Iter-2 Wilson 0.3269 lands inside the
falsification band 0.311 ± 2pp (= [0.291, 0.331], per `notes.md` "F1 better warm-start — scoping"
exit criteria line 141). The +1.6pp lift over item17's 0.311 is within n=500 Wilson half-width
and an order of magnitude below the predicted +14pp. Load-bearing detail: the **pre-registered
secondary check also fired** — per-epoch val_accuracy from `events.jsonl` shows every iter reaches
99% of peak val_acc by **epoch 2-3** (best @ ep7/ep2/ep3 for iter-0/1/2), then *declines* over the
remaining 70+ epochs while train_acc climbs to 0.91-0.97. Classic plateau-then-overfit at fixed
64/2 capacity. This converts the gate-level falsification from "an arbitrary number landed in the
band" into "the predicted falsification mechanism fired in train-time diagnostics first, then
validated at eval time." Comparison: 0.3269 matches the F1 ceiling shared by phase 2/G/H (0.3109),
R5 (0.3109), and item17 take-2 (0.311) — three distinct axes (PPO HPs, opponent pool, SL compute),
one ceiling. What this closes: the compute-starved-at-fixed-capacity branch of the F1 next-moves
(R15.S1). With R15.S2 also FAIL this morning, the two highest-ranked F1 post-mortem next moves
both falsified. Surviving F1 candidates: R15.S3 (richer reward shaping) and R15.S4 (sampling-
temperature gate, diagnostic-only); deeper branches the falsification opens are R7 (multi-teacher
labels) and R8 (DPO). F1's "Wilson lower >= 0.40 from current pipeline" is now empirically out of
reach without a different axis. Writeup at `docs/ai-performance-research-progress.md` § "Phase K
— F1 DAgger compute-scaled warm-start"; backlog R15.S1 marked DONE / FAIL with Result block.
Queue: removed `f1-warmstart-sweep`; `side-asymmetry-confirmation-gate` remains P2 head
(autonomous-launch eligible under user's blanket AI-launch permission); new P3
`r15-s3-reward-shaping-scoping` added as the top surviving F1 candidate.

**side-asymmetry-confirmation-gate LAUNCHED (2026-05-14, twelfth slot of day).** Only autonomous-
launch-eligible item remaining after R14.E (human-blocked) and the two R15 falsifications. Cheap
diagnostic to stick the R14 production claim: R14.A flagged that the player-vs-opponent gap
(player Wilson 0.31, opponent 0.42-0.63) is partly seed-clustered, and A.followup asked for one
independent seed range to confirm. Command (verbatim, using `training/.venv/bin/python` — system
`python3` lacks torch):

```
nohup training/.venv/bin/python -u training/r14_ood_gate.py \
  --iter1-ckpt runs/R13-W6-phase-d/iter-2/checkpoint.pt \
  --r4-ckpt runs/R4-value-head-retrain/checkpoint.pt \
  --out-dir runs/R14-A-side-asymmetry-seed900000 \
  --games 50 --seed-start-fresh 900000 --seed-start-ood 950000 \
  --workers 4 --target-wilson-lower 0.40 \
  > runs/R14-A-side-asymmetry-seed900000/launch.log 2>&1 &
```

Flag clarifications: `--iter1-ckpt` is misnamed but accepts any checkpoint (matches R14.A's prior
use targeting iter-2). `--games 50` × `--model-side both` (hard-coded in script) = n=100 per gate.
Seed ranges intentionally non-overlapping with R14.A's 800000/850000 to give a third independent
range. The script runs both Gate 1 (vs rule-bot, seeds 900000+) and Gate 2 (vs R4 MCTS, seeds
950000+) — Gate 1 is the primary side-asymmetry signal (apples-to-apples with R14.A Gate 1);
Gate 2 is a bonus MCTS-vs-MCTS check. MCTS config locked to the production deployment: value-head
leaf, c_puct=1.5, 100 sims (note: ratio=1.5 in the queue summary refers to c_puct, confirmed by
matching R14.A's script). PID **1934035**, log `runs/R14-A-side-asymmetry-seed900000/launch.log`
(empty — script writes per-gate `gate1-fresh-seeds.log`). Startup banner clean: gate1 progressing
at 11/100 by ~27s elapsed, ETA gate1 ~3.8 min, total run ~10-15 min on this machine. No NaN /
OOM / error in any log. **Exit gate:** next /work firing checks `runs/R14-A-side-asymmetry-
seed900000/summary.json` for `gate1.by_side` (player vs opponent Wilson95 lower) plus
`gate1.wilson_lower` (overall); decision rule from queue is "record per-side Wilson". If
player-side Wilson lower at seeds 900000+ matches R14.A's ~0.31 (within Wilson half-width at
n=50/side), side-asymmetry is real and the production claim should note it; if it lifts to
parity with opponent-side ~0.42-0.63, the gap was seed-clustered and the original A note can
relax. Queue item flipped to `in_progress` with `started_at`, `run_dir`, `pid` so the 60s cron
`6f926118` doesn't double-launch.

**R15.S3 reward-shaping scoping landed (2026-05-14, thirteenth slot of day).** P3
`r15-s3-reward-shaping-scoping` deliverable written to notes.md as new `## F1 reward shaping —
scoping (2026-05-14)` block parallel to the existing warm-start / self-play scoping sections.
Verdict: GO conditional on human accepting a ~50-80 line `training/ppo_orchestrator.py` diff
(orchestrator-only — every signal is already on each trace row's PublicObservation per
`frontend/src/game/engine/ai-policy/observation.ts:6-60`). Five proposed signals:
Δactive-energy-total, Δbench-energy-total, retreat-event indicator, Δcard-throughput,
Δactive-hp-relative; aggregate per-game shaped sum sized to ±0.3 so it stays sub-dominant to
±1.0 terminal. Implementation site:
`training/ppo_orchestrator.py:parse_trace_to_trajectories` lines 715-740 (current shape
`reward = α*Δpoints + β*win_indicator`, α=1/3, β=1 — lines 725, 739, defaults 811, 813).
Linear-decay schedule pre-registered (iter-0 full / iter-1 0.5× / iter-2 0×). Exit gate:
Wilson lower ≥ 0.40 at n=500 vs rule-bot (success) or 0.311 ± 2pp (falsification, split into
"reward moved gradient but didn't win" vs "even shaping didn't move ratios" branches — re-ranks
R7 vs R8 differently per which fires). Compute estimate ~10-15 min; code work ~1.5h. Queue:
`r15-s3-reward-shaping-scoping` flipped to `status: "done"` with awaiting-human-decision summary
(load-bearing nuance: this is NOT a launch-and-watch decision like R15.S1/S2 were — code lands
first). No code written, no compute spent.

**side-asymmetry-confirmation-gate CLOSED — gate1 FAIL @ Wilson 0.394 (2026-05-14, fourteenth
slot of day).** The diagnostic that was supposed to "stick the R14 production claim" instead
**contradicted it**. Run finished 9m 38s wall-clock. Headline: gate1 (vs rule-bot, seeds 900000+,
value-head leaf + c_puct=1.5, n=100) **Wilson lower 0.394** [0.394, 0.587] — **0.6pp below the
0.40 production bar**. Gate2 (vs R4 MCTS, seeds 950000+, MCTS-vs-MCTS, n=100) **Wilson lower
0.482** [0.482, 0.672] — PASS by 8pp. gap_gate1_minus_gate2 = −0.088. **Two implications, both
material.** (1) The R14 cheap-inference production headline "Wilson 0.452 at value-head leaf +
ratio=1.5" was a single-seed-range datapoint; across F-iter-2 (0.443 at seeds 820000+) + R14.A
(0.404 at 800000+) + this gate (0.394 at 900000+) the empirical range is **Wilson lower
0.39–0.45**, with one-of-three failing the 0.40 bar — the 0.452 number should be cited as the
upper end of a range, not a stable point estimate. (2) The R14.A "side-asymmetry is partly
seed-clustered" caveat is **refuted**: the player/opponent gap (+0.11–0.30pp Wilson) replicates
with the same shape across all four gates and two independent seed ranges (gate1 player Wilson
0.294 vs opponent 0.423, gap +0.129; gate2 player 0.366 vs opponent 0.522, gap +0.156). Real
strategic asymmetry, not seed noise. **What survives:** the R14 max-strength deployment pick
(rollout-leaf MCTS at iter-2, Wilson 0.6479) was NOT tested by this gate and is unchanged.
Writeups: sprint-plan R14.A.footnote with 4-row side-gap table; backlog R14.A.footnote Result
section also with table; old "Open/wild" backlog entry struck through. Queue: removed
`side-asymmetry-confirmation-gate`; remaining items `r14-e-manual-ui-exercise` (P1, human-
blocked) and `r15-s3-reward-shaping-scoping` (P3, done, awaiting human decision). Escalations
updated with a new entry framing the open research-stance decision (re-target / re-run with
larger n / accept and route to rollout-leaf MCTS). Not a launch-and-watch decision; human owns it.

**R14 cheap-inference escalation RESOLVED self-directed via (a)+(c) (2026-05-14, fifteenth slot
of day).** User direction post-escalation was "dont need to ask me for direction. be self
directed". (a)+(c) is the empirically-correct + zero-compute resolution. Surfaces updated:
`docs/r14-sprint-plan.md` Progress outcome paragraph (line 113) — primary production pick is now
rollout-leaf MCTS @ iter-2 Wilson 0.6479, cheap-inference fallback is value-head leaf + ratio=1.5
at the qualified Wilson 0.39–0.45 range with 1-of-3 below the 0.40 bar; the F bullet (line 121)
re-titled to "cheap-inference fallback pick" with the qualified range. `docs/ai-research-backlog.md`
R14 progress checkpoint (line 449), F-iter-2 ratio=1.5 line (498), headline (502), and post-R14
follow-ups intro (639) all re-cited along the same lines. The R14.A.footnote sections in both
docs (sprint-plan 123-136, backlog 538-567) untouched — they already carried the correct framing
that this resolution propagates. Escalation moved from `## Open` to a new `## Resolved` section in
`docs/ai-agent-state/escalations.md` with the resolution line prepended; original entry preserved
intact for audit. (b) — re-run R14.A at larger n — deferred; would not change the qualitative
picture across three seed ranges. Zero compute spent. Queue unchanged (R14.E P1 human-blocked,
R15.S3 P3 done). Likely next bounded job: R15.S3 reward-shaping implementation (orchestrator-only
~50-80 LOC, pre-scoped in notes.md) pending the human go/no-go that's already itemized.

**R15.S3 reward-shaping sweep LAUNCHED (2026-05-14, sixteenth slot of day).** Self-directed GO
on the pre-registered F1 reward-shaping experiment (notes.md `## F1 reward shaping — scoping`).
Orchestrator-only diff to `training/ppo_orchestrator.py` (~100 LOC): added 5 new
`--reward-{active-energy,bench-energy,retreat,throughput,hp-diff}-coef` args (default 0.0
each → identical numerics to phase H when unset), `--reward-shape-start/--reward-shape-end`
pair driving linear iter-decay (default 0.0/0.0 → no shaping), and a `shape_attribution` dict
on the `trajectory-parse/completed` event for the branch-1/branch-2 falsification split.
Inside `parse_trace_to_trajectories` the row loop now also computes per-step Δ-active-energy,
Δ-bench-energy, retreat-event indicator, Δ-throughput (handCount + len(discard)), and Δ-hp-ratio
all from already-loaded PublicObservation fields — schema matches scoping exactly (no field
renames needed; `discard` is a list so `len()`, `bench` is nullable-slot list). Function signature
changed: returns 3-tuple `(n_episodes, n_transitions, attribution)` — single caller at line 269
updated. `TMPDIR=/tmp npm run test:ppo-smoke` **PASS** post-diff (2 iters, 32 events, both
promoted, KL/entropy/ratio fields present). Launch command (verbatim, `training/.venv/bin/python`
— system `python3` lacks torch):

```
nohup training/.venv/bin/python -u training/ppo_orchestrator.py \
  --out-dir runs/R15-S3-reward-shaping-sweep \
  --init-from runs/item17-2026-05-11/iter-002/model/checkpoint.pt \
  --iterations 3 --games-per-update 800 \
  --lr 3e-4 --clip-epsilon 0.3 --entropy-coef 0.01 --ppo-epochs 4 \
  --reward-alpha 0.333 --reward-beta 1.0 \
  --reward-active-energy-coef 0.02 \
  --reward-bench-energy-coef 0.02 \
  --reward-retreat-coef 0.03 \
  --reward-throughput-coef 0.02 \
  --reward-hp-diff-coef 0.05 \
  --reward-shape-start 1.0 --reward-shape-end 0.0 \
  --eval-games 250 \
  > runs/R15-S3-reward-shaping-sweep/launch.log 2>&1 &
```

Warm-start path correction vs scoping/brief: actual checkpoint lives at
`runs/item17-2026-05-11/iter-002/model/checkpoint.pt` (the per-iter `model/` subdir holds
`checkpoint.pt` + `manifest.json` — same file phase H's `iter-parent/model/` was seeded from,
verified by size+mtime match). PID **2398458**, log
`runs/R15-S3-reward-shaping-sweep/launch.log`. Startup banner clean: iter-0 rollout running at
~64/1600 trace evals after ~5s, WR ~0.31 (in line with item17 warm-start's 0.311), ETA ~1.3 min
for the iter-0 rollout phase alone. Expected total wall-clock ~10-15 min per scoping
(phase H precedent ~340s for the PPO loop + 3 gates). **Exit gate:** any iter
`gate.manifest.json` Wilson lower ≥ 0.40 at n=500 side-balanced.
**Falsification band:** iter-2 in [0.291, 0.331]. **Branch split:** check
`events.jsonl` `stage=ppo-update event_type=minibatch` `ratio_mean` range per iter — if it's
moved off ~1.00 (matching R15.S2 0.05–4.6) the shaping was non-trivial; if it stayed at ~1.00
PPO didn't move, re-rank R8 over R7. **Diagnostic event:** `trajectory-parse/completed.data
.shape_attribution` per iter exposes which of the 5 components dominated (drop small contributors
in any follow-up). Queue: `r15-s3-reward-shaping-scoping` flipped to `done` (replaced by
`f1-reward-shaping-sweep` `in_progress` with `started_at`, `run_dir`, `pid` — the 60s cron
`6f926118` won't double-launch).

**R15.S3 reward-shaping CLOSEOUT (2026-05-14, seventeenth slot of day).** Verdict: **PARTIAL —
best F1 rule-bot result on record (Wilson 0.3560), neither success nor falsification.** Run
`runs/R15-S3-reward-shaping-sweep/` finished iter-2 at Wilson lower **0.3560** (WR 39.8%, n=500
side-balanced), missing the pre-registered 0.40 success bar by **4.4pp** and landing **+2.5pp
above** the pre-registered falsification band [0.291, 0.331]. Trajectory monotone 0.2730 →
0.2787 → 0.3560 (+8.3pp end-to-end, +7.7pp iter-1→iter-2 — the largest single sustained PPO step
in any F1 phase). All three iters promoted (floor 0.2787, tolerance 0). **+4.5pp absolute over
the prior F1 ceiling** (phase H iter-2 0.3109; R15.S1 iter-2 0.3269; R15.S2 iter-2 0.2188) — first
F1 PPO configuration to materially exceed (not match) the SL cap. **Pre-registered branch-1
mechanism check fired**: importance ratios moved decisively off ~1.00 in every iter (ratio_max
19.06 / 32.59 / 11.12 vs phase H ~1.00) — shaped reward IS moving the surrogate gradient. Entropy
stable (0.168 → 0.154 → 0.153, no collapse), `numerical_anomalies = 0` across all 48 minibatches,
approx_kl_max < 0.02 throughout. Wall-clock **5m 53.7s** end-to-end (~3× faster than scoping
10-15 min forecast). This is a *go-deeper-on-this-axis* result, not a *close-this-branch* result:
the branch is alive; the gap to 0.40 is now quantitative (coefficient scale + signal mix) rather
than mechanistic. **F1 post-mortem update:** the `progress.md:735` "0.40 NOT REACHABLE" line was
qualified (not deleted) — the claim was correct under the unshaped reward mechanism but R15.S3
demonstrably *changed the mechanism*; 0.40 is now reachable in principle from this warm-start.
Files touched: `docs/ai-performance-research-progress.md` (Phase L section + post-mortem
qualification + summary-table row), `docs/ai-research-backlog.md` (R15.S3 Result block, status
DONE / PARTIAL), `docs/ai-agent-state/notes.md` (Closeout block on F1 reward shaping scoping),
`docs/ai-agent-state/queue.json` (removed `f1-reward-shaping-sweep` in_progress + stale
scoping item; added `r15-s3-followup-tune-shaping` P3 ready). **Next move (queued):** hold the
same 5 signals, scale all five coefs 1.5–2×, rerun the 3-iter sweep. Expected ~10-15 min compute.
If iter-2 crosses 0.40 → first F1 success on record; if iter-2 stalls at ~0.36 → coef scaling
saturated and next is signal-mix change. Full writeup: `progress.md` § "Phase L — F1 PPO + reward
shaping".

**r15-s3-followup-tune-shaping LAUNCHED (2026-05-14, eighteenth slot of day).** Self-directed
launch of the queued P3 follow-up — same 5 signals, all 5 coefs scaled **1.75×** (midpoint of
the queued 1.5–2× range). Same warm-start (`runs/item17-2026-05-11/iter-002/model/checkpoint.pt`),
same opponent (rule-bot, no pool), same HPs (`--lr 3e-4 --clip-epsilon 0.3 --entropy-coef 0.01
--ppo-epochs 4`), same code (commit `8da53bd` shaping diff intact, no new diff). Scaled coefs
(verbatim): `active-energy 0.035` / `bench-energy 0.035` / `retreat 0.0525` / `throughput 0.035` /
`hp-diff 0.0875` (1.75× the R15.S3 baseline 0.02/0.02/0.03/0.02/0.05); schedule unchanged at
`--reward-shape-start 1.0 --reward-shape-end 0.0`; also mirrored R15.S3's `--reward-alpha 0.333
--reward-beta 1.0 --eval-games 250` to keep the flag set identical. Per-game shape budget now
~0.23 (vs R15.S3 ~0.13, scoping target ±0.3), still sub-dominant to the ±1.0 terminal. Launch
race: first attempt to capture PID hit a cwd-reset issue and I accidentally launched twice
(PIDs 2515564 + 2516839 both writing to same out-dir); killed both, cleaned the run dir,
re-launched cleanly. PID **2543462** alive at +12s, log
`runs/R15-S3-followup-tune/launch.log`, iter-0 rollout progressing (~234/1600 trace evals, WR
~0.30 in line with item17 warm-start), no duplicates (`ps aux | grep ppo_orchestrator | grep -v
bash | wc -l = 1`). **Exit gate:** any iter `gate.manifest.json` Wilson lower ≥ 0.40 (success);
iter-2 in [0.291, 0.331] (falsification, closes coef-scaling axis); partial = stalled ~0.36 →
coef scaling saturated, pivot to signal-mix change. **ETA:** ~6 min wall-clock per R15.S3
precedent (5m 54s). Queue: `r15-s3-followup-tune-shaping` flipped to `in_progress` with
`started_at`, `run_dir`, `pid` so the 60s cron `6f926118` doesn't double-launch.

**r15-s3-followup-tune-shaping CLOSEOUT — SATURATION as predicted (2026-05-14, nineteenth slot
of day).** Run `runs/R15-S3-followup-tune/` finished 6m 22.3s wall-clock. Final result: iter-0
Wilson **0.2960** (WR 33.6%, +2.3pp vs Phase L iter-0 0.2730, promoted) → iter-1 **0.2825**
(WR 32.2%, **REJECTED** — first F1 sweep-internal regression on record this day, `wilson_lower
0.2825 < floor 0.2960`) → iter-2 **0.3580** (WR 39.8%, rolled forward from iter-0 parent after
iter-1 reject, promoted). iter-2 0.3580 vs Phase L's 0.3560: **Δ +0.002, within Wilson noise**.
Pre-registered "coef saturation" outcome fired cleanly — the (warm-start, opponent, 5-signal,
linear-decay) tuple has a true ceiling at ~0.358; 1.75× coefs moved iter-0 by +2.3pp but did not
compound through decay. Mechanism still healthy: ratio_max 15.86 / 12.50 / 18.82 (gradient
active, comparable to Phase L's 19.06 / 32.59 / 11.12); entropy stable (0.171 → 0.164 → 0.164,
no collapse); `numerical_anomalies = 0` across 48 minibatches; approx_kl_max < 0.02 throughout.
This is a *change-the-signal* outcome, not a *PPO-stopped-working* outcome. Surviving F1 axes:
signal-mix change OR shaping-schedule change. Three candidates pre-scoped in notes.md follow-up
block — constant-shaping schedule (`--reward-shape-end 1.0`, smallest single-axis change,
recommended v1), drop low-attribution signals + scale survivors, add value-head-derived
strategic-tempo signal. Files touched: `docs/ai-performance-research-progress.md` (Phase M
section appended; Phase L "what this opens" table extended; F1 summary line at 736 updated to
cite Phase M saturation), `docs/ai-research-backlog.md` (R15.S3 Follow-up Result block under
existing entry, marked DONE / SATURATED), `docs/ai-agent-state/notes.md` (follow-up block on
F1 reward shaping scoping covering saturation finding + v1 next-move scoping), this digest,
`docs/ai-agent-state/queue.json` (removed `r15-s3-followup-tune-shaping`; added P3
`r15-s3-signal-mix-or-schedule` ready with constant-shaping v1 recommendation). Not committed.

**r15-s3-signal-mix-or-schedule v1 LAUNCHED — constant-shaping (2026-05-14, twentieth slot of
day).** Self-directed launch of the queued P3 v1 — same Phase L 1.0x coefs (active 0.02 /
bench 0.02 / retreat 0.03 / throughput 0.02 / hp-diff 0.05), same warm-start
(`runs/item17-2026-05-11/iter-002/model/checkpoint.pt`), same opponent (rule-bot, no pool),
same HPs (`--lr 3e-4 --clip-epsilon 0.3 --entropy-coef 0.01 --ppo-epochs 4 --reward-alpha 0.333
--reward-beta 1.0 --eval-games 250 --iterations 3 --games-per-update 800`), **only diff vs
Phase L: `--reward-shape-end 0.0` → `--reward-shape-end 1.0`** (constant shaping, no
linear-decay). Tests the schedule axis after Phase M closed the coef-magnitude axis at
SATURATION. Launch hit the same cwd-reset pidfile race as slot-18; recovered cleanly via `ps
aux | grep R15-S3-constant-shape` (single process confirmed). PID **2663413** alive at +11s,
log `runs/R15-S3-constant-shape/launch.log`, iter-0 trace-gen progressing (~74/1600 evals,
WR ~0.31 — in line with item17 warm-start). Banner verbatim confirms `--reward-shape-start
1.0 --reward-shape-end 1.0` and all 1.0x coefs. No duplicates. **Exit gate:** success if any
iter `gate.manifest.json` Wilson lower ≥ 0.40 (first F1 success on record); partial if iter-2
in (0.36, 0.40); reward-hacking if iter-2 ≥ iter-0 by ≥ +5pp Wilson but WR drops vs heuristic
gate (constant shape gamed but games lost — quote WR alongside Wilson); saturation if iter-2
≈ 0.36 (then pivot to v2 signal-mix-change). ETA ~6 min wall-clock (Phase L 5m54s, Phase M
6m22s). Queue: `r15-s3-signal-mix-or-schedule` flipped to `in_progress` with `started_at`,
`run_dir`, `pid` so the 60s cron `6f926118` doesn't double-launch.

**R15.S3 constant-shape CLOSEOUT — Phase N, new F1 ceiling (2026-05-14, twenty-first slot of
day).** Verdict: **PARTIAL — new F1 rule-bot ceiling at iter-2 Wilson 0.3677, +1.2pp over
Phase L.** Run `runs/R15-S3-constant-shape/` finished clean (run_completed, halted=false), all
3 iters promoted, monotone trajectory: iter-0 **0.2787** (WR 31.8%) → iter-1 **0.2883** (WR
32.8%, **no rejection** under constant shape vs Phase M's iter-1 reject under linear-decay) →
iter-2 **0.3677** (WR 41.0%, +7.9pp over iter-1). Cross-phase iter-2 Wilson: Phase L
linear-decay 1.0× = 0.3560, Phase M linear-decay 1.75× = 0.3580, **Phase N constant 1.0× =
0.3677** — Δ vs L = +0.012, Δ vs M = +0.010. Schedule axis moved iter-2 by ~5× more than
Phase M's coef-magnitude axis (+0.002) but both are small (~1.5σ Wilson noise at n=500). Still
**3.2pp short of 0.40**. Mechanism healthy in all three sweeps: Phase N ratio_max 11.82 / 7.97
/ 32.78 (gradient strongly active), entropy stable 0.173 → 0.160 → 0.155, approx_kl_max
< 0.015, `numerical_anomalies = 0` across 48 minibatches. Reward-hacking check passes: WR
tracks Wilson in lockstep (31.8 → 32.8 → 41.0%), no Wilson-up-WR-down iter; the +7.9pp iter-2
Wilson lift mirrored by +8.2pp WR. Wall-clock **5m 25.4s** (fastest of L/M/N, zero runtime
cost). **Headline:** three configurations of the same 5-signal mix have now landed iter-2 at
0.356 / 0.358 / 0.368. PPO is healthy and moving the policy correctly in all three; the
reward-shape axis is worth ~+5pp Wilson over unshaped baseline; but **scaling magnitude or
schedule on the existing signal mix does not produce the +9pp needed for 0.40 — the binding
constraint is the signal set itself**. Next move: signal-set change. Recommended candidate is
the value-head-delta tempo signal (option 3 from prior scoping) — per-step delta in own-win-
probability from the trained value head's output, added as a 6th additive signal at coef ~0.05
(scale similar to existing hp-diff). Keep 5 existing signals. Same constant schedule
established as winning by Phase N. Same Phase H HPs / warm-start. Implementation
**~20-40 LOC** additive to `training/ppo_orchestrator.py:parse_trace_to_trajectories` (value
head output already in trajectory inference stream); smoke first, then launch as new Phase O.
Files touched: `docs/ai-performance-research-progress.md` (Phase N section appended; summary
table extended with Phase N row; F1 summary line at 736 updated with Phase N + binding-
constraint framing), `docs/ai-research-backlog.md` (R15.S3 Phase N Result block appended
under existing entry), `docs/ai-agent-state/notes.md` (Phase N closeout block + cross-phase
summary + value-head-delta pre-scoping appended to F1 reward shaping section),
`docs/ai-agent-state/queue.json` (`r15-s3-signal-mix-or-schedule` flipped to `done`; added P3
`r15-s3-value-head-tempo-signal` ready; `updated` bumped), this digest. Not committed.

**R15.S3 value-head signal BLOCKED + Phase O' reduced-mix LAUNCHED (2026-05-14, twenty-second
slot of day).** Phase O original plan (value-head-delta tempo signal) audited and found
blocked on TS-side instrumentation: rollout emits placeholder `value_pred=0.0`
(`ppo_orchestrator.py:858`), PPO recomputes value at training-time, the value head is not in
the decision-trace schema. Implementation requires ONNX export audit + TS-side inference
call-site instrumentation + schema extension on `behaviorPolicy.valueEstimate` (or top-level
`valuePred`) + `relabelDecisionTrace.ts` forward — separate sprint-slot work. Filed as new P3
`r15-s3-value-head-trace-instrumentation` ready; original `r15-s3-value-head-tempo-signal`
flipped to **blocked** with the instrumentation reason. Pivoted to the scoping doc's option
(2): drop the weakest of the 5 existing signals, scale the strongest 2-3. **Audit step:**
attribution data IS present in `events.jsonl` (`trajectory-parse.shape_attribution` field per
iter); Phase N iter-2 absolute coef-weighted contributions: throughput 171.4, active_energy
90.4, hp_diff **-6.9** (anti-correlated), bench_energy 4.2, retreat **0.0** (agent never
retreats). Drop retreat (zero) and bench_energy (smallest non-zero, redundant with
active_energy). Scale active_energy and throughput by 1.5×. Keep hp_diff at 0.05 as a
control rather than amplifying an anti-correlated signal (preserves signal-set parity test
integrity). **Picked coefs** (matching Phase M's ~0.23/game budget concentrated on fewer
signals): active-energy 0.03 / bench-energy 0.0 / retreat 0.0 / throughput 0.03 /
hp-diff 0.05. **Launched** PID 2782486 at 22:46Z; `runs/R15-S3-reduced-mix/`; banner shows
all 5 new coefs reflected; iter-000 trajectory eval running clean (wr=0.27 at 35/1600);
ETA ~6 min wall-clock. Pidfile race repeated (cwd-reset before bash `>` redirect), recovered
via `ps aux | grep R15-S3-reduced-mix` then `echo PID > launch.pid`. Exit gate: **SUCCESS**
iter-2 Wilson ≥ 0.40 → first F1 success, signal-mix unlocks more headroom; **PARTIAL**
0.368 < iter-2 < 0.40 → mix has incremental headroom worth pursuing; **SATURATION** iter-2
~= 0.37 → signal-SET is genuinely binding (not the MIX over the 5 signals), pivot to
value-head instrumentation or R7/R8 SL-side branches. Files touched: this digest,
`docs/ai-agent-state/queue.json` (3 entries updated). Not committed.

**R15.S3 reduced-mix CLOSEOUT — Phase O', BRANCH CLOSED (2026-05-14, twenty-third slot of
day).** Verdict: **CAPSTONE — R15.S3 observation-delta reward-shaping BRANCH CLOSED at
iter-2 Wilson 0.3677 (identical to Phase N's 0.3677 to 4 decimal places).** Run
`runs/R15-S3-reduced-mix/` finished clean (`run_completed`, `halted=false`), all 3 iters
promoted, monotone trajectory: iter-0 **0.2825** (WR 32.2%, ratio_max 10.53) → iter-1
**0.2883** (WR 32.8%, ratio_max 18.02; identical to Phase N iter-1 0.2883 to 4dp) → iter-2
**0.3677** (WR 41.0%, ratio_max 10.57; **identical to Phase N iter-2 0.3677 to 4dp**). Δ
iter-2 vs Phase N = **+0.0000pp**. Dropping bench-energy (Phase N contribution 4.2) + retreat
(0.0 — agent never retreats) and scaling active-energy 0.02→0.03 and throughput 0.02→0.03,
holding hp-diff 0.05 as control, did not move iter-2 at all. Mechanism healthy in all 3 iters
(KL bounded, entropy stable 0.171→0.161→0.153, no anomalies, WR tracks Wilson in lockstep);
attribution confirms the drop choices (bench-energy / retreat correctly silenced 0.0 across
all 3 iters; throughput dominant ~259, active-energy secondary ~130, hp-diff anti-correlated
~-6.5). Wall-clock **5m 28.2s** (fastest of L/M/N/O' alongside Phase N). **4-axis synthesis:**
three single-axis moves inside the 5-signal observation-delta family have now been tested
independently — coef magnitude (L→M) Δ +0.002, schedule (L→N) Δ +0.012, signal mix (N→O') Δ
**+0.000**. PPO is healthy and moving the policy in all four phases (ratio_max well off 1.0,
WR tracks Wilson, no anomalies). The 5-signal family encodes ~+5pp Wilson over the unshaped
Phase H baseline (0.311 → 0.368), but **all three axes inside that family are exhausted** —
iter-2 caps at **0.368 ± 0.001** regardless of coefs, schedule, or signal subset. The binding
constraint is the *information content* of the signal set itself, not the weights or schedule.
**Total R15.S3 branch cost ~23 min wall-clock across 4 phases** (L 5m54s + M 6m22s + N 5m25s
+ O' 5m28s) — cheap research, decisive answer. **F1 post-mortem framing update:** the
"reward-shape axis lifted Wilson 0.311 → 0.356 without crossing the 0.40 bar" has been
upgraded to "**reward-shape axis fully explored and converged at 0.368; gap to 0.40 is now
categorical (needs different information source), not quantitative (needs more tuning)**".
**Surviving F1-line candidates** require pulling information from a different source:
(a) value-head tempo signal (highest-leverage, blocked on TS-side ONNX/trace instrumentation
queued as `r15-s3-value-head-trace-instrumentation`); (b) R7 multi-teacher labels (SL warm-
start rebuild, doesn't touch reward); (c) R8 DPO (PPO replacement, doesn't depend on hand-
shaped signal). A 4th option — non-per-step reward shaping (turn-based / game-phase
aggregate) — is untested and ranks below the three. **This is a research-stance decision**;
the autonomous loop should NOT pick one. Escalation filed; queue item
`r15-s3-branch-synthesis-and-next-pick` P2 ready autonomous-launch ineligible. Files touched:
`docs/ai-performance-research-progress.md` (Phase O' section + summary table extended + F1
post-mortem qualification updated), `docs/ai-research-backlog.md` (R15.S3 Phase O' Result
block + status flipped to DONE / SATURATED-BRANCH + branch-closed paragraph),
`docs/ai-agent-state/notes.md` (Phase O' Closeout + 4-axis synthesis + 3-candidate next-move
ranking appended to F1 reward shaping section), `docs/ai-agent-state/escalations.md` (new
open entry framing R15.S3 closure + 3-candidate research-stance decision),
`docs/ai-agent-state/queue.json` (`r15-s3-reduced-mix-sweep` flipped to done; added P2
`r15-s3-branch-synthesis-and-next-pick` ready; `r15-s3-signal-mix-or-schedule` summary
updated to reflect Phase O' closeout), this digest. Not committed. **Next action for main
session:** commit this closeout; surface escalation; human picks among (a)/(b)/(c) before
the autonomous loop touches F1 again.

## Slot 24 — value-head trace instrumentation TS-side scoping (2026-05-14)

Cost-info pass for queue P3 `r15-s3-value-head-trace-instrumentation` to give the user a
concrete comparison vs the R7 / R8 alternatives in the open escalation. **Headline finding:
the prior worker's "deeper than a Python diff" claim was right but smaller than implied — it
is a 2-file TS-only change, not a TS+ONNX+schema half-day change.** The ONNX export already
names `value` as an output (`training/export_onnx.py:46` passes
`output_names=["logits", "value"]` and `dynamic_axes` includes `"value": {0: "batch"}` at
line 52), the model returns `(logits, value)` from `forward()`
(`training/uma_ai/model.py:81-95`, value head defined at 73-79 with Tanh range [-1, +1]),
and the `/predict` server already unpacks and returns it (`training/serve_onnx.py:95, 146`).
**The TS client at `backend/src/sim/evaluateModelVsHeuristic.ts:513-519` simply omits `value`
from the typed payload destructure — the field arrives and is dropped.** Implementation path:
add `valueEstimate?: number` to the duplicated `BehaviorPolicySnapshot` type in
`evaluateModelVsHeuristic.ts:140-146` and `relabelDecisionTrace.ts:5-11` (+1 LOC each), add
`value?: number[]` to the payload type at line 519 (+1 LOC), and assign
`payload.value?.[0]` onto the snapshot in `chooseModelAction` at lines 527-536 (+1-2 LOC).
The trace writer at line 308-329 and the relabeler at line 109 already pass `behaviorPolicy`
through unchanged so they need zero new lines. Python parser
`ppo_orchestrator.py:844-849` does object-pass-through `.get()` so unknown keys are ignored —
no backward-compat break. Smoke tests `evalGateSmoke.ts:182-213` and
`daggerRoundSmoke.ts:65-86` assert presence/absence by selection source but never check the
snapshot's exact field count — the added optional field passes. **Grand total: ~4-6 LOC
across 2 TS files, ~30-45 min including TMPDIR=/tmp npm run test:train + jq smoke check.**
Risks called out: (1) value-head output ordering in serve_onnx assumes `(logits, value)`
tuple — silent breakage if a re-export reorders, low likelihood; (2) trace-file bloat <1% on
the 232 MB R15-S3 trace, negligible; (3) gate-vs-collector value semantics is a Phase P
consumer decision, not a blocker for the instrumentation slot; (4) duplicate type-decl drift
across the two TS files would be a future `shared/src/` refactor candidate. Files touched:
`docs/ai-agent-state/notes.md` (new `## F1 reward shaping value-head tempo — TS
instrumentation scoping (2026-05-14)` section, ~120 lines), `docs/ai-agent-state/queue.json`
(`r15-s3-value-head-trace-instrumentation` summary + next_action pointed at scoping, `updated`
bumped), this digest. No code changes (scoping-only per brief). **Bottom line for the
user:** option (a) is closer to "30-min Python diff equivalent" cost than to "half-day
TS+ONNX+schema change" — ranking weight against (b) R7 / (c) R8 should be adjusted
accordingly.

## Slot 25 — Phase O TS instrumentation + orchestrator signal + launch (2026-05-14)

Self-directed end-to-end execution of the value-head tempo branch.

**TS-side diff (3 files, ~7 LOC production + ~13 LOC smoke):**
- `backend/src/sim/evaluateModelVsHeuristic.ts:140-147` — added `valueEstimate?: number` to
  `BehaviorPolicySnapshot`; added `value?: number[]` to `/predict` destructure at line ~514;
  set `snapshot.valueEstimate = payload.value[0]` (guarded by `typeof === "number"`) in
  `chooseModelAction` at ~line 535.
- `backend/src/sim/dagger/relabelDecisionTrace.ts:5-12` — mirrored `valueEstimate?: number`
  on the duplicate type so the relabeler stays type-clean as it forwards `behaviorPolicy`
  through.
- `backend/src/tests/evalGateSmoke.ts` — fake server now returns `value: [0.42]`; added an
  assertion that policy-source trace rows carry `behaviorPolicy.valueEstimate` ≈ 0.42 (locks
  the wire-through in CI).

**Trace smoke result.** `TMPDIR=/tmp npm run test:train` PASS — all 5 train smokes green,
including the new valueEstimate assertion in evalGateSmoke. `has("valueEstimate") = true`
verified by the explicit equality check on 0.42 inside the smoke.

**Orchestrator-side diff (~30 LOC additive, `training/ppo_orchestrator.py`):**
- New CLI arg `--reward-value-head-coef` (default 0.0, recommended 0.05).
- `shaping_coefs` dict extended with `value_head` key (call site at iter loop).
- `attribution` dict extended with `value_head` key (zeroes-by-default keeps prior runs
  comparable).
- In `parse_trace_to_trajectories` row loop: when `coefs['value_head']` is non-zero and the
  step is non-terminal, read `behaviorPolicy.valueEstimate` from the current row and the
  next row in the episode; if both are finite numbers, add `coef · (v_{t+1} − v_t)` to the
  reward and to the attribution. Missing/None values fall back to a 0 contribution so
  pre-Phase-O traces and single-action rows never crash.
- Docstring updated.

**PPO smoke result.** `TMPDIR=/tmp npm run test:ppo-smoke` PASS — 2 iters, 32 events, both
promoted, status PASS. Default coef 0.0 keeps F1 numerics identical to Phase L baseline.

**Phase O launch.** Pre-checks clean (warm-start exists, out-dir clear, no other PPO
running). Launched:

```
nohup training/.venv/bin/python -u training/ppo_orchestrator.py \
  --out-dir runs/R15-S3-value-head-tempo \
  --init-from runs/item17-2026-05-11/iter-002/model/checkpoint.pt \
  --iterations 3 --games-per-update 800 \
  --lr 3e-4 --clip-epsilon 0.3 --entropy-coef 0.01 --ppo-epochs 4 \
  --reward-alpha 0.333 --reward-beta 1.0 \
  --reward-active-energy-coef 0.02 --reward-bench-energy-coef 0.02 \
  --reward-retreat-coef 0.03 --reward-throughput-coef 0.02 \
  --reward-hp-diff-coef 0.05 --reward-value-head-coef 0.05 \
  --reward-shape-start 1.0 --reward-shape-end 1.0 \
  --eval-games 250 \
  > runs/R15-S3-value-head-tempo/launch.log 2>&1 &
```

PID **2904732** alive at +6s; iter-0 rollout collecting (~35/1600 trace evals, WR ~0.29).
Banner: orchestrator does not emit a coef dump in events.jsonl (events `run_started` only
carries iterations/games_per_update/init_from/temperature) — the new coef is verified
present via the live `ps aux` cmdline: `--reward-value-head-coef 0.05 --reward-shape-start
1.0 --reward-shape-end 1.0` reflected. `shape_attribution` will emit the `value_head`
component in the iter-0 `trajectory-parse.completed` event once rollout finishes (~1.5 min).

**Exit gate.** SUCCESS if iter-2 Wilson lower ≥ 0.40 → first F1 success on record.
PARTIAL if 0.368 < iter-2 < 0.40 → value-head signal moved past Phase N ceiling but
not enough. SATURATION if iter-2 ≈ 0.37 → entire 6-signal mix saturated, R7/R8 are the
only remaining moves. Reward-hacking flag if Wilson rises ≥+5pp without WR tracking
(value-head can be self-referentially gamed if the policy learns to inflate its own value
estimates).

**ETA.** ~6 min wall-clock per L/M/N/O' precedent.

**Files changed:**
- `backend/src/sim/evaluateModelVsHeuristic.ts`
- `backend/src/sim/dagger/relabelDecisionTrace.ts`
- `backend/src/tests/evalGateSmoke.ts`
- `training/ppo_orchestrator.py`
- `docs/ai-agent-state/queue.json` (`r15-s3-value-head-trace-instrumentation` → done;
  `r15-s3-value-head-tempo-signal` → in_progress; removed
  `r15-s3-branch-synthesis-and-next-pick`)
- `docs/ai-agent-state/escalations.md` (R15.S3 escalation moved to Resolved with
  self-directed path-(a) note)
- this digest

Not committed. **Next action for main session:** wait ~6 min, read
`runs/R15-S3-value-head-tempo/orchestrator-state.json` + each iter's `gate.manifest.json`
for the three Wilson numbers, then route per exit gate above.

## Slot 26 — Phase O closeout (REGRESSED) + Phase P launch (2026-05-14)

**Phase O verdict: REGRESSED.** Run `runs/R15-S3-value-head-tempo/` finished clean
(`run_completed`, `halted=false`, 5m 22.0s wall-clock). Trajectory: iter-0 Wilson **0.2845**
(WR 32.4%, promoted) → iter-1 **0.2768** (WR 31.6%, **REJECTED** at floor 0.2845, same
over-shape pattern as Phase M iter-1 at 1.75× coefs) → iter-2 **0.3502** (WR 39.2%, promoted,
rolled from iter-0 parent). `promoted_iterations: [0, 2]`. **Δ iter-2 vs Phase N: -0.018**
(0.3502 vs 0.3677). Adding the value-head signal at coef 0.05 made iter-2 *worse* than the
unshaped-by-value-head Phase N baseline.

**Mechanism diagnosis: signal correctly wired but magnitude is ~20× the design budget.** Value
head Tanh output range [−1, +1] gives per-step delta range [−2, +2]; at `--reward-value-head-coef
0.05` per-step shape contribution is ±0.1; at the F1 episode length of ~60 steps/game per-game
contribution is ±6, vs the R15.S3 scoping ±0.3/game budget. iter-0
`shape_attribution.value_head = 26.01` confirms empirically (third-largest absolute attribution
behind throughput 171.4 and active-energy 85.1; for a mean-zero Tanh-delta signal that 26 is
all per-step variance contributed to the surrogate gradient). iter-1/2 attributions drop to
9.28/8.25 as iter-0's promoted policy learns to flatten the value-head delta — classic
over-shape signature. iter-1 rejection mirrors Phase M: over-shaped iter-0 → policy exploits
shape → iter-1 overfits → greedy WR falls below floor → iter-2 rolls back to iter-0 parent
and reclaims ~6pp but cannot recover the ground Phase N covered without over-shape. PPO
healthy in all non-reward dimensions (ratio_max 12.7/12.3/22.4, approx_kl_mean
0.014/0.011/0.012, entropy stable 0.167→0.159→0.158, `numerical_anomalies = 0` × 48
minibatches).

**Phase P launched — single change `--reward-value-head-coef 0.05` → `0.01` (5× smaller).**
Per-step contribution becomes ±0.02; at ~60 steps/game per-game total ±1.2 — still 4× above
the ±0.3 budget but ~5× closer than Phase O. All else identical (same warm-start, opponent,
HPs, 5 obs-delta coefs at Phase L 1.0×, constant schedule, --eval-games 250). Output dir
`runs/R15-S3-value-head-tempo-low/`. Launch command verbatim:

```
nohup training/.venv/bin/python -u training/ppo_orchestrator.py \
  --out-dir runs/R15-S3-value-head-tempo-low \
  --init-from runs/item17-2026-05-11/iter-002/model/checkpoint.pt \
  --iterations 3 --games-per-update 800 \
  --lr 3e-4 --clip-epsilon 0.3 --entropy-coef 0.01 --ppo-epochs 4 \
  --reward-alpha 0.333 --reward-beta 1.0 \
  --reward-active-energy-coef 0.02 --reward-bench-energy-coef 0.02 \
  --reward-retreat-coef 0.03 --reward-throughput-coef 0.02 \
  --reward-hp-diff-coef 0.05 --reward-value-head-coef 0.01 \
  --reward-shape-start 1.0 --reward-shape-end 1.0 \
  --eval-games 250 \
  > runs/R15-S3-value-head-tempo-low/launch.log 2>&1 &
```

**PID 3017314** alive at +7s; iter-0 trace-eval running (35/1600 at WR 0.257 in line with
item17 warm-start). `--reward-value-head-coef 0.01` confirmed via `ps aux | grep
R15-S3-value-head-tempo-low` cmdline (orchestrator does not emit a coef dump in events.jsonl
banner — verification path matches slots 18/20/22/25). Launch.log first line `[ppo-orchestrator]
=== iteration 0 ===` clean; no error/NaN/OOM in early output. ETA ~6 min wall-clock per
L/M/N/O'/O precedent.

**Exit gate.** SUCCESS if iter Wilson lower ≥ 0.40 → first F1 success on record. PARTIAL
if iter-2 ∈ (0.368, 0.40) → value-head signal contributes additively at low magnitude.
NEUTRAL if iter-2 ≈ 0.36-0.37 → signal redundant with obs-delta family at low magnitude.
REGRESSION if iter-2 < 0.35 → value-head info quality (not magnitude) is the problem;
pivot to R7/R8.

**Pidfile race recurred (4th occurrence).** The bash `nohup ... &` capture writes the
*wrapper* PID (3017312) to `$!`, not the orchestrator's real PID (3017314). Same pattern
hit at slots 18, 20, 22, 25, and now 26. Recovered each time via `ps aux | grep <out-dir>`
followed by `echo PID > launch.pid`. **Worth fixing soon** — a small helper that launches
via `python ... &` directly (not nested under `bash -c eval '... &'`) and captures the real
`$!` in the orchestrator process, or a post-launch `pgrep -P` resolver, would eliminate this.
Track as a `harness-pidfile-race-fix` queue item next planning slot.

**Files changed.**
- `docs/ai-performance-research-progress.md` — appended `### Phase O — F1 PPO + value-head
  tempo signal, coef 0.05` section; extended L/M/N/O' summary table with the Phase O
  regression row; updated F1 post-mortem qualifier line ~736 to cite Phase O.
- `docs/ai-research-backlog.md` — appended Phase O Result block under R15.S3 (DONE /
  REGRESSED with full diagnosis + Phase P scoping).
- `docs/ai-agent-state/notes.md` — appended "Phase O Closeout" block to the F1 reward shaping
  section + Phase P scoping sub-block.
- `docs/ai-agent-state/queue.json` — flipped `r15-s3-value-head-tempo-signal` to `done`
  with REGRESSED summary; added new P3 `r15-s3-value-head-tempo-low-coef` `in_progress`
  with `pid: 3017314`, `started_at: 2026-05-14T13:18Z`; bumped `updated` to T26.
- this digest.
- `runs/R15-S3-value-head-tempo-low/` — orchestrator-created run dir + launch.log + launch.pid.

Not committed. **Next action for main session:** wait ~6 min, read
`runs/R15-S3-value-head-tempo-low/orchestrator-state.json` + each iter's `gate.manifest.json`
for the three Wilson numbers, then route per Phase P exit gate above.

## Slot 28 — Phase P CLOSEOUT + R15.S3 GENUINE BRANCH CLOSE (both axes exhausted, 2026-05-14)

**Phase P verdict: REGRESSED — second value-head data point worse than the first; R15.S3
branch genuinely closed across both axes.** Run `runs/R15-S3-value-head-tempo-low/`
finished clean (`run_completed`, `halted=false`, **5m 23.2s** wall-clock; run_started ts
1778735914.37 → run_completed 1778736237.61, delta 323.24s). Trajectory: iter-0 Wilson
**0.2730** (WR 31.2%, promoted — **identical to Phase L iter-0 0.2730 to 4dp**; at coef
0.01 the value-head signal contributes effectively nothing at iter-0) → iter-1 **0.2845**
(WR 32.4%, promoted, +1.1pp; **no rejection**, unlike Phase O iter-1 which rejected at
0.2768 < 0.2845 floor) → iter-2 **0.3463** (WR 38.8%, promoted, +6.2pp from iter-1).
`promoted_iterations: [0, 1, 2]`, `consecutive_failures: 0`, `halted: false`,
`promoted_wilson_lower 0.34629528411824795`. **Δ iter-2 vs Phase N: -0.021; Δ iter-2 vs
Phase O: -0.004.** Both value-head magnitudes regressed; the 5× lower coef did not help
and slightly hurt.

**Headline finding.** Both value-head coefficients tested regressed vs Phase N's
unshaped-by-value-head baseline: Phase O coef 0.05 → iter-2 0.3502 (-1.8pp); Phase P coef
0.01 → iter-2 0.3463 (-2.1pp, AND -0.4pp vs Phase O). Lowering the coef did not help —
the **value-head-delta signal mechanism is wrong-shape, not wrong-magnitude**. At coef
0.05 the signal was loud and over-shaped the policy (Phase O iter-1 rejection); at coef
0.01 the signal is quiet but noisy and contributes random variance without informational
gain (Phase P clean trajectory but lower ceiling). The data point is the *mechanism*, not
the coefficient.

**Dimensional check passes.** iter-0 `shape_attribution.value_head = 5.03` (Phase P) vs
`26.01` (Phase O); ratio **5.17×** matches the 5× coef ratio within rounding — value-head
signal is wired and scaled correctly. iter-1/2 attributions 1.45/1.27 (Phase P) vs
9.28/8.25 (Phase O) — same flatten-the-delta pattern but at smaller absolute level. PPO
healthy in non-reward dimensions: ratio_max 12.53/13.29/21.63, approx_kl_mean
0.013/0.012/0.009, entropy stable 0.173→0.164→0.164, `numerical_anomalies = 0` × 48
minibatches.

**R15.S3 BRANCH CLOSED across both axes.** Final 6-phase table (axes 1 and 2 combined):

| Phase | Axis | Config | iter-2 Wilson | Δ vs Phase N |
| --- | --- | --- | --- | --- |
| L | obs-delta | 5 signals, 1.0×, decay | 0.3560 | -0.012 |
| M | obs-delta | 5 signals, 1.75×, decay | 0.3580 | -0.010 |
| N | obs-delta | 5 signals, 1.0×, constant | **0.3677** | — (baseline) |
| O' | obs-delta | 3 signals (scaled), constant | 0.3677 | +0.000 |
| O | value-head | +VH coef 0.05, constant | 0.3502 | -0.018 |
| P | value-head | +VH coef 0.01, constant | **0.3463** | **-0.021** |

Axis 1 (obs-delta) saturated at 0.368 ± 0.001 across coef magnitude / schedule / signal
mix. Axis 2 (value-head tempo) regressed across 5× coef range. **Total branch cost ~36
min wall-clock across 6 phases.** The previous `87e9e77` "BRANCH CLOSED" commit framing
was premature — it covered only axis 1; Phases O + P add axis 2 and produce the genuine
cross-axis closeout.

**F1 post-mortem framing — final update.** Prior: "gap to 0.40 is categorical — needs a
different information source, not more tuning." Phases O + P tested *exactly* that — a
different information source (value-head delta) — and that information source actively
regressed at both magnitudes. **New: "The F1 reward-shape mechanism cannot break 0.368
from this warm-start. Per-step shaping from any observation-derived signal saturates at
0.368, and per-step shaping from the policy's own value-head delta actively regresses.
The remaining F1 moves must change either the warm-start (R7 multi-teacher labels
rebuild) or the optimization objective (R8 DPO replacement). The reward-shape branch
is closed."**

**What survives.** R7 (multi-teacher labels, SL warm-start rebuild) and R8 (DPO, PPO
replacement). Both human-rank, not autonomous-launch. Path (a) value-head tempo signal
has been executed and exhausted; it is no longer a surviving candidate. Escalation
re-opened at `docs/ai-agent-state/escalations.md` `## Open` with updated framing.

**Files changed.**
- `docs/ai-performance-research-progress.md` — appended `### Phase P — F1 PPO + value-head
  tempo signal, coef 0.01 (R15.S3 GENUINE BRANCH CLOSEOUT)` section; extended L/M/N/O'/O
  summary table with the Phase P regression row (now 6 rows); updated F1 post-mortem
  qualifier ~line 736 to cite Phase P and add the final cross-axis framing.
- `docs/ai-research-backlog.md` — flipped R15.S3 header from "DONE / SATURATED-BRANCH"
  to "DONE / EXHAUSTED (both axes closed)"; rewrote the final-branch-closeout block
  to cover both axes; appended Phase P Result block under R15.S3.
- `docs/ai-agent-state/notes.md` — appended Phase P Closeout block + the authoritative
  "Final R15.S3 branch summary — both axes exhausted" block (6-row table, axis-level
  synthesis, final F1 post-mortem framing, surviving candidates) to the F1 reward
  shaping section.
- `docs/ai-agent-state/queue.json` — flipped `r15-s3-value-head-tempo-low-coef` to
  `done` with REGRESSED summary; re-added `r15-s3-branch-synthesis-and-next-pick` P2
  ready autonomous-launch-ineligible item (was removed at `87e9e77`); bumped `updated`
  to T28.
- `docs/ai-agent-state/escalations.md` — re-opened the R15.S3 escalation under `## Open`
  with updated framing (path a executed and exhausted; remaining candidates R7 + R8 need
  human ranking). Old `## Resolved` entry from prior premature closeout retained as
  historical record with cross-reference.
- this digest.

Not committed. **Next action for main session:** commit Phase P closeout (analogous to
`24911c6`/`c449632` pattern); the autonomous loop should NOT pick R7 vs R8 — surface the
re-opened escalation to the human for the research-direction decision. The queue still
contains autonomous-bounded P1 work (`r14-e-manual-ui-exercise`) and the new
`r15-s3-branch-synthesis-and-next-pick` P2 item which is autonomous-launch-ineligible by
design.
