# 2026-05-18 — coverage audit DONE + R110 W6-repro launched

## Slot 1 — Training-data coverage audit: DONE (digested)

- P1 `training-data-coverage-audit` closed (R7 mixed retains
  3789/12387; ~70% drop = 100% single-legal-action; zero
  schema/label loss). Canonical:
  `docs/ai-research/analysis/training-data-coverage-audit.md`
  (+ `training/data_coverage_audit.py`, npm
  `test:data-coverage-audit`).

## Slot 11 — serve_onnx 96/110/164 schema guard SHIPPED (R16-P1 unblocked)

- `serve_onnx._resolve_feature_schema` now resolves the builder
  STRICTLY by ONNX `state_features` dim via explicit
  `_SCHEMA_BY_STATE_DIM` (96→v2, 110→v3.0); old binary "else→v3"
  removed. Fails fast (`SystemExit(2)`) on 164 (declared-but-
  unimplemented v3.1 placeholder), unknown dims, embedding/dim
  inconsistency, and explicit-pin mismatch — never silent v3.
  `features.py`: `STATE_DIM_V3=110`/`STATE_DIM_V3_1=164`(placeholder),
  import-time `STATE_DIM==STATE_DIM_V3` assert + both frozen builders
  assert output width. 96-d serving bit-identical. Test
  `training/serve_schema_guard_smoke.py` ALL PASS; py_compile +
  `test:python-train` PASS. queue `r16-p1`→scoped/unblocked,
  `r12-throughput`→done (#4 deferred pointer folded). Canonical:
  scope doc § P1 "Load-bearing implementation prerequisite".

## Slots 2–4 — R110 launch / R16-P0 fix / true-v3 relaunch (digested)

- R110 W6-repro launched, killed at iter-0 (confounded by the R16-P0
  inert card-embed bug), P0 landed (commit `b38de0e`,
  `r16_mcts_embedding_smoke.py`), relaunched true-v3. Confounded run
  preserved `runs/R110-W6-repro-CONFOUNDED-p0bug-iter0only` (discard).
  iter-0 true-v3 = Wilson 0.5273 (identical win-count to confounded but
  materially different ckpt md5 → P0 IS exercised; rollout-leaf gate
  model-independence at barely-distilled iter-0). Canonical narrative:
  `docs/ai-research/progress/r110.md` §1 + Slot 6/7 below.

## Slot 6 — R110 W6-repro verdict: MARGINAL, not promoted

- **Verdict:** MARGINAL. Best-promoted iter-2 110-d v3 Wilson lower
  **0.6042** (0.60-0.6479 band) → reproduced-but-not-superior, NOT
  promoted; pinned 96-d 0.6479 stays production. R16-P0 fix confirmed
  (real monotone 0.527→0.561→0.604 vs inert confounded run).
- **Trajectory:** 110-d v3 0.5273→0.5612→**0.6042**(peak)→0.5106→0.5612;
  run COMPLETE all 5 iters, did NOT auto-halt (iter-4 promoted by 0.001
  tolerance vs lower 0.6042 floor; verdict unchanged on best iter-2).
- **Mechanism:** iter-2-peak-then-rot is a schema-independent W6-recipe
  property (both 96-d + 110-d). Cause: per-iter-only self-play +
  moving (not fixed) KL anchor → softening targets, no anti-drift.
- **Forward:** loop-recipe axis (NOT closed representation axis),
  read-only KL/entropy discriminator first. Canonical:
  `docs/ai-research/progress/r110.md`.

## Slot 7 — W6 loop-rot discriminator: COMPLETE, gate SATISFIED

- **Result:** read-only discriminator ran clean over fixed v3 corpus,
  all 10 ckpts (R13-W6-phase-d + R110-W6-repro iter-0..4). Recipe-rot
  confirmed as **MONOTONE representation drift from iter-0** — entropy
  ↑, rule-bot top-1 ↓, KL‖iter-0 ↑ monotone; NOT an iter-2 peak. Both
  schemas agree; v3 drifts ~2.4× harder in KL (0.423 vs 0.179).
- **Impact:** §3 mechanisms 1&2 confirmed, ranked first. §4 gate
  SATISFIED → forward = implementer r12_orchestrator.py recipe-fix.
- **Canonical:** `docs/ai-research/progress/r110.md` §4a (queue item
  `w6-loop-anti-degradation` → ready).

## Slot 8 — r12 throughput #1-#3 landed (trajectory-neutral)

- #1 workers 1→24 (7d5bfd2), #2 hash-carry (`MCTS_HASH_CARRY_ENABLED`),
  #3 /predict keep-alive (`MCTS_KEEPALIVE_ENABLED`) — all default ON,
  gate-proven bit-identical (12 keys/0 mismatch), #4 leaf DEFERRED.
  Canonical: scoping doc + r110.md §4b/§4c. Detail superseded by Slot 10.

## Slot 5 — R16-P1 readiness audit (safe concurrent, doc-only)

- **Verdict:** P1 (temporal/turn-state features) readiness-audited
  read-only while R110 runs; commit `0f9953c`. All spec fields exist
  in `shared/src/types.ts` and are hidden-info-safe (clean leak story).
- **Corrections folded into scoping § P1:** exact `STATE_DIM = 110→164`
  with a committed slot table (spec's "~148" was arithmetically low);
  field renames (`retreatCostReduction`, `usedAbilityNames*`→counts,
  side-level `activeAttackDamageBonus`, derived boolean stamps); two
  omissions to resolve (`globalRetreatCostReduction` stadium effect;
  `===0` vs `<=1` first-turn ambiguity).
- **Load-bearing prerequisite:** the shared `STATE_DIM` constant means
  bumping it breaks 110-d v3.0 serving — must freeze the v3.0 builder +
  make `serve_onnx` schema resolution 3-way (96/110/164) BEFORE the
  bump. Only non-trivial P1 code change; R110-safe.
- **Status:** P1 implementation-ready but BLOCKED (queue
  `r16-p1-temporal-turn-state-features`): gated on R110 verdict +
  no mid-run feature-file edits (the P0 lesson).

## Slot 9 — R111 W6 recipe-fix verdict + 2.11× speedup confirmed

- **Mechanism CONFIRMED:** the W6 recipe-fix eliminates the iter-3 rot
  — baseline craters 0.6042→0.5106 (fail-promote); R111 iter-3 holds
  0.5527, promoted, no crater.
- **But over-damps at default HP → net ceiling LOSS:** R111 Wilson
  lower bit-identical 0.5527 across iter-1/2/3 (policy froze) then
  decays 0.5358 at iter-4; entire trajectory below baseline iter-2
  peak 0.6042 and below the §6 band. Forward = user-gated
  regularization-dose sweep (do NOT auto-launch).
- **Speedup:** 2.11× wall-clock (3088s vs 4-worker baseline 6532s),
  trajectory-neutral (iter-0 bit-identical) — #1–#3 validated at
  production scale. Gap vs idealized localizes to integer-partition
  tail + single serve_onnx contention at 24 workers.
- **Canonical:** `docs/ai-research/progress/r110.md` §4c (queue
  `w6-loop-anti-degradation` → scoped; `r12-throughput` updated).

## Slot 10 — r12 throughput #5 work-stealing dispatch shipped

- Static `ceil(N/workers)` chunking left selfplay workers 20-23 dark
  (60 games→20 slices) + 2-3× variance skew; replaced w/ shared
  task-index queue, 2-prefetch, slot-ordered aggregation (selfplay
  +gate). Refutes "games-multiple-of-workers" (gate 5/worker, still
  ~20% skew). `MCTS_WORK_STEALING_ENABLED` (`workStealing.ts`) ON;
  `UMA_MCTS_WORK_STEALING=0` reverts. Prefetched tasks MUST drain
  sequentially per worker (FIFO, 1 game in flight) — concurrent ran
  broke RNG/serve_onnx isolation; fixed. Bit-identical (selfplay sha256
  + 16 gate fingerprints, workers>1) via r12_workstealing_determinism_gate.py.
- Balanced ceiling ~2.6-2.85× (max ~2.9×; rest of gap = never-real
  4w-baseline + rollout floor). serve_onnx-sharding OUT OF SCOPE.
  R12 launch command unchanged. Canonical: scoping doc + r110.md §4c.

## Slot 11 — R16-P1 temporal/turn-state v3.1 features SHIPPED

- Commit `357f0d6` (13 files +1139/-92): TS observation schemaVersion
  2→3 (temporal block + side/Uma turnState, counts-only ability locks,
  derived `effectiveRetreatCostReduction`) → NEW Python
  `observation_to_features_v3_1` (164-d; frozen v3.0 verbatim 0–109
  byte-identical + 54 temporal slots) → serve_onnx 164→v3.1 entry →
  164-d ONNX export. `STATE_DIM` stays frozen alias of 110 (assertion
  intact); v3.1 opt-in via dim-keyed selectors. Two scope omissions
  resolved vs engine code (globalRetreatCostReduction → derived
  effective field; `ownIsFirstTurn` = energy-phase flag
  turnsTakenBySide===0). All smokes green incl. real 96/110-d ckpts,
  frozen-head byte-identity, ONNX roundtrip <1e-3.
- Forward: user-gated ~0.5-day v3.1 strength ablation/gate vs 110-d
  v3.0 baseline (do NOT auto-launch). P2 slot-tokens now unblocked.
  Canonical: scoping/r16-model-feature-backlog-refinement.md (P1
  IMPLEMENTED); queue `r16-p1-...` → done; escalations Open → none.

## Slot 12 — R16-P1 v3.1 strength ablation: trainer wired + RUNNING

- User go-ahead → run v3.1 ablation, then R16-P2 if green, keep going.
- BLOCKER found+fixed: `357f0d6` wired dim-selectors to serve/export
  only; mcts-distill trainer still hardcoded 110-d. Commit `3ce1404`:
  `--state-dim` threaded through `selfplay_dataset.py`/`train_bc.py`/
  `r12_orchestrator.py` + `make_v31_additive_init.py` (164-d
  additive-tail warm-start from R7b2 110-d; logit/value delta vs v3.0
  init = exactly 0.0 → cleanest possible A/B init).
- 1st launch (pid 70423) KILLED ~1min in: events.jsonl proved W6
  recipe-fix defaulted ON (r12_orch:61-62) — would confound v3.1 vs
  the unregularized 0.6042 baseline. (My `--help` inference was wrong;
  `--no-w6-fix-*` flags exist at r12_orch:773/779.) Lesson: verify
  effective config in events.jsonl, never infer flag defaults.
- RELAUNCHED ~15:13 (detached pid 2600, 4 iters) WITH
  `--no-w6-fix-cross-iter-replay --no-w6-fix-fixed-kl-anchor`. Config
  VERIFIED in events.jsonl: cross_iter_replay/fixed_kl_anchor=false,
  iters=4, iter-0 selfplay running, no errors. Apples-to-apples.
  GREEN iff best-promoted aggregate wilson95.lower > 0.6042 (v3.0
  cited). Progress: runs/R16-P1-v31-ablation/loop/{events,orchestrator-state}.json.

## Slot 13 — R16-P1 ablation: gate-kills-serve crash root-caused + fixed

- Launch #2 (verified w6-fix OFF) iter-0 gate FAILED: 30/240 games OK
  then serve_onnx died -> ECONNREFUSED cascade -> `gate exited 1`,
  bogus wilson_lower=0.0 (crash artifact, not strength).
- Root cause (investigator): `serve_onnx.py` PolicyServer used Python
  default TCP request_queue_size=5; 24-worker gate fan-in overflowed
  the listen backlog (dmesg SYN-flood on serve port). NOT v3.1, NOT
  OOM. Also: serve stderr was DEVNULL'd (blind to the death).
- 2nd confound found: launched `--eval-games 120` but real
  R110-W6-repro gate = `--games 60` (n=120 side-balanced, the 0.6042
  baseline) — verified from runs/R110-W6-repro/iter-*/gate.log.
- Fix commit `e846881`: request_queue_size=128 + per-iter serve.log
  (proven vs the exact listen(5) mechanism; schema-guard smoke green).
- RELAUNCHED pid-tracked via pgrep; recipe VERIFIED in events.jsonl
  (eval:60, w6-fix off, iters:4). Apples-to-apples. GREEN iff
  best-promoted wilson95.lower > 0.6042. Monitoring per loop_note.
