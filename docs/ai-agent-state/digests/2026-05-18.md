# 2026-05-18 — coverage audit DONE + R110 W6-repro launched

## Slot 1 — Training-data coverage audit: DONE (hypothesis confirmed)

- **Verdict:** P1 `training-data-coverage-audit` closed. R7 mixed
  corpus retains **3789/12387 (30.6%)**; the ~70% drop is **100%
  single-legal-action** (`min_actions=2`, `lt_min_actions(<2_legal)`
  = 8598/8598). Zero schema/label/feature/card-id loss.
  Cross-checked independently: R7 manifest `samples:3789` ==
  audit retained; `mixed.jsonl` line count 12387 == audit total.
- **Mechanism:** bottleneck is decision-state coverage, not row
  count/label/schema. Coverage-vs-loss confirms it — val loss
  clusters in thin/shifted slices (`useAbility` 3.51, `pass` 3.30,
  `attachEnergy` 3.00, relabeled-source 3.37). Top dropped:
  `action_kind=pass` 91.0%, `hand_size<=2` 77.2%.
- **Env gaps:** gate per-state traces never persisted
  (`decisionTraceOut:""`) → train-vs-gate overlap is structural
  proxy only; per-state eval loss not logged → phase/action/source
  proxy. Recorded as audit limitations, not fabricated.
- **Artifacts:** reproducible `training/data_coverage_audit.py`
  (+ paired smoke, npm `test:data-coverage-audit`); canonical
  report `docs/ai-research/analysis/training-data-coverage-audit.md`
  (+ JSON sidecar). Real run exit 0; smoke PASS.
- **Next:** unblocks backlog item 2 (rule-bot-covered
  state-generation recipe); proposed source-mix floors in report.

## Slot 2 — R110 W6-reproduction launched (background, ~1–2h)

- **Action:** user-steered launch of `r110-w6-reproduction`.
  `runs/R110-W6-repro`, PID 11197, faithful W6 iter-2 recipe
  (`r12_orchestrator.py --mcts-leaf rollout`, 60 selfplay/iter,
  100 sims, K=3, gate n=120 rollout-leaf both sides, eps 20,
  64/2, kl-anchor 0.05, 4 workers, cuda, 5 iters w/ halt-after-2).
- **Init decision (self-directed, supersedes scoping):** scoping
  §5 recommended random init, but the orchestrator **mandates**
  `--init-checkpoint` (no random path; `raise SystemExit`). W6's
  own precedent was a *same-schema SL warm-start*, so the faithful
  analogue is `runs/R7b2-card-embed/iter-000/model/checkpoint.pt`
  (110-d/v3.0, hidden 64/depth 2 = W6's promoted arch). More
  faithful than random and orchestrator-supported.
- **Exit gate:** agg Wilson lower ≥ 0.6479 → promote via
  `serve_onnx --feature-schema v3`. Detail:
  `docs/ai-research/scoping/r110-w6-reproduction.md` §6.
- **Status:** SUPERSEDED — killed at iter-0 (confounded, see Slot 3).
  Confounded run preserved `runs/R110-W6-repro-CONFOUNDED-p0bug-iter0only`
  (iter-0 Wilson 0.5273, embeddings inert — discard).

## Slot 3 — R16-P0 fix: mcts-distill card-embed branch was inert

- **Trigger:** user surfaced `docs/ai-research/scoping/r16-model-feature-backlog-refinement.md`;
  P0 finding verified in code — directly confounded the running R110.
- **Bug:** `train_bc.py --data-mode mcts-distill` trained the 110-d/v3
  model with the card-embedding branch **inert**. `selfplay_dataset.py`
  (`MctsSelfPlaySample`/`collate_mcts_selfplay_batch`/`load_mcts_selfplay_samples`)
  never emitted `card_ids_by_zone`/`action_card_idx` → `train_bc.py:464`
  `batch.get(...)`→None. BC path (`JsonlPolicyDataset`) was fine.
- **Decision (user-chosen):** kill confounded R110 (only iter-0 spent),
  land P0, relaunch as a true v3 test.
- **Fix (commit `b38de0e`):** self-play dataset emits v3 embedding
  tensors (strict fail-loud + named `allow_missing_card_ids` compat);
  `evaluate_grouped()` collator no longer hardcoded; value-target
  consumers forward optional tensors. New `r16_mcts_embedding_smoke.py`
  proves nonzero grad `card_embed.weight` 2.04 / `zone_projection.weight`
  355.0. r12-distill/python-train/train smokes exit 0; ONNX still 5-input
  v3. (`r13_value_retrain_smoke` exit 1 = pre-existing stale-96d
  checkpoint env gap, fails at load_state_dict before edited code.)
- **Doc:** `r110-w6-reproduction.md` §5 readiness note corrected (the
  2026-05-15 "no code blocker" verdict was wrong).

## Slot 4 — R110 W6-reproduction relaunched (true v3)

- **Action:** relaunched post-P0, identical faithful W6 recipe, same
  R7b2 110-d init, fresh `runs/R110-W6-repro` PID 52908. Now genuinely
  exercises the card-embedding path each distill step. Exit gate
  unchanged (agg Wilson lower ≥ 0.6479 → promote). Running iter-0;
  Monitor armed, loop heartbeat fallback.
- **Validity verified (iter-0):** true-v3 iter-0 gate = Wilson 0.5273
  / 74-of-120 — *identical win-count* to the confounded run's iter-0.
  NOT a re-confound: iter-0 checkpoints differ materially (confounded
  1,324,283 B vs true-v3 1,484,673 B, different md5) → P0 IS exercised;
  the identical gate number is the rollout-leaf gate's model-
  independence at a barely-distilled iter-0 (8.5 s distill / 20 ep /
  2031 rows from same R7b2 init). Expected; later iters diverge. Do
  not re-panic on the matching iter-0 number.

## Slot 6 — R110 W6-repro verdict: MARGINAL, not promoted

- **Verdict:** MARGINAL. Best-promoted iter-2 110-d v3 Wilson lower
  **0.6042** (0.60-0.6479 band) → reproduced-but-not-superior, NOT
  promoted; pinned 96-d 0.6479 stays production. R16-P0 fix confirmed
  (real monotone 0.527→0.561→0.604 vs inert confounded run).
- **Trajectory:** 110-d v3 0.5273→0.5612→**0.6042**(peak)→0.5106→0.5612;
  run COMPLETE all 5 iters, did NOT auto-halt (iter-4 promoted by 0.001
  tolerance vs lower 0.6042 floor; verdict unchanged on best iter-2).
- **Mechanism:** iter-2-peak-then-rot is a schema-independent W6-recipe
  property (both 96-d + 110-d). Cause: per-iter-only self-play +
  moving (not fixed) KL anchor → softening targets, no anti-drift.
- **Forward:** loop-recipe axis (NOT closed representation axis),
  read-only KL/entropy discriminator first. Canonical:
  `docs/ai-research/progress/r110.md`.

## Slot 7 — W6 loop-rot discriminator: COMPLETE, gate SATISFIED

- **Result:** read-only discriminator ran clean over fixed v3 corpus,
  all 10 ckpts (R13-W6-phase-d + R110-W6-repro iter-0..4). Recipe-rot
  confirmed as **MONOTONE representation drift from iter-0** — entropy
  ↑, rule-bot top-1 ↓, KL‖iter-0 ↑ monotone; NOT an iter-2 peak. Both
  schemas agree; v3 drifts ~2.4× harder in KL (0.423 vs 0.179).
- **Impact:** §3 mechanisms 1&2 confirmed, ranked first. §4 gate
  SATISFIED → forward = implementer r12_orchestrator.py recipe-fix.
- **Canonical:** `docs/ai-research/progress/r110.md` §4a (queue item
  `w6-loop-anti-degradation` → ready).

## Slot 8 — r12 throughput: worker default 1→24 (trajectory-neutral)

- **Diagnosis:** R110-W6-repro per-iter wall-clock = selfplay ~603s
  (~48%) + mcts-gate ~694s (~52%) ≈ 99%; distill ~0.6%; GPU idle.
  Root cause: 4 workers on a 32-core box → ~28 cores idle.
- **Fix (Deliverable 1):** `r12_orchestrator.py` `--workers` 1→24.
  Verified trajectory-neutral — both stages partition a FIXED
  seed-indexed game set; per-game RNG seeded from game seed only
  (mctsSelfPlay.ts:120,158,255 / evalGate.ts:46,126 /
  evaluateModelVsHeuristic.ts:269). Bit-comparable to 4-worker baseline.
- **Canonical:** r110.md §4b; scoping `r12-selfplay-gate-throughput.md`
  (ranked follow-ups; rollout-clone refactor HARD-GATED on a
  determinism replay gate). Queue `r12-throughput`.

## Slot 5 — R16-P1 readiness audit (safe concurrent, doc-only)

- **Verdict:** P1 (temporal/turn-state features) readiness-audited
  read-only while R110 runs; commit `0f9953c`. All spec fields exist
  in `shared/src/types.ts` and are hidden-info-safe (clean leak story).
- **Corrections folded into scoping § P1:** exact `STATE_DIM = 110→164`
  with a committed slot table (spec's "~148" was arithmetically low);
  field renames (`retreatCostReduction`, `usedAbilityNames*`→counts,
  side-level `activeAttackDamageBonus`, derived boolean stamps); two
  omissions to resolve (`globalRetreatCostReduction` stadium effect;
  `===0` vs `<=1` first-turn ambiguity).
- **Load-bearing prerequisite:** the shared `STATE_DIM` constant means
  bumping it breaks 110-d v3.0 serving — must freeze the v3.0 builder +
  make `serve_onnx` schema resolution 3-way (96/110/164) BEFORE the
  bump. Only non-trivial P1 code change; R110-safe.
- **Status:** P1 implementation-ready but BLOCKED (queue
  `r16-p1-temporal-turn-state-features`): gated on R110 verdict +
  no mid-run feature-file edits (the P0 lesson).
