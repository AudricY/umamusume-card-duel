# 2026-05-18 — coverage audit DONE + R110 W6-repro launched

## Slot 1 — Training-data coverage audit: DONE (hypothesis confirmed)

- **Verdict:** P1 `training-data-coverage-audit` closed. R7 mixed
  corpus retains **3789/12387 (30.6%)**; the ~70% drop is **100%
  single-legal-action** (`min_actions=2`, `lt_min_actions(<2_legal)`
  = 8598/8598). Zero schema/label/feature/card-id loss.
  Cross-checked independently: R7 manifest `samples:3789` ==
  audit retained; `mixed.jsonl` line count 12387 == audit total.
- **Mechanism:** bottleneck is decision-state coverage, not row
  count/label/schema. Coverage-vs-loss confirms it — val loss
  clusters in thin/shifted slices (`useAbility` 3.51, `pass` 3.30,
  `attachEnergy` 3.00, relabeled-source 3.37). Top dropped:
  `action_kind=pass` 91.0%, `hand_size<=2` 77.2%.
- **Env gaps:** gate per-state traces never persisted
  (`decisionTraceOut:""`) → train-vs-gate overlap is structural
  proxy only; per-state eval loss not logged → phase/action/source
  proxy. Recorded as audit limitations, not fabricated.
- **Artifacts:** reproducible `training/data_coverage_audit.py`
  (+ paired smoke, npm `test:data-coverage-audit`); canonical
  report `docs/ai-research/analysis/training-data-coverage-audit.md`
  (+ JSON sidecar). Real run exit 0; smoke PASS.
- **Next:** unblocks backlog item 2 (rule-bot-covered
  state-generation recipe); proposed source-mix floors in report.

## Slot 2 — R110 W6-reproduction launched (background, ~1–2h)

- **Action:** user-steered launch of `r110-w6-reproduction`.
  `runs/R110-W6-repro`, PID 11197, faithful W6 iter-2 recipe
  (`r12_orchestrator.py --mcts-leaf rollout`, 60 selfplay/iter,
  100 sims, K=3, gate n=120 rollout-leaf both sides, eps 20,
  64/2, kl-anchor 0.05, 4 workers, cuda, 5 iters w/ halt-after-2).
- **Init decision (self-directed, supersedes scoping):** scoping
  §5 recommended random init, but the orchestrator **mandates**
  `--init-checkpoint` (no random path; `raise SystemExit`). W6's
  own precedent was a *same-schema SL warm-start*, so the faithful
  analogue is `runs/R7b2-card-embed/iter-000/model/checkpoint.pt`
  (110-d/v3.0, hidden 64/depth 2 = W6's promoted arch). More
  faithful than random and orchestrator-supported.
- **Exit gate:** agg Wilson lower ≥ 0.6479 → promote via
  `serve_onnx --feature-schema v3`. Detail:
  `docs/ai-research/scoping/r110-w6-reproduction.md` §6.
- **Status:** running iter-0 self-play at digest time. Awaiting
  completion (loop fallback wakeup armed).
