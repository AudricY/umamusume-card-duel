# 2026-05-15 — MCTS-distill v1 closeout

## Slot 1 — MCTS-distill v1 SL gate: FAIL

- **Verdict:** FAIL. Iter-0 SL gate Wilson lower **0.1470** (n=1000
  side-balanced, rule-bot, seed-start 9000), point 0.169, on
  `runs/mcts-distill-v1/iter-000/`.
- **Mechanism:** soft-visit rollout-leaf MCTS BC labels did not lift
  raw-policy — landed below all F1 argmax baselines (-15 to -18pp vs
  R7/R8/R7.b.2) and ≈ the n=20 W6/iter-2 raw anchor (0.1455). <0.30 →
  scoping § 5(c) state-coverage mismatch is the binding constraint
  (mcts-vs-mcts corpus vs raw-policy rule-bot gate); the pre-registered
  biggest risk. 4 F1 axes now exhausted (labels/objective/repr/shape).
- **Repro note:** retrain-from-item17 blocked by post-R7.b.2 arch
  incompat (item17 ckpt predates card-vocab embedding; train_bc.py
  strict load_state_dict). Canonical gate verdict was produced at
  scoping commit 73c2d8c and is intact — gap blocks re-running train,
  not the proposition.
- **Next:** queued `mcts-distill-followup-direction` P2 — change
  warm-start (W6/iter-2) or supervision distribution; escalate pick.
- **Link:** `docs/ai-research/progress/r15.md` § MCTS-distill v1;
  `runs/mcts-distill-v1/iter-000/README.md`.

## Slot 2 — planning-refresh-after-r14-e: backlog/queue reconciliation

- **Reconciled:** F1 raw-policy SL line closed across all 4 axes
  (R7 0.2921 / R8 0.3318 / R7.b.2 0.3045 / mcts-distill 0.1470).
  Backlog Current-state #4, north star, and the four Tier-2 R7/R8/
  R7.b/mcts-distill subsections collapsed to one forward pointer;
  added § "Production-path forward work" (search-wrapped path only).
- **Queue hygiene:** stripped 5 done/superseded entries (mcts-distill-
  scoping, mcts-distill-v1-run, r7b-card-embedding-pass, r7b-attention-
  set-encoder, r7b-history-and-aux-heads) — all already canonical in
  r15.md / Slot 1. `planning-refresh-after-r14-e` completed → digested
  here (this slot).
- **New top forward job:** `mcts-determinism-and-side-asymmetry-audit`
  P2 (strongest autonomous production-path job; R14.E is P1 but
  human-gated). Single-worker CRN determinism audit + side-asymmetry
  shape at rollout-leaf — both gate honest 0.6479 production-claim
  phrasing; read/measure only, no training.
- **Link:** `docs/ai-research-backlog.md` § "Production-path forward
  work"; `docs/ai-agent-state/queue.json`.

## Slot 3 — MCTS production-claim audit: determinism PASS + side gap

- **(a) Determinism:** PASS. Single-worker CRN self-consistency 0.0
  full-outcome divergence over 16 (seed,side) keys × 2–3 replays at the
  production rollout-leaf config (sims=100/crn=3/rolloutSteps=200), run vs
  an isolated worktree at production SHA bc6db85 (96-dim; HEAD is 110-dim
  v3.0 → version skew, documented not a bug). Per-game advantage/Wilson
  estimates behind 0.6479 are reproducible.
- **(b) Side split @ rollout-leaf** (from existing iter-2 manifest, no
  re-gate): player 0.6833 [0.5577,0.7869] vs opponent 0.7833
  [0.6638,0.8688], +10.0pp directional, CIs overlap, player floor 0.5577.
- **Claim impact:** 0.6479 is a sound aggregate/side-balanced/reproducible
  Wilson-lower — NOT a per-side guarantee; disclose the ~10pp gap. No
  gate-rerun stop hit. Unblocked `deployment-policy-rollout-vs-valuehead`.
- **Link:** `docs/ai-research/progress/r15.md` § "MCTS production-claim
  audit"; `runs/R15-single-worker-determinism/result.json`.

## Slot 4 — Deployment-policy DECIDED: rollout-leaf is production

- **Decision:** production MCTS config = **rollout-leaf @ W6 iter-2**
  (agg Wilson lower 0.6479, n=120) over value-head-leaf (best-case 0.4524,
  honest-envelope-low 0.394, 1-of-3 seeds <0.40). ~20pp strength delta
  decides it; rollout-leaf is ~2.5× slower (value-head+ratio=1.5 cuts 61%
  wall-clock) — acceptable, no hard move-clock. Value-head+ratio=1.5 kept
  only as documented latency-SLA fallback.
- **Asymmetry:** disclose-as-aggregate + per-side caveat. Config choice
  does NOT cleanly shrink the gap (rollout +10pp; value-head +6 to +14pp
  across seeds, both player-weaker); side-conditioned sims is a
  non-blocking forward bet. No human stance call needed.
- **Claim string:** "rollout-leaf MCTS @ W6 iter-2 wins ≥64.8% vs rule-bot
  (Wilson-95 lower, n=120, side-balanced, reproducible); asymmetric —
  opponent ≥66.4%, player ≥55.8%; aggregate not per-side guarantee."
- Evidence-only, no new gate. Doc-hygiene: R8 block rolled to r15-archive.
- **Link:** `docs/ai-research/progress/r15.md` § "Deployment-policy
  decision — rollout-leaf vs value-head-leaf".
