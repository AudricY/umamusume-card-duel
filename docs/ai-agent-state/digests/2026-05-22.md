# 2026-05-22 — Slice 3b/3c land + tight-gate re-verdict #2 PARTIAL + deck-variety directive

## Slot 1 — throughput-spike Slice 3b LANDED (Rust v3.1 featurizer)

- 164-d temporal/turn-state featurizer ported to Rust (`engine-rs/crates/engine/src/policy/featurize.rs` gains `STATE_DIM_V3_1=164` + `observation_state_features_v3_1` = byte-identical 110-d v3.0 head + 54-d temporal/turn-state tail; +6 v3_1_* unit tests; engine 91→97 green). Inference module dispatches v3.0/v3.1/v3.2 by graph signature (5-input contract → state_features last-dim 110 vs 164; 7-input → v3.2). Unblocks the v3.1 re-verdict on the Rust fast path. Commit `d18021f`.

## Slot 2 — sim-eval-gate Slice 3c LANDED (real --workers parallelism)

- `--workers N` was a no-op (`let _ = args.workers;`). Now spawns N OS threads via `std::thread::scope` draining an `AtomicUsize`-cursored task queue; outcomes slotted by `task_index` for bit-stable aggregation. Workers=1 vs workers=8 wilsonLower identical to 16 decimal places on R110-W6-repro/iter-0 n=200 (0.4957060908195922); 4.5× speedup at 8 workers. Progress writer wrapped in `Mutex<BufWriter<File>>`. Commit `154248d`.

## Slot 3 — GPU EP scoping + queue/backlog refinement

- New scoping doc `docs/ai-research/scoping/gpu-inference-execution-provider.md` (222 lines, 5-slice plan G1-G5). Distinct from `gpu-fed-stronger-mcts.md` (strength axis vs this throughput axis). Surfaced because workers=16 scaling was 3.76× not 6-8× — `InferenceSession::session: Mutex<Session>` saturates around 8 concurrent workers. New queue entry `gpu-inference-execution-provider` P2; new backlog item P1 #10 (subsequent items renumbered 11-20). Commit `70fbc0d`.

## Slot 4 — tight-gate re-verdict #2 PARTIAL (v3.1 = R110-W6 v3.0)

- R16-P1 v3.1 iter-3 at n=10,000 returned wr=0.5907, wilson95=[**0.5810**, 0.6003], 10.9 min wall at workers=16 (vs HTTP-path attempt that died at game 426/10000 ETA 9+ days). **Δ < 0.0001** vs R110-W6 iter-2 wilson_lower 0.5811 — statistically indistinguishable. The n=120 0.5955 "v3.1 +0.0144 OVER 0.5811" §4d backlog flip was SAME high-tail noise that mis-framed R110's 0.6042. Representation axis (110-d vs 164-d temporal/turn-state) CLOSED as a strength lever. Pin position UNCHANGED. Per-side: player 0.5396 (vs R110's 0.5494), opponent 0.6145 (vs 0.6048). Canonical: `r110.md` §4e; `r16.md` closing footnote on R16-P1. Commit `3f881a5`.

## Slot 5 — USER DIRECTIVE: deck-variety gating training-bearing lines

- Investigation found every post-R110 measurement was on a single near-mirror Matikane matchup: `defaultAiOpponentDeckId='riceShowerHaruUrara'` does not match any id in `aiPremadeDecks`; silent fallback to `ai_decks.first()` = AI-flavor `matikanetannhauser`. 9 of 11 AI decks never simulated. Featurizer verified deck-agnostic with zero OOV; tight-gate 0.5811 ceiling is matchup-conditional. NO MORE TRAINING WITHOUT DECK VARIETY. `deck-pair-sampling` PROMOTED P2→P1; all training-bearing lines (w6-loop-anti-degradation HP sweep, per-game-pfsp-league-retry, value-head-data-program Stage 2/3, any new selfplay) tagged `BLOCKED-DECK-VARIETY` until deck-pair-sampling Slices P0+1+2 land. Measurement-only work on existing checkpoints (tight-gate re-verdict #3, high-sim probe, GPU EP slice) remains UNBLOCKED. Scoping: `docs/ai-research/scoping/deck-pair-sampling.md`.

## Slot 6 — GPU EP slice (G1+G2+G4) IN FLIGHT

- Implementer agent running; expected to land `--device cpu|cuda` flag on `sim-eval-gate` + `Device` enum + CUDA EP wiring in `InferenceSession` + n=200 CPU-vs-CUDA validation matrix (acceptance: |wl_cuda - wl_cpu| < 0.02 AND wallclock ≥1.5× faster). Highest-risk unknown: ort 2.0.0-rc.12 + CUDA 13.1 + ORT 1.22.0 ABI; clean-abort path documented in brief if dlopen fails on `libonnxruntime_providers_cuda.so`. Falsification: if rollouts dominate so heavily that GPU is dwarfed, commit G1+G2 as opt-in and document that throughput motivation was wrong. Pending notification.

## Slot 7 — deck-pair-sampling Slice 2 LANDED (training-bearing lines UNBLOCKED)

- All three orchestrators (`r12`, `dagger`, `ppo`) default-on `--deck-sampling=uniform` in self-play; eval-gate pinned to `fixed` (preserves apples-to-apples vs the 0.5811 v3.0 tight-gate history). TS plumbing: `--deck-sampling` flag added to `evaluateModelVsHeuristic.ts` CLI (sampler mirrors Rust row-major decompose). `orchestrator-state.json` records `selfplay_deck_sampling` + `eval_deck_sampling`. Smoke `runs/deck-sampling-slice2-smoke/` (R110-W6-repro/iter-2 v3.0, 1-iter, 19s wall): selfplay corpus covers **22/22** player×AI pairs in 30 games; gate manifest `perMatchup` ABSENT (eval stayed fixed); orchestrator-state confirms uniform/fixed split. UNBLOCKS `w6-loop-anti-degradation` HP sweep, `per-game-pfsp-league-retry`, `value-head-data-program` Stage 2/3 from the DECK-VARIETY axis (directive B v3.2-mandatory still applies independently). Slice 3 (diverse-matchup gate at n=10k) remains USER-GATED — fire only if a recipe-axis verdict comes back suspected to be matchup-conditional. Canonical: `docs/ai-research/scoping/deck-pair-sampling.md` § Slice 2 validation.

## Slot 8 — Slice 3 v3.2 first-gate done + G5 CPU Mutex removal landed

- **Slice 3 v3.2 first-gate** per user directive 'for slice 3 gate i want to be using v3.2 first'. Ran `sim-eval-gate --deck-sampling=uniform` on v3.2 lineage best (`runs/R16-P2-c8-w6fix-on-extended-2x-games/loop/iter-3/policy.onnx`, fixed n=240 wl=0.5538) at n=10,000 workers=16. Aggregate **wl=0.5690** (wr 0.5787), per-side player 0.6641 / opponent 0.4664 (+19.8pp asymmetry), per-matchup spread 15.6pp (riceShower:tamamoCross 0.6608 → riceShower:agnesDigital 0.5044). **KEY FINDING: v3.2 generalizes BETTER across decks than v3.0** — v3.2 uniform 0.5690 only -1.2pp below v3.0 fixed 0.5811, vs v3.0's own -4.8pp drop fixed→uniform. The slot-token representation is less matchup-overfit. Caveat: iter-3 ckpt was trained under `--deck-sampling=fixed` (Slice 2 landed after); this is off-distribution generalization, not "v3.2 ceiling under matchup-diverse training" (next: v3.2 retrain from R110-W6 with uniform selfplay default). Canonical: `docs/ai-research/scoping/deck-pair-sampling.md` § 'Slice 3 v3.2 first-gate verdict'. Artifact: `runs/deck-sampling-slice3-v32-gate/gate.manifest.json`.
- **G5 CPU-path Mutex removal** (commit `fb7d3f8`): replaced `SessionGuard::Cpu(Mutex<Session>)` with `UnsafeCell<Session>` (same pattern as G2's CUDA path). Acceptance matrix on R110-W6-repro/iter-0 v3.0 (n=200, sims=100, leaf=rollout): workers=1/16/32 wilson_lower all 0.4957060908195922 (bit-identical); elapsed 58.9s / 8.3s / 7.4s = **7.11× at workers=16** (was 4.5× at workers=8 in Slice 3c). Clears the ≥6× gate. CPU `intra/inter_threads=1` pins preserved (R14.G FP-determinism is about ORT-internal threading, orthogonal to Rust-side concurrent dispatch). Compounds for the v3.2 retrain — every iter's selfplay + gate runs ~7× faster.

## Slot 9 — v3.2 uniform retrain from R110-W6 — NEW LINEAGE BEST wl=0.5750 (+2.1pp)

- Recipe = C8-W6FIX-ON-extended-2x-games verbatim (n=240 fixed-matchup gate, rollout-leaf, sims=100/k=3/rollout-steps=200, W6-fix-ON, --kl-anchor-weight 0.05, --w6-replay-window 3 / --w6-replay-old-fraction 0.40, workers=24) seeded from v3.2 additive-tail init of R110-W6 iter-2 (bit-identical at iter-0: delta_logits=0.0 / delta_value=0.0) + `--uma-slot-tokens on` + `--deck-sampling=uniform` (Slice 2 default).
- Auto-halted at consec_fail=2 after 4 iters; ~5.5 min wall on G5 fast path. Trajectory wl = **0.5622 / 0.5750 / 0.5327 / 0.5369**. Selfplay corpus covered 22/22 deck pairs every iter.
- **NEW LINEAGE BEST** = `runs/v32-uniform-retrain-from-R110-W6/loop/iter-1/checkpoint.pt`, fixed-matchup wilson_lower **0.5750 at n=240**. **+2.1pp over prior v3.2 best** (C8-W6FIX-ON-extended-2x-games iter-3 wl=0.5538). Uniform-deck training under W6-fix-ON IS a real strength lift on the v3.2 lineage; the Slice 3 first-gate generalization claim compounds with the actual matchup-diverse training signal.
- The W6-loop iter-2 wobble + halt-after-2 pattern survived even under uniform decks — same mechanism now confirmed across W6-fix-ON/OFF, vhleaf/rollout-leaf, fixed/uniform decks, and seed-ckpt quality 0.5106→0.5750. The remaining untried lever is the W6-fix knob tune (--kl-anchor-weight / --w6-replay-window / --w6-replay-old-fraction).
- State: `runs/v32-uniform-retrain-from-R110-W6/loop/orchestrator-state.json`. Canonical writeup: `docs/ai-research/progress/r16.md` § R16-P3.

## Slot 10 — v3.2 PFSP-pool retrain from uniform iter-1 — FALSIFIED (regressed vs parent)

- Recipe = same uniform retrain skeleton + Option B PFSP pool wiring (commit `5509089`): `--rollout-vs-pool --pool-size 5 --pfsp-floor 0.05 --rollout-pool-state-file <uniform-retrain>/loop/orchestrator-state.json`. Pool at iter-0 resolved to 2 ckpts (uniform's iter-0 wl=0.5622 + iter-1 wl=0.5750); PFSP weights inversely proportional to opponent's rule-bot wilson_lower; iter-0 games split [61, 59] across [iter-1, iter-0].
- Auto-halted at consec_fail=2 after 3 iters. Trajectory wl = **0.5159 / 0.4785 / 0.5034**. iter-0 starts -0.0463pp BELOW the parent's iter-0 (0.5622) despite warm-starting from the parent's BEST ckpt (0.5750), and never recovers.
- **FALSIFIED per `scoping/cross-iter-opponent-pool-selfplay.md` acceptance gate.** Stronger-than-falsified: not "indistinguishable from baseline" — actively degraded. PFSP weighting (by-opponent-wl-vs-rule-bot) pulled the model toward weaker pool members, producing worse selfplay corpora than mirror-match would have.
- Option B wiring stays landed and is reusable for a future experiment with a DIFFERENT PFSP weight rule (e.g. weight-by-recent-model's-loss-vs-opponent rather than weight-by-opponent-wl-vs-rule-bot). The wiring landed clean; the lever doesn't work at this scale on this weighting rule.
- State: `runs/v32-pfsp-retrain-from-uniform-iter1/loop/orchestrator-state.json`. Canonical writeup: `docs/ai-research/progress/r16.md` § R16-P3.

## Slot 11 — set-attention architecture probe P0a PASS + Slice 1 local plumbing

- User directive promoted the set-attention architecture probe ahead of the W6 HP sweep. P0a ONNX MHA opset-17 roundtrip passed locally via `training/r7b3_mha_onnx_roundtrip_smoke.py`: max_abs_diff **7.153e-07** over 100 trials, below the 1e-05 pass threshold and far below the 1e-04 kill threshold.
- Local Slice 1 plumbing is implemented but not yet committed: `ModelConfig.model_variant`, `CandidatePolicyNet` `set_attention` branch, `train_bc.py --model-variant`, and `export_onnx.py` sidecar stamping. Important correction made during heartbeat: set-attention is a zero-initialized residual over the existing v3.2 MLP/slot trunk, not a replacement at init. This preserves the documented v3.2 warm-start parity while allowing training to learn relational corrections through `set_attention_encoder.output_proj`.
- Added `training/r7b3_set_attention_model_smoke.py`. It passes: set_attention requires v3.2 slot tensors, zero-init residual preserves v3.2 MLP logits/value, and non-zero attention projection moves outputs. Synthetic set-attention checkpoint export produced the expected 7-input ONNX graph with `model_variant=set_attention` in meta.json.
- Regression smokes still pass: `training/r16_p2_slot_encoder_smoke.py`, `training/r16_p2_slot_init_smoke.py`, plus py_compile over touched scripts. Next load-bearing step is Slice 2 SL training/gate; no Slice 3 launch until Slice 2 passes.

## Slot 12 — set-attention Slice 2 raw-policy gate MARGINAL (no Slice 3)

- P0b code-read completed: Rust inference dispatch treats any 7-input graph as v3.2 and already feeds `state_features`, `card_ids_by_zone`, `action_card_idx`, `uma_slot_card_ids`, and `uma_slot_features`; set-attention changes no Rust input shape.
- The historical item17/R4-era Slice 2 corpus `runs/item17-2026-05-11/iter-002/mixed.jsonl` predates `cardIdsByZone`, so it was re-extracted to `runs/r7b3-set-attention-slice2/mixed-v3.jsonl` via `training/r7b2_extract_features.py`. Re-extraction processed 3859 rows; loader retained 1344 rows with full v3.2 batch tensors.
- Trained `runs/r7b3-set-attention-slice2/model/checkpoint.pt` with `train_bc.py --epochs 50 --hidden-dim 64 --depth 2 --uma-slot-tokens --model-variant set_attention`. Training PASS; ONNX roundtrip PASS (`max_logit_diff` 1.43e-06). Final train accuracy 0.941; val accuracy 0.518.
- Raw-policy Slice 2 gate via `training/r8_gate_eval.py --games 1000 --seed-start 9000 --min-ci-lower 0.40` returned clean FAIL/marginal: aggregate wr **0.341**, wilson95 lower **0.3205** (TS side-balanced manifest has 2000 games total). Per-side: player lower 0.2753, opponent lower 0.3494. This is above the strict non-regression floor (R7.b.2 0.3045) but below the 0.40 acceptance gate, so it lands in the pre-registered **MARGINAL** band. Do NOT proceed to Slice 3 without explicit user gate. Artifact: `runs/r7b3-set-attention-slice2/gate.manifest.json`.

## Slot 13 — value-head wl>0.6 push NO-GO (best wl=0.4661)

- User directive: `wl>0.6 with value head; continuously commit.` Direct value-head gates on the current v3.2 lineage best (`runs/v32-uniform-retrain-from-R110-W6/loop/iter-1/checkpoint.pt`) stayed low: sims=100 wl **0.4005**, sims=400 wl **0.4127**. More value-head search depth is not enough.
- Fixed value-target tooling for v3.2: commit `c3ab174` threads slot tensors through `ValueTargetDataset`, `r13_value_retrain.py`, and `r14_value_crossover_probe.py`; commit `04865f5` forwards slot flags from `r12_orchestrator.py` into the crossover probe. Smoke `training/r16_value_target_v32_smoke.py` passed.
- Value-only rootValue retrains still failed: iter-1-only corpus gate wl **0.4209**; all-iters corpus gate wl **0.4250**. c-puct grid on the better retrain found local best at c=1.0, sims=100: wl **0.4661** / wr 0.5292; c=1.0 sims=400 regressed to wl 0.4250.
- Tuned vhleaf loop from the lineage best (`runs/value-head-wl60-v32-cpuct1-loop/loop/`) with c=1.0 halted after trajectory **0.4372 / 0.3883 / 0.3722**. Slot-aware crossover rerun on iter-2 crossed (ratio 0.604 / pearson 0.757), but strength was worst, confirming calibration-strength decoupling. Canonical writeup: `docs/ai-research/progress/r16.md` § "Value-head wl>0.6 push".

## Slot 14 — Q-head Stage 2 infra landed; first recipes NO-GO

- Landed the action-value data path: `2b70957` emits `rootMeanQ` in TS/Rust self-play rows; `f0df29a` loads it as optional `q_targets`/`q_target_mask`; `ee6fca1` adds `ModelConfig.uses_q_value_head` + `train_bc.py --q-value-head --q-value-weight`, exporting masked max-Q as the existing scalar `value`; `e0808d5` adds `--freeze-non-q-value-head` for an isolated-head control.
- Smoke `runs/qhead-rootmeanq-smoke/` produced 14 rows with rootMeanQ length matching legal actions. Tiny train/export smoke passed ONNX roundtrip with the new q head.
- First real Q corpus: `runs/qhead-v32-iter1-probe/selfplay.jsonl`, 120 rollout-leaf games from v3.2 iter-1, uniform decks, 100 sims, 808 rows. Shared-trunk Q-head recipe: val q_loss 0.428, gate wl **0.3802**. Frozen-base Q-head control: val q_loss 0.475, gate wl **0.3924**.
- Medium Q corpus: `runs/qhead-v32-large-corpus/selfplay.chunked.jsonl`, 150 rollout-leaf games / 1101 rows. Shared-trunk 60-epoch Q-head (`runs/qhead-v32-large-corpus/model-medium`) lifted over the first pass but still failed: val q_loss **0.581**, gate wl **0.4209** / wr 0.4833. It badly overfit target Qs (train 0.088 vs val 0.581).
- Lower-epoch control on the same corpus (`runs/qhead-v32-large-corpus/model-medium-e15`) improved val q_loss to **0.428** but gate stayed **wl 0.4209** / wr 0.4833. Early stopping alone does not convert Q-target fit into value-head MCTS strength.
- Frozen-base control on the same corpus (`runs/qhead-v32-large-corpus/model-medium-freeze`) val q_loss **0.436**, gate **wl 0.4086** / wr 0.4708. Freezing the trunk is worse than the shared-trunk medium controls.
- Verdict: Q-head/max-Q path is executable but current recipes are below scalar value-head baseline and nowhere near 0.6. Simple regularization is not enough; next self-directed axis is better target generation (more games and/or higher-sim rootMeanQ), not another head-only variant on the current rows.
