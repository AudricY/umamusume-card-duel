# 2026-05-21 — R16-TD 3a matrix locked + corpus harness spiked

## Slot 1 — R16-TD 3a source-recipe matrix smoke landed

- Source-recipe matrix (rule-bot-mirror / policy-vs-rule /
  search-vs-rule) is achieved via existing `--selection` +
  `--relabel-state-source` flags on `evaluateModelVsHeuristic.ts`;
  **no new flag needed** (runMctsRelabel operates on the live
  GameState independent of which selection played the action).
- New smoke `backend/src/tests/relabelMctsSmoke.ts` codifies three
  sub-cases: (a) rule-bot-mirror full audit (`policyTargets` length+sum,
  `oracle` schema, `stateSource` tag, contested-only emission) + relabel
  pass-through; (b) `policy-vs-rule` tag-wire-through (proves the tag
  is selection-independent); (c) `--relabel-mcts` OFF no-op invariant
  (no `policyTargets`/`oracle`/`stateSource` field leakage). Wired into
  `npm run test:train`; standalone ~2.5s, 34 rows/case.
- Per-recipe cost table (per decision): rule-bot-mirror = 1 MCTS call,
  no server; policy-vs-rule = 1 NN forward + 1 MCTS relabel, server
  required; search-vs-rule = 2 MCTS calls, server required if
  `--mcts-prior policy` (preferred — uniform priors degrade played
  search). Canonical: `docs/ai-research/progress/r16.md` §
  "Source-Recipe Matrix".
- NEXT CHUNK (user-gated, NOT auto-launched): the 200-400-game
  production corpus across the three recipes + Audit-v2 acceptance
  gate; then 3b. Larger-n contested-coverage confirmation gate stays
  DECOUPLED. queue `training-data-deep-program` `next_action` updated.

## Slot 4 — 3a prod corpus COMPLETE + Audit-v2 ALL PASS

- 3-recipe corpus generated at runs/R16-TD-3a-prod-corpus/ in 35.5 min
  (09:54-10:30) on the 16-worker evalGate harness against the 96-d
  production pin. Totals: 4196 + 3664 + 3938 = 11798 contested rows.
- Audit-v2 100% retained per recipe + aggregate; stateSource tags
  match; full 11-key oracle block; simplex policyTargets; all 7
  phases + 8 action kinds present in every recipe; ~50% of rows
  carry >=3 legal actions (the contested signal for 3b).
- Played-side WR: policy-vs-rule 0.319 (raw policy weak), 
  search-vs-rule 0.490 (search lifts model to near-parity vs rule-bot
  — expected ordering, search > raw policy).
- BC loader verified to consume 100% (JsonlPolicyDataset, soft path
  exercised in train_bc.py:490-498). Loader retention agrees with
  audit retention — scoping § P1 downstream gate closed.
- NEXT (3b chunk 1, scope pinned): new training/pair_builder.py
  (~150-200 LOC, runner-up mode only) + fixture smoke. Canonical:
  `docs/ai-research/progress/r16.md` § 'Production Corpus — Audit-v2 ALL PASS'.

## Slot 3 — Served-recipe smokes green + prod corpus launching

- Both served source-recipes smoked against live `serve_onnx`
  (`runs/R13-W6-phase-d/iter-2/policy.onnx`, 96-d v2, `--ort-threads
  auto`, CUDA + CPU providers): `policy-vs-rule` 16 rows PASS,
  `search-vs-rule` 28 rows PASS. Per-row audit (oracle + simplex
  `policyTargets` + `stateSource` tag) green; `/health` 200 in ~1s;
  clean SIGTERM shutdown. Smoke pattern matched relabelMctsSmoke.
- Launch script `runs/R16-TD-3a-prod-corpus/launch.sh` ready
  (sequential phase 1 server-free → phase 2 served policy → phase 3
  served mcts; trap-handled server lifecycle; disjoint seed ranges
  10000/20000/30000). Wall-clock projection: ~25-30 min. Canonical:
  `docs/ai-research/progress/r16.md` §§ "Multi-Worker Corpus Harness"
  / "Served-Recipe Smokes".

## Slot 2 — Multi-worker corpus harness spike (5.83x at N=16)

- Pilot's 14m23s was strictly single-thread (`evaluateModelVsHeuristic`
  iterates games in a sequential `for` loop; no workers). evalGate.ts
  already implemented work-stealing dispatch over the same per-game
  function but hardcoded `relabelMcts: false` and never serialized
  worker traces. Closing those two gaps lifts evalGate into the corpus
  harness — no new harness needed.
- Landed in `backend/src/sim/evalGate.ts`: --relabel-mcts / 
  --relabel-state-source pass-through; orchestrator buffers
  `result.decisionTraces` by taskIndex and flushes in deterministic
  order (covers static + work-stealing + single-worker paths);
  corpus-mode bypass makes gate floors informational under
  --relabel-mcts. Smoke `evalGateCorpusGenSmoke.ts` locks bit-identical
  determinism across --workers 1/4; wired into npm run test:train.
- Wall-clock on pilot-equivalent recipe (50g modelSide=both, 50 sims):
  N=1 873s; N=16 150s (5.83x); N=24 154s (past the knee). Sub-linear
  vs naive 16x = per-game variance × ~6 games/worker straggler tail
  dominates; no serve_onnx contention; no IPC backpressure. Projected
  prod-corpus wall-clock at N=16: ~25 min total (~5 min rule-bot-mirror
  + ~10 min each served recipe). Canonical:
  `docs/ai-research/progress/r16.md` § "Multi-Worker Corpus Harness".
