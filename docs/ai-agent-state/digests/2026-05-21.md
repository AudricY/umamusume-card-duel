# 2026-05-21 — R16-TD 3a/3b done-negative + Rust orchestrator wiring

## Slots 2-3 — 3a/3b harness + served-recipe smokes (collapsed)

- Multi-worker corpus harness landed (evalGate.ts: --relabel-mcts +
  trace buffer; N=16 5.83x at the knee; smoke
  `evalGateCorpusGenSmoke.ts` pins determinism). Served-recipe smokes
  green (CUDA+CPU); `R16-TD-3a-prod-corpus/launch.sh` ran the full
  3-phase corpus. All subsumed by chunks 5a-i (done-negative, slot 9).
  Canonical: `docs/ai-research/progress/r16.md` §§ "Multi-Worker
  Corpus Harness" / "Served-Recipe Smokes".

## Slot 5 — BC-on-relabel falsifies candidate-3

- Soft-CE BC on the 11798-row 3a relabel corpus (3ep, h64 d2
  dropout 0.05, lr 3e-4) lands pair_accuracy 0.6802 vs 0.6981
  baseline = -0.0179 — **OUTSIDE the ±0.022 noise band**. All 5
  ranking metrics regress vs ref; not flat, meaningfully worse.
- Contested heads (pass 0.632 / playTrainer 0.591 / retreatAttack
  0.484) drag the headline; routine actions (playBasic 0.938 /
  attachEnergy 0.856) are high. Exactly the decisions the corpus
  was supposed to teach.
- Candidate-3 (objective mismatch) FALSIFIED; candidate-1
  (relabel-MCTS too weak vs ref) is now the binding hypothesis.
  Forward line is a user direction call: stronger-MCTS re-relabel
  OR W6 HP-sweep (CEILING PATH A). Canonical:
  `docs/ai-research/progress/r16.md` § '3b — BC-on-relabel probe
  (chunk 5f)'.

## Slot 6 — Candidate-1 kill-test = Case B (labels confidently wrong on contested heads)

- Read-only relabel-vs-untrained-96d-ref audit (11798 rows): top-1
  agree 0.6338; craters on playTrainer 0.499 / pass 0.479 /
  retreatAttack 0.464 — same three heads BC cratered.
- Relabel entropy 0.766 < ref 0.876 (more committed, not
  budget-starved); disagreement p90 max-prob 0.91.
- **Case B**: labels confidently wrong. Stronger-MCTS demoted from
  cheap-first; new ladder = value-eval audit → 400-sim argmax-flip
  → only then escalate. Canonical: `docs/ai-research/progress/r16.md` sub-§ 'Candidate-1 kill-test (chunk 5g)'.

## Slot 8 — Chunk 5i — candidate-1 DIES (17.3% flip at 4× sims, below 30% threshold)

- Step 2 ran end-to-end (rule-bot-mirror @ 400 sims, 16 workers). On the contested-head + 100-sim max-prob ≥0.8 + state-identical subset (n=365): **flip 17.3% (63/365)**, 95% CI ~13.6-21.7% — below 30% LIVES AND 20% sticky.
- KL(400‖100) 1.85 nats but argmax sticks; entropy 100/400 = 0.351/0.543 → 100-sim MCTS is argmax-converged on this subset. Sim count NOT the binding lever.
- Per kind: pass 17.4% (n=195) / playTrainer 17.7% (n=164) / retreatAttack 0% (n=6 too small).
- Candidate-1 eliminated. Forward direction (user-gated): reframed step 1 (rollout-leaf return audit on contested states, no CRN-paired ambiguity) OR pivot to W6 HP-sweep (CEILING PATH A).
- Secondary finding: relabel MCTS bleeds RNG via global `random()` proxy in `frontend/src/game/engine/core/random.ts` — rule-bot-mirror trajectories diverged across sim counts despite deterministic selection. Tracked as P3 queue item `mcts-relabel-rng-bleed`.
- Canonical: `docs/ai-research/progress/r16.md` sub-§ 'Step 2 verdict (chunk 5i)'. Artifacts: `runs/R16-TD-3a-prod-corpus-400sim-rulebot/{flip-analysis.json,launch.log,rule-bot-mirror/traces.jsonl}`; script `training/killtest_argmax_flip.py`.

## Slot 9 — 3b corpus arm DONE-NEGATIVE; queue refreshed

- `training-data-deep-program` flipped to `done-negative`; chunks 5a-i exhaust the 3a/3b corpus signal under every tested objective (DPO 5a-e flat, soft-CE BC 5f −0.0179), label audit (5g labels confidently wrong), and MCTS budget (5i flip 17.3% < 30%).
- Deferral basis for `r16-p2-per-uma-slot-tokens` is now spent: the cheaper-than-P2 data-infra arm has concluded. P2 is unblocked but still user-gated for activation.
- Active autonomous P1 forward lines reduced to two: (a) `training-data-coverage-pilot` larger-n contested-coverage confirmation gate (n>=1000, side-balanced, mechanisms already shipped default-OFF) and (b) reframed step 1 rollout-leaf return audit on contested states (diagnostic-only, not a strength lever).
- CEILING PATH unchanged: A (W6 HP-sweep) and B (value-head-leaf-recipe-axis) both user-gated.
- P3 `mcts-relabel-rng-bleed` (chunk 5i secondary finding) unchanged.
- Queue-truth refresh only; no new research content. Canonical evidence: `docs/ai-research/progress/r16.md` chunks 5a-i.

## Slot 10 — Contested-coverage cheap slope FALSIFIED at n=1000

- Pre-registered n=1000/side confirmation gate ran end-to-end against the same three swept points (Option 1 contested resampling).
- Setup-error rule-outs PASSED: n=300 sanity reproduced pilot 0.2416 within tolerance (0.2447); coverage smoke matched pilot exactly (0.2022/0.3001/0.4500).
- n=1000 numbers (side-balanced Wilson-lower): baseline **0.2840** / frac030 **0.2860** / frac045 **0.2656**. Monotonicity broken (frac045 regressed below baseline); margin sign-flipped from pilot +2.2pp to **−1.8pp**.
- All three pilot point estimates fall BELOW the n=1000 95% CIs — the n=300 pilot was on the low tail of sampling noise. Documented "falsifies the contested-coverage bottleneck" outcome triggered.
- `training-data-coverage-pilot` → `done-falsified`. Both autonomous P1 data-side forward lines now closed end-to-end (sibling `training-data-deep-program` done-negative chunks 5a-i). Only remaining autonomous work: diagnostic-only reframed step 1.
- Canonical: `docs/ai-research/progress/r16.md` sub-§ 'Chunk 5j — n=1000 confirmation gate FALSIFIES cheap-slope'. Artifacts: `runs/R16-TD-coverage-pilot-confirmation/verdict.json`.

## Slot 11 — Rust sim-cli wired as default in r12 orchestrator (Slice 1)

- `training/r12_orchestrator.py:run_selfplay` + `run_gate` now dispatch via new `resolve_engine_command(engine, sim_name, repo_root)`; `--engine` CLI flag added, **default `rust`**, `--engine ts` opt-out.
- Rust release binaries built (`cargo build --release -p sim-cli`): `engine-rs/target/release/sim-{eval-gate,mcts-selfplay,export-training,throughput-probe}` (4.1/4.2/1.9/0.95 MB). Added `--model-url` alias on `sim-eval-gate --challenger`; orchestrator appends `--record-rows` on the Rust selfplay path (TS implicit).
- Smoke: 2-game Rust `sim-eval-gate` via dispatch path exit 0 in 0.05s, valid manifest + 2 progress rows; TS dispatch returns legacy `npm` prefix; missing-binary + bad-engine errors raise as expected.
- `ppo_orchestrator.py` + `dagger_orchestrator.py` audited — no direct sim CLI invocations (only `train_bc.py`/`export_onnx.py`/`serve_onnx.py`); left alone.
- NEXT: Slice 2 = 1-iter R110 parity run `--engine rust` vs paired `--engine ts`. Canonical: queue entry `rust-port-orchestrator-wiring` + `docs/ai-research/scoping/rust-engine-port-handoff.md`.

## Slots 12/13/19 — repo-cleanup buckets 1/3/4 (collapsed)

- Bucket 4 (state-file truth): queue.json reconciled vs r16/r110/backlog; escalations trimmed 88→67 lines (open 759→<500 chars); caps all under (digest 124/132, backlog 300/300, r16 324/500, r110 296/500, r15 393/500, notes 31).
- Bucket 1 (TODO/FIXME/XXX/HACK sweep): exactly 1 word-boundary hit (`r13_ai_decide_smoke.py:151` stale JSON string referencing already-landed R14.E successor); rewritten to pointer. 3 false-positives left alone (prose-`hack`, `"xxx"` seed-anonymization in audit f-strings).
- Bucket 3 (docs link-walk + canonical-home audit): ~322 unique inline-code refs walked; zero broken routing-map links; five stale 'next' / superseded sections fixed inline (`rust-engine-port-plan` → SUPERSEDED; `rust-engine-port-handoff` 'What is left' → historical pointer; `r110-w6-reproduction` → DONE-MARGINAL; `r16-model-feature-backlog-refinement` P2 plan → LANDED; `r12-selfplay-gate-throughput` item 4 → graduated to queue item; `r16-training-data-backlog-refinement` → SUPERSEDED). Pattern: scoping docs lacked top-of-file STATUS-DONE headers when their experiments landed.

## Slot 14 — Slice 2 R110 parity A/B FOUND SCHEMA GAP (collapsed)

- Paired R110-faithful 1-iter A/B caught Rust `sim-mcts-selfplay --record-rows` emitting per-game wrapper while Python distill loader expects flat per-decision rows with `kind='mcts-selfplay'`. Fixed in slot 15 (option a — flatten Rust output). Full record at `docs/ai-research/scoping/rust-engine-port-handoff.md` § 'Phase 1h follow-up'.

## Slot 15 — Slice 2 schema gap FIXED + parity driver re-launched

- Rust `sim-mcts-selfplay --record-rows` now emits TS-flat per-decision rows (key set identical to TS `mctsSelfPlay.ts` SelfPlayRow incl. `kind="mcts-selfplay"` + post-hoc `valueTarget`/`result` backfill after each game terminates; `seed` serialized as string). Per-game wrapper retained for `record_rows=false` callers.
- Two cargo unit tests inside `engine-rs/crates/sim-cli/src/bin/mcts_selfplay.rs` pin the canonical TS-flat key set + `fill_terminal_result` (+1/-1/0 + correct `points{P,O}`) so future drift fails at `cargo test`, not at distill-stage crash time.
- Schema-parity smoke at N=2 matched seeds (`prior=uniform leaf=rollout`) confirmed identical top-level + observation + legalActions key sets; Python `load_mcts_selfplay_samples` accepted 18 Rust rows cleanly into `MctsSelfPlaySample`s.
- Driver re-launched 09:16Z (PID 64362; partial `iter-0/` cleared, no prior `orchestrator-state.json`). Rust selfplay in-flight at 191 rows after ~20s (60-game target). ETA Rust ~30-40min, TS sequential ~90-100min after.
- Canonical: `docs/ai-research/scoping/rust-engine-port-handoff.md` § 'Phase 1h follow-up — Resolution (option a landed)'. Queue `rust-port-orchestrator-wiring` → `slice-2-parity-run-relaunched-post-schema-fix`.

## Slots 16-18 — Slice 2 closed (collapsed)

- Bugs 1+2 fixed: `sim-eval-gate` emits TS-shape manifest (parsed by `r12_orchestrator.load_summary` w/ defense-in-depth reproject), `sim-mcts-selfplay` `add_root_dirichlet` defaults ON (opt-out `--no-root-dirichlet`, mirrors `mctsSelfPlay.ts:627`); `--workers` single-process by design.
- Re-scoped eval-gate-only A/B PASS at edge of ±0.07 band: Rust wilson_lower 0.5189 / TS 0.5869 over 120 side-balanced games each; wall ratio Rust:TS=0.343x.
- Games-semantics fixed: `sim-eval-gate --seeds` → `--games` (legacy alias kept), tasks built `sides × games` to mirror `evalGate.ts:43-49`; cargo `test --release -p sim-cli` 6/6 PASS. mcts_selfplay needed no change (no modelSide axis).
- Canonical: `docs/ai-research/scoping/rust-engine-port-handoff.md` § 'Phase 1h follow-up — Slice 2 verdict' + § 'RESOLVED 2026-05-21'.

## Slot 20 — repo-cleanup bucket 2 (orphan scripts) — final slice, pass DONE

- Census: training 70 .py (65 with `__main__`), engine-rs/scripts 8 (.py/.ts/.sh/.mjs), backend/scripts NONE, repo-root NONE.
- Deleted 20 orphan scripts (zero non-archive refs): 19 training/*.py (R12 closed: `r12_orchestrator_smoke`, `r12_spike`, `r12_spike_smoke`; R13 closed: `r13_adaptive_halt_smoke`, `r13_latency_probe`, `r13_mcts_ladder`, `r13_mcts_ladder_smoke`, `r13_parallel_smoke`, `r13_selfplay_parallel_smoke`, `r13_value_retrain_smoke`, `r13_value_retrain_experiment`; R14 closed: `r14_adaptive_ratio_sweep`, `r14_ood_gate`, `r14_ort_throughput_smoke`; R2: `multi_temp_gate`; zero-ref smokes: `observability_app_smoke`, `serve_backlog_smoke`, `r16_p2_slot_dataset_smoke`, `r16_v31_distill_init_smoke`) + `engine-rs/scripts/dump-ts-rollout.ts` (resolved heuristic-divergence debug helper).
- Training count 70→51 .py; engine-rs/scripts 8→7. Kept `r12_distill_smoke.py` (active r16 scoping acceptance test), `r15_single_worker_determinism_replay.py` (active r15.md).
- Headline pattern: closed-sprint smokes outlive their sprint plans by years; sprint-plan archival did not propagate to script-level cleanup. Documented for next sprint-closure.
- `repo-cleanup-consolidation` status flipped DONE; loop_note updated. All six buckets landed. Forward priority: rust-port-orchestrator-wiring escape-hatch retire → deferred research.

## Slot 21 — C8-W6FIX-ON launch (collapsed — superseded by slot 22 verdict)

- Launch parameters + acceptance + monitor commands subsumed by Slot 22's verdict. See `runs/R16-P2-c8-w6fix-on/launch.sh` for the exact recipe.

## Slot 22 — C8-W6FIX-ON NO-GO on strength but FIRST-EVER value-head crossover

- 5 iters, ~17 min wallclock on Rust path. Trajectory wl=0.5358/0.5189/0.4281/0.5022/0.5189. NO-GO: iter-0 0.5358 < 0.5955 floor; best 0.5358 < 0.6042 ceiling. W6-fix-ON dampens iter-0 lift (−6.0pp vs C8 W6-fix-OFF) while partially preserving iter-1 (−1.7pp vs C8's −6.8pp).
- iter-1 produced FIRST-EVER R14 value-head crossover (ratio 0.722 / pearson 0.725) across any retrain experiment (R13.W3 PARTIAL / R13.W8 regressed / C8/C8b/C8c failed). Strength axis and calibration axis are decoupled — iter-1 was the worst non-crater iter on strength but the only crossed iter.
- Pre-flighted vhleaf gate against iter-1 ckpt: wilson_lower **0.4281** — PASS vs 0.40 production bar (+0.0281), FAIL vs R14.A 0.452 baseline (−0.0239). vhleaf unblocked (first non-degenerate vhleaf ever) but not yet a strength lever.
- Queue refresh: w6-loop-anti-degradation → `done-no-go-but-calibration-positive`; value-head-data-program → `done-positive-via-c8-w6fix-on` (Stage 1 satisfied as side effect); value-head-leaf-recipe-axis → P3 `unblocked-at-production-bar-not-yet-strength-lever`; rust-port-orchestrator-wiring → `done-rust-default-confirmed` (first real promoted-checkpoint gate completed cleanly).
- Forward call: W6-fix knob tune (anneal `--kl-anchor-weight` 0.05→0.02-0.03 OR reduce `--w6-replay-old-fraction` 0.40→0.25) to recover C8 iter-0 0.5955 floor while keeping the calibration crossing. Parallel/independent: full vhleaf selfplay loop seeded from iter-1 ckpt (user-gated).
- Canonical: `docs/ai-research/progress/r16.md` § 'C8-W6FIX-ON'. Artifacts: `runs/R16-P2-c8-w6fix-on/loop/orchestrator-state.json` + `runs/R16-P2-c8-w6fix-on-vhleaf-gate/gate.manifest.json`.

## Slot 23 — C8-W6FIX-ON EXTENDED launched + first-ever loop-throughput profile in flight

- `runs/R16-P2-c8-w6fix-on-extended/launch-profiled.sh` running 10 more iters seeded from parent iter-4 ckpt (wl=0.5189); recipe held verbatim (state-dim 110 / hidden 64 / depth 2 / kl-anchor-weight 0.05 / W6-fix-ON / `--uma-slot-tokens` / `--engine rust` / workers 24 / n=120 gate). Wrapper PID 5822 → orchestrator 5848 → Rust `sim-mcts-selfplay` 5945; `serve_onnx` on port 47153 `--provider cpu` (default).
- Three background profilers writing at 5s sample to `runs/R16-P2-c8-w6fix-on-extended/profile-{gpu.csv,cpu.txt,mem.txt}`. ETA ~35 min for 10 iters.
- T+0:15 GPU sample: **0% utilization / 0 MiB memory** during selfplay — `serve_onnx --provider cpu` default means the entire selfplay+gate path is CPU-only, GPU sits idle for ~95% of wall-time. Sharpens the throughput-spike thesis before the run even finishes.
- New P3 queue item `throughput-optimization-spike` scopes the post-hoc analysis: cheapest plausible win is `serve_onnx --provider cuda` (one-line CLI flip to unload predict off CPU); larger wins contingent on whether HTTP round-trip dominates (-> NAPI in-process predict via `rust-port-backend-napi-consumer`).
- Parent run timing (210s/iter = 70s selfplay + 4s distill + 134s gate) shows gate is ~2x selfplay despite same MCTS — also a spike candidate.
- Monitor: `ps -p 5822`; `jq '.iterations[-3:]' runs/R16-P2-c8-w6fix-on-extended/loop/orchestrator-state.json`; `tail runs/R16-P2-c8-w6fix-on-extended/profile-gpu.csv`.
