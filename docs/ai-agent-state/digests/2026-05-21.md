# 2026-05-21 — R16-TD 3a source-recipe matrix regression-locked

## Slot 1 — R16-TD 3a source-recipe matrix smoke landed

- Source-recipe matrix (rule-bot-mirror / policy-vs-rule /
  search-vs-rule) is achieved via existing `--selection` +
  `--relabel-state-source` flags on `evaluateModelVsHeuristic.ts`;
  **no new flag needed** (runMctsRelabel operates on the live
  GameState independent of which selection played the action).
- New smoke `backend/src/tests/relabelMctsSmoke.ts` codifies three
  sub-cases: (a) rule-bot-mirror full audit (`policyTargets` length+sum,
  `oracle` schema, `stateSource` tag, contested-only emission) + relabel
  pass-through; (b) `policy-vs-rule` tag-wire-through (proves the tag
  is selection-independent); (c) `--relabel-mcts` OFF no-op invariant
  (no `policyTargets`/`oracle`/`stateSource` field leakage). Wired into
  `npm run test:train`; standalone ~2.5s, 34 rows/case.
- Per-recipe cost table (per decision): rule-bot-mirror = 1 MCTS call,
  no server; policy-vs-rule = 1 NN forward + 1 MCTS relabel, server
  required; search-vs-rule = 2 MCTS calls, server required if
  `--mcts-prior policy` (preferred — uniform priors degrade played
  search). Canonical: `docs/ai-research/progress/r16.md` §
  "Source-Recipe Matrix".
- NEXT CHUNK (user-gated, NOT auto-launched): the 200-400-game
  production corpus across the three recipes + Audit-v2 acceptance
  gate; then 3b. Larger-n contested-coverage confirmation gate stays
  DECOUPLED. queue `training-data-deep-program` `next_action` updated.
