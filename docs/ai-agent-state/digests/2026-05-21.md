# 2026-05-21 — R16-TD 3a/3b done-negative + Rust orchestrator wiring

## Slots 2-3 — 3a/3b harness + served-recipe smokes (collapsed)

- Multi-worker corpus harness landed (evalGate.ts: --relabel-mcts +
  trace buffer; N=16 5.83x at the knee; smoke
  `evalGateCorpusGenSmoke.ts` pins determinism). Served-recipe smokes
  green (CUDA+CPU); `R16-TD-3a-prod-corpus/launch.sh` ran the full
  3-phase corpus. All subsumed by chunks 5a-i (done-negative, slot 9).
  Canonical: `docs/ai-research/progress/r16.md` §§ "Multi-Worker
  Corpus Harness" / "Served-Recipe Smokes".

## Slots 5/6/8 — R16-TD chunks 5f/5g/5i done-negative (collapsed)

- Chunk 5f (BC-on-relabel) falsified candidate-3 at pair_accuracy 0.6802 vs 0.6981 (−0.0179, outside ±0.022). Chunk 5g (label audit) showed Case B (labels confidently wrong on contested heads, top-1 0.6338, entropy 0.766<0.876). Chunk 5i (400-sim flip) eliminated candidate-1 at 17.3% (n=365, 95% CI 13.6-21.7%, below 30% LIVES). Secondary: P3 `mcts-relabel-rng-bleed`. Canonical: `docs/ai-research/progress/r16.md` chunks 5f/5g/5i.

## Slot 9 — 3b corpus arm DONE-NEGATIVE; queue refreshed

- `training-data-deep-program` flipped to `done-negative`; chunks 5a-i exhaust the 3a/3b corpus signal under every tested objective (DPO 5a-e flat, soft-CE BC 5f −0.0179), label audit (5g labels confidently wrong), and MCTS budget (5i flip 17.3% < 30%).
- Deferral basis for `r16-p2-per-uma-slot-tokens` is now spent: the cheaper-than-P2 data-infra arm has concluded. P2 is unblocked but still user-gated for activation.
- Active autonomous P1 forward lines reduced to two: (a) `training-data-coverage-pilot` larger-n contested-coverage confirmation gate (n>=1000, side-balanced, mechanisms already shipped default-OFF) and (b) reframed step 1 rollout-leaf return audit on contested states (diagnostic-only, not a strength lever).
- CEILING PATH unchanged: A (W6 HP-sweep) and B (value-head-leaf-recipe-axis) both user-gated.
- P3 `mcts-relabel-rng-bleed` (chunk 5i secondary finding) unchanged.
- Queue-truth refresh only; no new research content. Canonical evidence: `docs/ai-research/progress/r16.md` chunks 5a-i.

## Slot 10 — Contested-coverage cheap slope FALSIFIED at n=1000

- Pre-registered n=1000/side confirmation gate ran end-to-end against the same three swept points (Option 1 contested resampling).
- Setup-error rule-outs PASSED: n=300 sanity reproduced pilot 0.2416 within tolerance (0.2447); coverage smoke matched pilot exactly (0.2022/0.3001/0.4500).
- n=1000 numbers (side-balanced Wilson-lower): baseline **0.2840** / frac030 **0.2860** / frac045 **0.2656**. Monotonicity broken (frac045 regressed below baseline); margin sign-flipped from pilot +2.2pp to **−1.8pp**.
- All three pilot point estimates fall BELOW the n=1000 95% CIs — the n=300 pilot was on the low tail of sampling noise. Documented "falsifies the contested-coverage bottleneck" outcome triggered.
- `training-data-coverage-pilot` → `done-falsified`. Both autonomous P1 data-side forward lines now closed end-to-end (sibling `training-data-deep-program` done-negative chunks 5a-i). Only remaining autonomous work: diagnostic-only reframed step 1.
- Canonical: `docs/ai-research/progress/r16.md` sub-§ 'Chunk 5j — n=1000 confirmation gate FALSIFIES cheap-slope'. Artifacts: `runs/R16-TD-coverage-pilot-confirmation/verdict.json`.

## Slot 11 — Rust sim-cli wired as default in r12 orchestrator (Slice 1)

- `training/r12_orchestrator.py:run_selfplay` + `run_gate` now dispatch via new `resolve_engine_command(engine, sim_name, repo_root)`; `--engine` CLI flag added, **default `rust`**, `--engine ts` opt-out.
- Rust release binaries built (`cargo build --release -p sim-cli`): `engine-rs/target/release/sim-{eval-gate,mcts-selfplay,export-training,throughput-probe}` (4.1/4.2/1.9/0.95 MB). Added `--model-url` alias on `sim-eval-gate --challenger`; orchestrator appends `--record-rows` on the Rust selfplay path (TS implicit).
- Smoke: 2-game Rust `sim-eval-gate` via dispatch path exit 0 in 0.05s, valid manifest + 2 progress rows; TS dispatch returns legacy `npm` prefix; missing-binary + bad-engine errors raise as expected.
- `ppo_orchestrator.py` + `dagger_orchestrator.py` audited — no direct sim CLI invocations (only `train_bc.py`/`export_onnx.py`/`serve_onnx.py`); left alone.
- NEXT: Slice 2 = 1-iter R110 parity run `--engine rust` vs paired `--engine ts`. Canonical: queue entry `rust-port-orchestrator-wiring` + `docs/ai-research/scoping/rust-engine-port-handoff.md`.

## Slots 12/13/19 — repo-cleanup buckets 1/3/4 (collapsed)

- Bucket 4 (state-file truth): queue.json reconciled vs r16/r110/backlog; escalations trimmed 88→67 lines (open 759→<500 chars); caps all under (digest 124/132, backlog 300/300, r16 324/500, r110 296/500, r15 393/500, notes 31).
- Bucket 1 (TODO/FIXME/XXX/HACK sweep): exactly 1 word-boundary hit (`r13_ai_decide_smoke.py:151` stale JSON string referencing already-landed R14.E successor); rewritten to pointer. 3 false-positives left alone (prose-`hack`, `"xxx"` seed-anonymization in audit f-strings).
- Bucket 3 (docs link-walk + canonical-home audit): ~322 unique inline-code refs walked; zero broken routing-map links; five stale 'next' / superseded sections fixed inline (`rust-engine-port-plan` → SUPERSEDED; `rust-engine-port-handoff` 'What is left' → historical pointer; `r110-w6-reproduction` → DONE-MARGINAL; `r16-model-feature-backlog-refinement` P2 plan → LANDED; `r12-selfplay-gate-throughput` item 4 → graduated to queue item; `r16-training-data-backlog-refinement` → SUPERSEDED). Pattern: scoping docs lacked top-of-file STATUS-DONE headers when their experiments landed.

## Slot 14 — Slice 2 R110 parity A/B FOUND SCHEMA GAP (collapsed)

- Paired R110-faithful 1-iter A/B caught Rust `sim-mcts-selfplay --record-rows` emitting per-game wrapper while Python distill loader expects flat per-decision rows with `kind='mcts-selfplay'`. Fixed in slot 15 (option a — flatten Rust output). Full record at `docs/ai-research/scoping/rust-engine-port-handoff.md` § 'Phase 1h follow-up'.

## Slot 15 — Slice 2 schema gap FIXED + parity driver re-launched

- Rust `sim-mcts-selfplay --record-rows` now emits TS-flat per-decision rows (key set identical to TS `mctsSelfPlay.ts` SelfPlayRow incl. `kind="mcts-selfplay"` + post-hoc `valueTarget`/`result` backfill after each game terminates; `seed` serialized as string). Per-game wrapper retained for `record_rows=false` callers.
- Two cargo unit tests inside `engine-rs/crates/sim-cli/src/bin/mcts_selfplay.rs` pin the canonical TS-flat key set + `fill_terminal_result` (+1/-1/0 + correct `points{P,O}`) so future drift fails at `cargo test`, not at distill-stage crash time.
- Schema-parity smoke at N=2 matched seeds (`prior=uniform leaf=rollout`) confirmed identical top-level + observation + legalActions key sets; Python `load_mcts_selfplay_samples` accepted 18 Rust rows cleanly into `MctsSelfPlaySample`s.
- Driver re-launched 09:16Z (PID 64362; partial `iter-0/` cleared, no prior `orchestrator-state.json`). Rust selfplay in-flight at 191 rows after ~20s (60-game target). ETA Rust ~30-40min, TS sequential ~90-100min after.
- Canonical: `docs/ai-research/scoping/rust-engine-port-handoff.md` § 'Phase 1h follow-up — Resolution (option a landed)'. Queue `rust-port-orchestrator-wiring` → `slice-2-parity-run-relaunched-post-schema-fix`.

## Slots 16-18 — Slice 2 closed (collapsed)

- Bugs 1+2 fixed: `sim-eval-gate` emits TS-shape manifest (parsed by `r12_orchestrator.load_summary` w/ defense-in-depth reproject), `sim-mcts-selfplay` `add_root_dirichlet` defaults ON (opt-out `--no-root-dirichlet`, mirrors `mctsSelfPlay.ts:627`); `--workers` single-process by design.
- Re-scoped eval-gate-only A/B PASS at edge of ±0.07 band: Rust wilson_lower 0.5189 / TS 0.5869 over 120 side-balanced games each; wall ratio Rust:TS=0.343x.
- Games-semantics fixed: `sim-eval-gate --seeds` → `--games` (legacy alias kept), tasks built `sides × games` to mirror `evalGate.ts:43-49`; cargo `test --release -p sim-cli` 6/6 PASS. mcts_selfplay needed no change (no modelSide axis).
- Canonical: `docs/ai-research/scoping/rust-engine-port-handoff.md` § 'Phase 1h follow-up — Slice 2 verdict' + § 'RESOLVED 2026-05-21'.

## Slot 20 — repo-cleanup bucket 2 (orphan scripts) — final slice, pass DONE

- Census: training 70 .py (65 with `__main__`), engine-rs/scripts 8 (.py/.ts/.sh/.mjs), backend/scripts NONE, repo-root NONE.
- Deleted 20 orphan scripts (zero non-archive refs): 19 training/*.py (R12 closed: `r12_orchestrator_smoke`, `r12_spike`, `r12_spike_smoke`; R13 closed: `r13_adaptive_halt_smoke`, `r13_latency_probe`, `r13_mcts_ladder`, `r13_mcts_ladder_smoke`, `r13_parallel_smoke`, `r13_selfplay_parallel_smoke`, `r13_value_retrain_smoke`, `r13_value_retrain_experiment`; R14 closed: `r14_adaptive_ratio_sweep`, `r14_ood_gate`, `r14_ort_throughput_smoke`; R2: `multi_temp_gate`; zero-ref smokes: `observability_app_smoke`, `serve_backlog_smoke`, `r16_p2_slot_dataset_smoke`, `r16_v31_distill_init_smoke`) + `engine-rs/scripts/dump-ts-rollout.ts` (resolved heuristic-divergence debug helper).
- Training count 70→51 .py; engine-rs/scripts 8→7. Kept `r12_distill_smoke.py` (active r16 scoping acceptance test), `r15_single_worker_determinism_replay.py` (active r15.md).
- Headline pattern: closed-sprint smokes outlive their sprint plans by years; sprint-plan archival did not propagate to script-level cleanup. Documented for next sprint-closure.
- `repo-cleanup-consolidation` status flipped DONE; loop_note updated. All six buckets landed. Forward priority: rust-port-orchestrator-wiring escape-hatch retire → deferred research.

## Slot 21 — C8-W6FIX-ON launch (collapsed — superseded by slot 22 verdict)

- Launch parameters + acceptance + monitor commands subsumed by Slot 22's verdict. See `runs/R16-P2-c8-w6fix-on/launch.sh` for the exact recipe.

## Slot 22 — C8-W6FIX-ON NO-GO on strength but FIRST-EVER value-head crossover

- 5 iters, ~17 min wallclock on Rust path. Trajectory wl=0.5358/0.5189/0.4281/0.5022/0.5189. NO-GO: iter-0 0.5358 < 0.5955 floor; best 0.5358 < 0.6042 ceiling. W6-fix-ON dampens iter-0 lift (−6.0pp vs C8 W6-fix-OFF) while partially preserving iter-1 (−1.7pp vs C8's −6.8pp).
- iter-1 produced FIRST-EVER R14 value-head crossover (ratio 0.722 / pearson 0.725) across any retrain experiment (R13.W3 PARTIAL / R13.W8 regressed / C8/C8b/C8c failed). Strength axis and calibration axis are decoupled — iter-1 was the worst non-crater iter on strength but the only crossed iter.
- Pre-flighted vhleaf gate against iter-1 ckpt: wilson_lower **0.4281** — PASS vs 0.40 production bar (+0.0281), FAIL vs R14.A 0.452 baseline (−0.0239). vhleaf unblocked (first non-degenerate vhleaf ever) but not yet a strength lever.
- Queue refresh: w6-loop-anti-degradation → `done-no-go-but-calibration-positive`; value-head-data-program → `done-positive-via-c8-w6fix-on` (Stage 1 satisfied as side effect); value-head-leaf-recipe-axis → P3 `unblocked-at-production-bar-not-yet-strength-lever`; rust-port-orchestrator-wiring → `done-rust-default-confirmed` (first real promoted-checkpoint gate completed cleanly).
- Forward call: W6-fix knob tune (anneal `--kl-anchor-weight` 0.05→0.02-0.03 OR reduce `--w6-replay-old-fraction` 0.40→0.25) to recover C8 iter-0 0.5955 floor while keeping the calibration crossing. Parallel/independent: full vhleaf selfplay loop seeded from iter-1 ckpt (user-gated).
- Canonical: `docs/ai-research/progress/r16.md` § 'C8-W6FIX-ON'. Artifacts: `runs/R16-P2-c8-w6fix-on/loop/orchestrator-state.json` + `runs/R16-P2-c8-w6fix-on-vhleaf-gate/gate.manifest.json`.

## Slot 23 — C8-W6FIX-ON EXTENDED launched + profiling (collapsed — superseded by slot 24)

- Launch parameters, profiler wiring, and T+0:15 GPU=0% observation subsumed by slot 24's completion. See `runs/R16-P2-c8-w6fix-on-extended/launch-profiled.sh` + `profile-{gpu.csv,cpu.txt,mem.txt}` artifacts.

## Slot 24 — C8-W6FIX-ON EXTENDED done (iter-9 0.5527 new best) + 2x-games extension in flight

- 10 iters completed cleanly. Trajectory wl=0.5106/0.5022/0.5022/0.4856/0.4690/0.4526/0.5358/0.5358/0.4773 NO-PROMOTE/**0.5527** — **iter-9 0.5527 is the new all-time best on this lineage** (+1.7pp over 5-iter parent's 0.5358, +4.2pp over extension iter-0). R14 value-head crossover crossed at iter-3 (ratio 0.79 / pearson 0.72; second-ever after parent iter-1). Profiler trio wrote complete to `runs/R16-P2-c8-w6fix-on-extended/profile-{gpu.csv,cpu.txt,mem.txt}` — analysis deferred to a later cycle.
- Doubled-games extension launched 2026-05-21T11:26Z: `runs/R16-P2-c8-w6fix-on-extended-2x-games/launch-profiled.sh` seeded from extended iter-9 ckpt, `--selfplay-games 120` + `--eval-games 120` (n=240 gate), `--selfplay-seed-start 30600` + `--eval-seed-start 9001`. All other knobs verbatim. Wrapper PID 45839 → orchestrator 45872 → Rust `sim-mcts-selfplay` 45949; `serve_onnx` PID 45910 on port 46849 `--provider cpu`. Same profiler trio (GPU/CPU/mem 5s sample). ETA ~60 min.
- `w6-loop-anti-degradation` queue refresh: status `extended-done-iter9-best-2x-in-flight`; on completion process profiler timeseries into `docs/ai-research/analysis/r12-loop-throughput.md`. Monitor: `ps -p 45839`; `jq '.iterations[-3:]' runs/R16-P2-c8-w6fix-on-extended-2x-games/loop/orchestrator-state.json`; `tail runs/R16-P2-c8-w6fix-on-extended-2x-games/profile-gpu.csv`.

## Slot 25 — throughput-spike analysis LANDED — HTTP-RTT is the binding constraint, NAPI is the lever

- `docs/ai-research/analysis/r12-loop-throughput.md` (127 lines) landed from sealed extended-run profilers. Per-iter mean 197.8 ± 8.4s = selfplay 69.3s (35.1%) + distill 4.49s (2.3%) + gate 124.0s (62.7%). Wrapper overhead 1.2%.
- GPU mean **0.26%**, 98.5% samples = 0%, peak 26% (transient ONNX stand-up only). CPU system mean-busy **5.4%** across 16 reported cores; zero cores ever ≥80% busy; load avg peak 2.26/32. Mem free min 27 GiB. **System is IDLE — neither GPU nor CPU saturated.**
- Bottleneck attribution: HTTP-RTT between Rust sim-cli and serve_onnx. Rust worker ~0.3 cores, serve_onnx ~0.1 cores active. Predict compute ~10μs vs round-trip ~ms ⇒ provider cpu→cuda is a near-no-op.
- Scoping hypothesis 'GPU idle 95% due to --provider cpu' CONFIRMED on idle observation but REFUTED on causal mechanism. Gate=2×-selfplay gap fully explained by 2× games (per-game wall: gate 1.03s ≤ selfplay 1.16s); candidate C retired.
- Ranking flipped: A (cuda) ≤1.05× refuted · **B (NAPI in-process predict) 3-10× recommended FIRST** · C retired · D (worker pool) 6-20× ceiling but capped by serve_onnx until B lands.
- Scoping doc STATUS → LANDED-pointer; `throughput-optimization-spike` queue P3→P2 `analysis-landed-recommend-napi-in-process-predict`; spike-B acceptance: 10-iter recipe wall <20 min (vs 33.4 baseline) + strength within ±0.022.

## Slot 26 — throughput-spike Slice 1 (Option A: Rust `ort`) PARITY GATE GREEN

- Landed Rust `ort 2.0.0-rc.12` (load-dynamic, api-21) + v3.0 featurizer port (`engine-rs/crates/engine/src/policy/featurize.rs`) + inference layer (`engine-rs/crates/engine/src/inference/mod.rs`) + parity-smoke binary (`engine-rs/crates/sim-cli/src/bin/inference_parity.rs`). Sim-cli binaries gain `--onnx-path`; `--model-url` kept ignored-legacy for one release. ORT 1.22 reuse via `ORT_DYLIB_PATH=training/.venv/lib/python3.12/site-packages/onnxruntime/capi/libonnxruntime.so.1.22.0`. Option A chosen over NAPI to keep engine canonical (per project-rust-first-class-default); NAPI bridge stays available for the backend MCTS-serve path (`rust-port-backend-napi-consumer`).
- Parity smoke: 50 random `(state, legal_actions)` snapshots on R110-W6-repro iter-2 v3.0 ONNX vs serve_onnx --ort-threads 1 — max_prob_diff **2.4e-7**, max_value_diff **3.0e-7** (both <<1e-5 tol). Per-call inference latency: Rust **106µs** vs HTTP **1606µs = 15× at the call site**. 5-game cross-run at `--mcts-leaf value-head --mcts-prior policy` bit-identical across two runs (4 player wins / 1 opp win, empty diff).
- v3.2 (per-Uma slot tokens) DEFERRED to follow-up slice — session-load validator rejects v3.2 graphs with a clean 'use v3.0 only in this slice' error so a smoke against the v3.2 lineage best fails loud (not silent). 85 engine unit tests pass (incl. 5 new featurize + 3 new inference); release build wall 21-25s post-ort.
- Cargo workspace adds ort + ndarray 0.17 workspace deps (matched ORT transitive major to avoid two-crate split that breaks the From impl). HTTP `/predict` path removed from `mcts/driver.rs`; `ureq` now only pulled by the parity smoke binary.
- `throughput-optimization-spike` queue → `slice-1-parity-gate-green-orchestrator-wiring-next`. NEXT (Slice 2): wire `training/r12_orchestrator.py:run_selfplay` + `run_gate` to pass `--onnx-path` + `ORT_DYLIB_PATH` env to the Rust sim-cli binaries instead of `--model-url`; skip `serve_onnx_context` stand-up on the Rust path. Acceptance: 10-iter C8-W6FIX-ON-extended recipe wall <20 min + strength within ±0.022. Canonical: `docs/ai-research/scoping/throughput-optimization-spike.md`.

## Slot 27 — throughput-spike Slice 2 orchestrator-wiring LANDED (collapsed — superseded by slot 28 verdict)

- Launch parameters + acceptance recipe + monitor commands subsumed by Slot 28. See `runs/throughput-spike-slice2-acceptance/launch.sh` + `docs/ai-research/scoping/throughput-optimization-spike.md` § 'Slice 2 acceptance' for the exact recipe and verdict numbers.

## Slot 28 — throughput-spike Slice 2 PASSED 4.18× + Slice 4 scale-up relook LANDED

- 10-iter acceptance (`runs/throughput-spike-slice2-acceptance/`) ran in **7.99 min wall vs 33.4 min baseline = 4.18× speedup** — clears both ≥1.65× PASS gate and ≥3× scale-up trigger. Trajectory 0.4773 / 0.5022 / 0.5273 / 0.4939 / 0.5189 / 0.4856 / 0.5442 / 0.5358 / 0.5189 / **0.5527** (iter-9 promoted). All 10 iters promote=True; no crashes. Per-iter mean ~48s = selfplay ~16s + distill ~4s + gate ~28s (gate dominates). R14 crossover crossed iter-4 (ratio 0.787, pearson 0.727) + iter-9 (ratio 0.730, pearson 0.746) without explicit W6-fix-ON. Canonical: `runs/throughput-spike-slice2-acceptance/loop/orchestrator-state.json`.
- Slice 4 scale-up relook LANDED at `docs/ai-research/scoping/post-throughput-scale-up-directions.md` (SCOPING, 51 lines). Re-prioritization call (made per `feedback-self-directed-research-picks`): **PRIMARY = `tight-gate-reverdict-program`** (P2→P1) for n=10k re-verdict of R110-W6-repro iter-2 0.6042 + R16-P1 v3.1 + 5g/5h/5i flips (~2h total wall; highest backlog-state-flip probability per dollar). **SECONDARY** = multi-knob W6-fix HP sweep (10-cell diagonal `--kl-anchor-weight` × `--w6-replay-window` × `--w6-replay-old-fraction`, ~80 min wall). **TERTIARY = `high-sim-mcts-regime-probe`** (P2→P1) at 800/4k/20k/50k sims against both 5g kill candidates AND R110 production reference. DEPRIORITIZED: larger-model arch sweep + 50+ iter long-horizon (gate on positive signal from PRIMARY/SECONDARY/TERTIARY). BACKGROUND: side-conditioned eval as default `sim-eval-gate` report shape (~half-day plumbing; recurring benefit).
- Queue refresh: `throughput-optimization-spike` → `slice-2-PASS-4.18x-slice-4-relook-landed-slice-3-optional`; `tight-gate-reverdict-program` + `high-sim-mcts-regime-probe` both P2→P1; `per-game-pfsp-league-retry` stays P2 unblocked. Slice 3 (v3.2 featurizer port) now OPTIONAL — required only for the user-queued vhleaf loop on the fast path; alternative is HTTP path at ~33 min/10 iters.
- Surprise in relook: R14 crossover crossed TWICE under v3.0 / W6-fix-OFF in the Slice 2 acceptance run (iter-4 + iter-9). C8-W6FIX-ON's 'first-ever crossover across any retrain experiment' claim still stands (C8 was v3.2 + W6-fix-ON; Slice-2 is v3.0 fresh chain from R110), but the broader 'calibration crossing requires W6-fix-ON' framing softens: a v3.0 R110-continuation can also cross. Worth a follow-up audit but not a forward line on its own.

## Slot 29 — tight-gate-reverdict-program PRIMARY launched — R110-W6-repro iter-2 at n=10,000

- `runs/R110-W6-repro-iter2-tight-gate/` launched 2026-05-21T23:30Z (PID 43242). Recipe-faithful to original R110 iter-2 gate (sims=100, c-puct=1.5, leaf=rollout, k=3, rollout-steps=200, prior=policy, collapse-max=64, max-nodes=5000, max-steps=500, seed-start=9000, model-side both); only n collapses 120 → 10,000. Pure-Rust in-process ORT path (no serve_onnx; project-rust-first-class-default).
- T+15s health snapshot: 56 games complete, runningWinRate 0.5357, ~3.7 games/sec sustained, binary-reported etaSec ~2720s = **~45 min wallclock** — matches scoping-doc per-re-verdict estimate (~40 min). Surprise vs brief's "~38 min" projection: in line, but slightly conservative.
- Verdict thresholds: PASS=wilson95.lower≥0.60 (defends 0.6042 pin); PARTIAL=[0.55,0.60) → pin overgenerous, downgrade to MARGINAL-WEAK; FAIL=<0.55 → 0.6042 was tail-noise, pin needs reset and multiple downstream NO-GO verdicts (R16-P1 v3.1, 5g/5h/5i kills) become re-verdict candidates.
- Monitor: `ps -p 43242 -o pid,etime`; `tail -1 runs/R110-W6-repro-iter2-tight-gate/gate-progress.jsonl | jq '{gameIndex,totalGames,runningWinRate,etaSec}'`; on completion `jq '.status,.summary.games,.summary.modelWinRate,.summary.wilson95' runs/R110-W6-repro-iter2-tight-gate/gate.manifest.json`. Next re-verdicts queue after this one: R16-P1 v3.1 + training-data-deep-program 5g/5h/5i flips. Canonical: `docs/ai-research/scoping/post-throughput-scale-up-directions.md` §4 PRIMARY.

## Slot 30 — throughput-spike Slice 3 LANDED — v3.2 featurizer on Rust fast path

- v3.2 per-Uma slot featurizer (`uma_slot_card_ids` int64[10] + `uma_slot_features` float32[10, 23]) ported to Rust verbatim from Python `observation_to_uma_slots` + `_uma_slot_feature_row`. Engine inference module now detects v3.0 vs v3.2 by ONNX input-set (5 vs 7) at load time and dispatches the right ort::inputs! map; partial-pair guard mirrors `serve_onnx._graph_has_partial_uma_slot_inputs`; v3.1 (164-d) still hard-rejected. 6 new unit tests (shape, polarity/role/present, absent 4th-bench reserved slot, hp+damage sum-to-1, typed-energy sum, absent-active row); engine 91/91 green.
- Parity smoke v3.2 ONNX (`runs/R16-P2-c8-w6fix-on-extended-2x-games/loop/iter-3/policy.onnx`, sidecar `state_feature_schema_version=3.2`, `uses_uma_slot_tokens=true`, `card_vocab.hash=e3a35716156494d6`): 50 random states agree at `max_prob_diff=1.788e-7`, `max_value_diff=4.470e-7` (well under 1e-5 tol); Rust 150µs vs HTTP 1632µs = **10.88× per-call speedup**. 5-game cross-run at `--leaf rollout --prior policy --no-root-dirichlet --seed-base 2500 --sims 20` is byte-identical between runs (`diff` empty, 37 lines each, 2/3 player/opp wins). `--leaf rollout` chosen per brief — vhleaf is exercised by the user-queued loop downstream.
- vhleaf loop unblock: the user-queued 10-iter w6-fix-ON value-head-leaf loop (seeded from 2x-games iter-3 ckpt wl=0.5538) can now ride the Rust fast path at expected ~12 min/10-iter wall (Slice-2 4.18× pace), down from ~33 min/10-iter on HTTP. Queue entries `throughput-optimization-spike` → `all-slices-LANDED-v3.0+v3.2-fast-path-green`; `w6-loop-anti-degradation` next_action updated to reflect unblock. Canonical: `docs/ai-research/scoping/throughput-optimization-spike.md` § Slice 3.
