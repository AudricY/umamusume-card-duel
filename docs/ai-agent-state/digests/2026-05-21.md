# 2026-05-21 — R16-TD 3a/3b done-negative + Rust orchestrator wiring

## Slot 3 — Served-recipe smokes green + prod corpus launching

- Both served source-recipes smoked against live `serve_onnx`
  (`runs/R13-W6-phase-d/iter-2/policy.onnx`, 96-d v2, `--ort-threads
  auto`, CUDA + CPU providers): `policy-vs-rule` 16 rows PASS,
  `search-vs-rule` 28 rows PASS. Per-row audit (oracle + simplex
  `policyTargets` + `stateSource` tag) green; `/health` 200 in ~1s;
  clean SIGTERM shutdown. Smoke pattern matched relabelMctsSmoke.
- Launch script `runs/R16-TD-3a-prod-corpus/launch.sh` ready
  (sequential phase 1 server-free → phase 2 served policy → phase 3
  served mcts; trap-handled server lifecycle; disjoint seed ranges
  10000/20000/30000). Wall-clock projection: ~25-30 min. Canonical:
  `docs/ai-research/progress/r16.md` §§ "Multi-Worker Corpus Harness"
  / "Served-Recipe Smokes".

## Slot 2 — Multi-worker corpus harness spike (5.83x at N=16)

- Pilot's 14m23s was strictly single-thread (`evaluateModelVsHeuristic`
  iterates games in a sequential `for` loop; no workers). evalGate.ts
  already implemented work-stealing dispatch over the same per-game
  function but hardcoded `relabelMcts: false` and never serialized
  worker traces. Closing those two gaps lifts evalGate into the corpus
  harness — no new harness needed.
- Landed in `backend/src/sim/evalGate.ts`: --relabel-mcts / 
  --relabel-state-source pass-through; orchestrator buffers
  `result.decisionTraces` by taskIndex and flushes in deterministic
  order (covers static + work-stealing + single-worker paths);
  corpus-mode bypass makes gate floors informational under
  --relabel-mcts. Smoke `evalGateCorpusGenSmoke.ts` locks bit-identical
  determinism across --workers 1/4; wired into npm run test:train.
- Wall-clock on pilot-equivalent recipe (50g modelSide=both, 50 sims):
  N=1 873s; N=16 150s (5.83x); N=24 154s (past the knee). Sub-linear
  vs naive 16x = per-game variance × ~6 games/worker straggler tail
  dominates; no serve_onnx contention; no IPC backpressure. Projected
  prod-corpus wall-clock at N=16: ~25 min total (~5 min rule-bot-mirror
  + ~10 min each served recipe). Canonical:
  `docs/ai-research/progress/r16.md` § "Multi-Worker Corpus Harness".

## Slot 5 — BC-on-relabel falsifies candidate-3

- Soft-CE BC on the 11798-row 3a relabel corpus (3ep, h64 d2
  dropout 0.05, lr 3e-4) lands pair_accuracy 0.6802 vs 0.6981
  baseline = -0.0179 — **OUTSIDE the ±0.022 noise band**. All 5
  ranking metrics regress vs ref; not flat, meaningfully worse.
- Contested heads (pass 0.632 / playTrainer 0.591 / retreatAttack
  0.484) drag the headline; routine actions (playBasic 0.938 /
  attachEnergy 0.856) are high. Exactly the decisions the corpus
  was supposed to teach.
- Candidate-3 (objective mismatch) FALSIFIED; candidate-1
  (relabel-MCTS too weak vs ref) is now the binding hypothesis.
  Forward line is a user direction call: stronger-MCTS re-relabel
  OR W6 HP-sweep (CEILING PATH A). Canonical:
  `docs/ai-research/progress/r16.md` § '3b — BC-on-relabel probe
  (chunk 5f)'.

## Slot 6 — Candidate-1 kill-test = Case B (labels confidently wrong on contested heads)

- Read-only relabel-vs-untrained-96d-ref audit (11798 rows): top-1
  agree 0.6338; craters on playTrainer 0.499 / pass 0.479 /
  retreatAttack 0.464 — same three heads BC cratered.
- Relabel entropy 0.766 < ref 0.876 (more committed, not
  budget-starved); disagreement p90 max-prob 0.91.
- **Case B**: labels confidently wrong. Stronger-MCTS demoted from
  cheap-first; new ladder = value-eval audit → 400-sim argmax-flip
  → only then escalate. Canonical: `docs/ai-research/progress/r16.md` sub-§ 'Candidate-1 kill-test (chunk 5g)'.

## Slot 8 — Chunk 5i — candidate-1 DIES (17.3% flip at 4× sims, below 30% threshold)

- Step 2 ran end-to-end (rule-bot-mirror @ 400 sims, 16 workers). On the contested-head + 100-sim max-prob ≥0.8 + state-identical subset (n=365): **flip 17.3% (63/365)**, 95% CI ~13.6-21.7% — below 30% LIVES AND 20% sticky.
- KL(400‖100) 1.85 nats but argmax sticks; entropy 100/400 = 0.351/0.543 → 100-sim MCTS is argmax-converged on this subset. Sim count NOT the binding lever.
- Per kind: pass 17.4% (n=195) / playTrainer 17.7% (n=164) / retreatAttack 0% (n=6 too small).
- Candidate-1 eliminated. Forward direction (user-gated): reframed step 1 (rollout-leaf return audit on contested states, no CRN-paired ambiguity) OR pivot to W6 HP-sweep (CEILING PATH A).
- Secondary finding: relabel MCTS bleeds RNG via global `random()` proxy in `frontend/src/game/engine/core/random.ts` — rule-bot-mirror trajectories diverged across sim counts despite deterministic selection. Tracked as P3 queue item `mcts-relabel-rng-bleed`.
- Canonical: `docs/ai-research/progress/r16.md` sub-§ 'Step 2 verdict (chunk 5i)'. Artifacts: `runs/R16-TD-3a-prod-corpus-400sim-rulebot/{flip-analysis.json,launch.log,rule-bot-mirror/traces.jsonl}`; script `training/killtest_argmax_flip.py`.

## Slot 9 — 3b corpus arm DONE-NEGATIVE; queue refreshed

- `training-data-deep-program` flipped to `done-negative`; chunks 5a-i exhaust the 3a/3b corpus signal under every tested objective (DPO 5a-e flat, soft-CE BC 5f −0.0179), label audit (5g labels confidently wrong), and MCTS budget (5i flip 17.3% < 30%).
- Deferral basis for `r16-p2-per-uma-slot-tokens` is now spent: the cheaper-than-P2 data-infra arm has concluded. P2 is unblocked but still user-gated for activation.
- Active autonomous P1 forward lines reduced to two: (a) `training-data-coverage-pilot` larger-n contested-coverage confirmation gate (n>=1000, side-balanced, mechanisms already shipped default-OFF) and (b) reframed step 1 rollout-leaf return audit on contested states (diagnostic-only, not a strength lever).
- CEILING PATH unchanged: A (W6 HP-sweep) and B (value-head-leaf-recipe-axis) both user-gated.
- P3 `mcts-relabel-rng-bleed` (chunk 5i secondary finding) unchanged.
- Queue-truth refresh only; no new research content. Canonical evidence: `docs/ai-research/progress/r16.md` chunks 5a-i.

## Slot 10 — Contested-coverage cheap slope FALSIFIED at n=1000

- Pre-registered n=1000/side confirmation gate ran end-to-end against the same three swept points (Option 1 contested resampling).
- Setup-error rule-outs PASSED: n=300 sanity reproduced pilot 0.2416 within tolerance (0.2447); coverage smoke matched pilot exactly (0.2022/0.3001/0.4500).
- n=1000 numbers (side-balanced Wilson-lower): baseline **0.2840** / frac030 **0.2860** / frac045 **0.2656**. Monotonicity broken (frac045 regressed below baseline); margin sign-flipped from pilot +2.2pp to **−1.8pp**.
- All three pilot point estimates fall BELOW the n=1000 95% CIs — the n=300 pilot was on the low tail of sampling noise. Documented "falsifies the contested-coverage bottleneck" outcome triggered.
- `training-data-coverage-pilot` → `done-falsified`. Both autonomous P1 data-side forward lines now closed end-to-end (sibling `training-data-deep-program` done-negative chunks 5a-i). Only remaining autonomous work: diagnostic-only reframed step 1.
- Canonical: `docs/ai-research/progress/r16.md` sub-§ 'Chunk 5j — n=1000 confirmation gate FALSIFIES cheap-slope'. Artifacts: `runs/R16-TD-coverage-pilot-confirmation/verdict.json`.

## Slot 11 — Rust sim-cli wired as default in r12 orchestrator (Slice 1)

- `training/r12_orchestrator.py:run_selfplay` + `run_gate` now dispatch via new `resolve_engine_command(engine, sim_name, repo_root)`; `--engine` CLI flag added, **default `rust`**, `--engine ts` opt-out.
- Rust release binaries built (`cargo build --release -p sim-cli`): `engine-rs/target/release/sim-{eval-gate,mcts-selfplay,export-training,throughput-probe}` (4.1/4.2/1.9/0.95 MB). Added `--model-url` alias on `sim-eval-gate --challenger`; orchestrator appends `--record-rows` on the Rust selfplay path (TS implicit).
- Smoke: 2-game Rust `sim-eval-gate` via dispatch path exit 0 in 0.05s, valid manifest + 2 progress rows; TS dispatch returns legacy `npm` prefix; missing-binary + bad-engine errors raise as expected.
- `ppo_orchestrator.py` + `dagger_orchestrator.py` audited — no direct sim CLI invocations (only `train_bc.py`/`export_onnx.py`/`serve_onnx.py`); left alone.
- NEXT: Slice 2 = 1-iter R110 parity run `--engine rust` vs paired `--engine ts`. Canonical: queue entry `rust-port-orchestrator-wiring` + `docs/ai-research/scoping/rust-engine-port-handoff.md`.

## Slot 12 — repo-cleanup-consolidation bucket 4 (state-file truth)

- Reconciled queue.json against r16.md / r110.md / backlog: no major drift found — prior wiring commit had already updated `r16-p2-per-uma-slot-tokens` to `done-mixed-signal` and `w6-loop-anti-degradation` to P2 `ready-with-new-evidence` per the C8 iter-0 +6.8pp lift. `repo-cleanup-consolidation` status flipped from `scoped-blocked-on-rust-orchestrator-wiring` to `bucket-4-state-file-truth-in-flight`; loop_note refreshed.
- Cap enforcement: escalations.md trimmed (open 759→<500 chars; two resolved pointers collapsed; file 88→67 lines). Backlog 300/300, digest 124/132 lines pre-slot, r16 324/500, r110 296/500, r15 393/500, notes.md 31 durable-only — all under cap.
- Loop_note priority order unchanged: Slice 2 → repo-cleanup buckets 1/2/3/5/6 → deferred research.

## Slot 13 — repo-cleanup-consolidation bucket 1 (TODO/FIXME/XXX/HACK sweep)

- Census across backend/ training/ engine-rs/ frontend/ (.ts/.tsx/.js/.py/.rs): exactly **1** word-boundary TODO/FIXME/XXX/HACK hit, in `training/r13_ai_decide_smoke.py:151` — and it lives inside a JSON status-string, not a code comment.
- Disposition: 0 resolved-in-place / 0 converted-to-pointer / **1 stale-deleted (rewritten)** / 0 left-alone. Hit referenced "Full real-state decision call is a TODO for next W5 milestone"; W5 successor `training/r14_ai_decide_e2e_smoke.py` (R14.E) already exists and covers exactly that. Replaced the note with a pointer to the R14 smoke.
- Case-insensitive sweep surfaced 3 false-positives (left alone, rationale): `r13_ai_decide_smoke.py:72` prose-`hack` mid-sentence in discarded-approach comment; `audit_relabel_corpus.py:67` + `data_coverage_audit.py:84` use `"xxx"` as seed-anonymization suffix in audit f-strings.
- No new queue/backlog entries needed (the deferred work the stale TODO referenced is already done; no orphan future-work).
- Validation: `python3 -m ast` parse on the touched file OK; no logic change, only a string-literal edit. Skipped npm/cargo (no TS/Rust touched).
- NEXT in cleanup ladder: buckets 2/3/5/6 still pending.
- Buckets 1/2/3/5/6 (TODO/FIXME sweep, orphan scripts, docs-link walk, post-Slice-2 cap re-check, CLAUDE.md re-read) remain.

## Slot 14 — Slice 2 R110 parity A/B FOUND SCHEMA GAP at distill interface

- Paired R110-faithful 1-iter A/B launched 09:02Z via `runs/R110-rust-parity/driver.sh` (sequential Rust→TS; matched seeds --selfplay-seed-start 30120 / --eval-seed-start 9000; same R110 iter-2 0.6042 starting ckpt; --epochs 20 --hidden-dim 64 --depth 2 --kl-anchor-weight 0.05 --mcts-leaf rollout --no-w6-fix-* --eval-games 60 --workers 24).
- Rust selfplay COMPLETED CLEANLY: 60 game records in 60.76s (single-process; Rust binary --workers documented IGNORED), 2.0 MB JSONL, events.jsonl recorded `selfplay:completed rows=60`. ~8.9x over R110 W6-repro TS 542s baseline at matched recipe.
- Distill stage then RAISED `uma_ai.dataset.RowSchemaError: Expected kind='mcts-selfplay' at selfplay.jsonl:1, got None`. Root cause: Rust `sim-mcts-selfplay --record-rows` emits ONE record per game with nested `rows` array (keys [seed, terminalReason, turnNumber, winner, totalSteps, modelDecisions, rows]); TS emits N flat per-decision records with `kind='mcts-selfplay'` discriminator + schemaVersion/sideId/observation/legalActions/visitDistribution/rootPriors/rootValue/valueTarget keys.
- Why Slice 1 smoke missed it: Slice 1 covered the eval-gate dispatch path only — selfplay→distill interface was never exercised end-to-end under --engine rust. TS side of A/B never launched (driver was sequential; Rust failure was blocker).
- DIVERGENCE FILED at `docs/ai-research/scoping/rust-engine-port-handoff.md` § "Phase 1h follow-up — Slice 2 parity-run finding". Two retire-options: (a) flatten Rust output to TS flat-row schema (clean Phase-0-faithful fix), or (b) teach `load_mcts_selfplay_samples` the new shape (cheap unblock). Pick (a) by default.
- Secondary: Rust `--workers` is single-process by design; only TS gets fan-out today. Load-bearing if Rust selfplay wall exceeds GPU distill time per iter — track for later.
- Artefacts: `runs/R110-rust-parity-rust/{iter-0/selfplay.jsonl, iter-0/distill.log, events.jsonl}` + `runs/R110-rust-parity/driver.log`. Queue `rust-port-orchestrator-wiring` flipped to `slice-2-parity-run-found-schema-gap`; Slice 2 re-launch BLOCKED on schema-gap fix.

## Slot 15 — Slice 2 schema gap FIXED + parity driver re-launched

- Rust `sim-mcts-selfplay --record-rows` now emits TS-flat per-decision rows (key set identical to TS `mctsSelfPlay.ts` SelfPlayRow incl. `kind="mcts-selfplay"` + post-hoc `valueTarget`/`result` backfill after each game terminates; `seed` serialized as string). Per-game wrapper retained for `record_rows=false` callers.
- Two cargo unit tests inside `engine-rs/crates/sim-cli/src/bin/mcts_selfplay.rs` pin the canonical TS-flat key set + `fill_terminal_result` (+1/-1/0 + correct `points{P,O}`) so future drift fails at `cargo test`, not at distill-stage crash time.
- Schema-parity smoke at N=2 matched seeds (`prior=uniform leaf=rollout`) confirmed identical top-level + observation + legalActions key sets; Python `load_mcts_selfplay_samples` accepted 18 Rust rows cleanly into `MctsSelfPlaySample`s.
- Driver re-launched 09:16Z (PID 64362; partial `iter-0/` cleared, no prior `orchestrator-state.json`). Rust selfplay in-flight at 191 rows after ~20s (60-game target). ETA Rust ~30-40min, TS sequential ~90-100min after.
- Canonical: `docs/ai-research/scoping/rust-engine-port-handoff.md` § 'Phase 1h follow-up — Resolution (option a landed)'. Queue `rust-port-orchestrator-wiring` → `slice-2-parity-run-relaunched-post-schema-fix`.

## Slot 16 — Slice 2 follow-up bugs 1+2 FIXED (orchestrator manifest + selfplay dirichlet)

- Bug 1 (load-bearing): `sim-eval-gate` now emits TS-shape manifest `{status,failures,expectFail,corpusMode,args,summary:{games,modelWinRate,wilson95:{lower,upper},heuristicFallbacks,overall,...}}` (mirrors `evalGate.ts:156`). `r12_orchestrator.load_summary` parses both shapes — defense-in-depth reproject for legacy flat `{overall:{wilsonLower,...}}` so `runs/R110-rust-parity-rust/iter-0/gate.manifest.json` re-parses to wilson_lower=0.441/games=60. New `RuntimeError` after gate fires loud if `passed=true` but parser returns 0/0.
- Bug 2: `sim-mcts-selfplay` `add_root_dirichlet` defaults ON; opt-out via `--no-root-dirichlet` (mirrors `mctsSelfPlay.ts:627`); manifest_args echoes `addRootDirichlet` for audit. Eval-gate stays Dirichlet=OFF (TS `evalGate.ts:509` opt-in only) — comment-pinned at construction site.
- Cargo `test --release -p sim-cli`: 3/3 pass (incl. new `dirichlet_gate_mirrors_ts_selfplay_default`). Matched-seed `sim-mcts-selfplay` smoke (sims=8, base=7000): 5/6 visit distributions differ vs `--no-root-dirichlet`, confirming Dirichlet noise now lives.
- Follow-up (3) `--workers` is by design (single-process Rust binary; orchestrator parallelises by spawning multiple binaries) — NOT a bug, struck. Queue → `slice-2-followup-bugs-1-and-2-fixed-rescoped-ab-pending`.
