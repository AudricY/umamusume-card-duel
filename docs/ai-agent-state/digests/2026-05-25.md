## Slot 1 — pure value-head wl>0.6 resume

- Goal clarified: endpoint must use pure learned value-head leaves replacing rollout. Rollout/value blending is diagnostic only, not an acceptable endpoint.
- Current best pure learned-leaf result remains far below target: 50k teacher corpus did not improve gate strength despite better validation q_loss.
- 50k teacher corpus (`runs/qhead-v32-50k-teacher-corpus/selfplay-50k.jsonl`): 53,164 rows, 4,000 unique seeds, 28,848 player / 24,316 opponent rows, 22/22 deck pairs, zero bad rows. It was generated with eight parallel chunk processes before the Rust selfplay worker-pool fix.
- First-pass 50k Q-head e5: val q_loss **0.346**, but pure legal-mean Q c=1.0/2.0 wl **0.4372/0.4331**. Export scalar sweep stayed flat: max **0.4372/0.4372**, policy_mean **0.4331/0.4290**, top2_mean **0.4250/0.4578**, top3_mean **0.4537/0.4372** at c=1.0/2.0.
- 50k e15 overfit slightly: train q_loss **0.219**, val q_loss **0.354**, and top2_mean c=2.0 regressed to wl **0.4331**.
- 50k diagnostic: q predictions correlate with targets but are MCTS-weak. Target-argmax accuracy **0.467**, predicted argmax in target top-2 **0.831**, scalar correlations mean/top2/top3/max **0.793/0.783/0.786/0.750**. Optimistic scalars are under-estimated, but calibrated exports did not recover strength: top2 scale 1.3 wl **0.4331**, top3 scale 1.25 **0.4290**, max scale 1.5 **0.4619**, max scale 2.0 c=1.0/2.0 **0.4331/0.4127**.
- Random-bot floor (`runs/random-bot-baseline/`) added `sim-eval-gate --selection random`, a true uniform legal-action modeled-side baseline. Fixed n=1000/side random reached wr **0.3635**, wl **0.3427**; uniform-deck n=1000/side random reached wr **0.4045**, wl **0.3832**. Current pure value-head failures are weak but above random, not catastrophic random-level play.
- High-sim label-stability probe (`runs/qhead-v32-highsim-label-stability-probe/`) re-ran the first 20 seeds/side from the 50k corpus at 800 sims. It produced 40 games / 280 rows. Whole-game trajectories diverged quickly: 137 `(seed, sideId, step)` matches but only 47 exact observation matches. On exact matches, 200-sim labels were already stable: Q-pair corr **0.957**, target argmax agreement **0.787**, selected-action agreement **0.915**, mean abs Q delta **0.096**. Verdict: do not brute-force whole games; high-sim work needs a state-level relabeler focused on hard/leaf-distribution states.
- Leaf-state tooling is now available: `sim-mcts-selfplay --record-rollout-leaf-rows --rollout-leaf-rows-per-decision N` emits internal rollout-leaf states as value-target rows with `rootValue = rollout label`, and `--workers` now runs games concurrently in-process. Smoke `runs/leaf-state-value-target-smoke/` emitted 108 valid leaf rows and `ValueTargetDataset(..., state_dim=110, uses_uma_slot_tokens=True)` loaded all 108. Worker determinism smoke `runs/selfplay-worker-determinism-smoke/` confirmed workers 1 vs 2 produce byte-identical JSONL on a fixed 6-game run.
- Small leaf-state value-head probe (`runs/value-head-v32-leafstate-probe/`) generated 8,907 first-capped leaf rows from v3.2 best policy-prior rollout MCTS (80 seeds/side, uniform decks, no Dirichlet, workers=16). Corpus has 22/22 deck pairs, no vector-length defects, sha `49afe041633b3767a309aea31e282fc45de04371e37ef6daef3f39a5f405c18e`, rootValue mean **0.171**. A 10-epoch value-only retrain fit poorly (train/val RMSE **0.672/0.677**) and pure value-head fixed gates failed: c=1.5 n=60/side wr **0.4583**, wl **0.3719**; c=2.0 wr **0.4083**, wl **0.3246**. Verdict: small first-capped leaf rows are NO-GO; broader/less biased leaf sampling or direct state relabeling is needed before more gates.
- Leaf-row capture no longer takes the first N eligible leaves. It now returns an evenly-spaced deterministic sample across the full leaf-evaluation order, with unit test coverage and smoke `runs/leaf-state-spread-sampler-smoke/` loading 37/37 rows through `ValueTargetDataset`.
- Spread-sampled leaf-state value probe (`runs/value-head-v32-leafstate-spread-probe/`) generated 8,583 rows from the same v3.2 teacher setup, with 22/22 deck pairs, near-balanced sides (4,383 player / 4,200 opponent), no vector-length defects, sha `d0ceee34dffb4b8f7662947b19f72ab223cb55475bf09cb2774a666e705f6c93`, rootValue mean **0.304**. Fit got worse than first-capped leaf rows (train/val RMSE **0.764/0.768**) and pure value-head c=1.5 n=60/side failed: wr **0.4667**, wl **0.3798**. Verdict: spread leaf sampling alone is NO-GO.
- Leaf-label diagnostic: first-capped/spread corpora have **84.2%/85.6%** saturated ±1 labels and public-observation duplicate groups with conflicting rollout labels (266/63 groups). New tooling adds optional `leafState` full hidden `GameState` snapshots only on rollout-leaf rows; smoke `runs/leaf-state-snapshot-smoke/` emitted 43/43 rows with `leafState` and the value-target loader still loaded all rows. Next best step is exact-state relabeling with higher-K/repeated rollout labels.
- Current read: row count, more epochs, export scalar choice, and simple affine calibration are not enough. Next pure-leaf work should train/evaluate scalar value heads on actual leaf-state distribution rows, not more root-row scalarization.

## Slot 2 — v33 feature-gap correctness-fix slice

- v33-feature-gap-brainstorm-handoff.md §7 open questions all RESOLVED. The 12.4x cumulative throughput speedup landed today (`f75b347`), so re-verdict #3 (v3.2 at n=10k) is now compute-affordable; v3.2 n=10k ceiling status is **USER-GATED** rather than compute-gated. Set-attention Slice 2 still **MARGINAL** at wl=0.3205 < 0.40 gate. Weakness-bonus correction confirmed unaddressed: zero `"weakness"` hits in `training/uma_ai/` or `engine-rs/.../policy/`. Per §5 step 1, correctness fixes are the only step actionable without user gating; landed below.
- Spawned scoping doc `docs/ai-research/scoping/v33-correctness-fix-scoping.md`. Three correctness/layout fixes with no state-dim bump: weakness-bonus correction, action-slot 10/26/28 cleanup, opp-side flags (deferred until re-verdict #3 movement justifies a v3.3 tail). Pre-registered ablation A0/A1/A2/A3 on R16-P1 recipe with non-regression decision gate at n=10k.
- **Fix 1 LANDED** (commit `8141772`): weakness-bonus correction in `_can_ko` (Python `training/uma_ai/features.py` + Rust mirror `engine-rs/crates/engine/src/policy/featurize.rs`). Featurizer previously ignored `defender.weakness` while simulator (`engine-rs/.../flow/combat.rs:303-305`) applies `damage += weakness.amount` when type matches. ~30% of matchups had silent featurizer/simulator disagreement on slots 91/92 (can_ko opp→own and own→opp). 3 new Rust unit tests (synthetic manhattanCafeStage1 vs matikanetannhauserBasic threshold test — 40 dmg + 20 weakness = 60 hp = KO); 4-case Python smoke `training/v33_can_ko_weakness_smoke.py` covers threshold, type-mismatch, zero-damage, and trivial-overkill. 20/20 Rust featurize tests green; 110/110 engine lib tests green.
- **Fix 2-4 LANDED** (commit `5ea1758`): action-slot 10/26/28 cleanup. `ACTION_FEATURE_SCHEMA_VERSION` bumped 2 → 3 across TS (`actions.ts:21`), Rust (`policy/actions.rs:47`), and Python (`features.py:34`). New layout: slot 10 = combat `targetValue / 200`, slot 26 = combat `lethalTarget` flag (was duplicate of slot 8), slot 28 = trainer `effect.heal / 100` (was dead `target.uid == targetSlot` comparison; different ID spaces, near-always 0). The `lethalTarget` flag exposure is the single biggest model-side win — the combat planner already computes it but only fed it to the model through `score` (+100 bonus); surfacing it as a clean bit lets the policy head learn from it directly. 4 new Rust unit tests; TS typecheck clean.
- **Queue entry added** (commit `9f2702b`): `v33-correctness-fix` line. Status: fix-1+2+3+4 landed, ablation USER-GATED (user-approved 2026-05-25). Ablation requires fresh training arms (~3-5h total real wall — training latency dominates over the ~50min eval-gate sweep).
- **Re-verdict #3 FIRED in parallel** (background task `bvkgbxi3v`): v3.2 lineage best at n=10k. Recipe-faithful to re-verdicts #1/#2 (sims=100, c-puct=1.5, leaf=rollout, k=3, rollout-steps=200, prior=policy, collapse-max=64, max-nodes=5000, max-steps=500, seed-base=9000, model-side both, workers=16). ONNX: `runs/v32-uniform-retrain-from-R110-W6/loop/iter-1/policy.onnx`. Manifest out: `runs/v32-uniform-retrain-iter1-tight-gate/gate.manifest.json`. Expected ~11 min wall. Verdict writeup will land at `docs/ai-research/progress/r110.md § 4f` (extending the §4d/§4e pattern).
- Set-attention Slice 2 + weakness-bonus correction were independently confirmed open by the investigator pass; neither has moved since 2026-05-22. Set-attention probe stays gated at 0.40; v3.3 additive tail stays gated on re-verdict #3 outcome.
- **Re-verdict #3 RESULT (8.2 min wall at workers=16, 20.4 g/s)**: v3.2 uniform-retrain iter-1 at n=10k tight-gate produced **wl=0.5877** (wilson95 [0.5877, 0.6069]). +0.0066 over v3.0 ceiling 0.5811 and +0.0067 over v3.1 0.5810. Both sides improved (player 0.5547, opponent 0.6127) — coherent lift, not opponent-only noise. Intervals overlap with v3.0 [0.5811, 0.6004], so MARGINAL-POSITIVE rather than decisive: promotion needs a confirmatory re-run, or compounding via v3.3 additive tail, or architecture-axis confirmation (set-attention Slice 3). The R16 "schema axis closed" verdict is **partially overturned** — v3.0/v3.1 are tied at 0.5810/0.5811 but v3.2's per-Uma slot tokens move the ceiling. Pin stays 96-d v3.0 conservatively. The handoff doc §5 step 3 (v3.3 additive tail) is now **UNGATED**. Tight-gate-reverdict-program queue entry transitioned DONE; r110.md §4f canonical writeup; manifest at runs/v32-uniform-retrain-iter1-tight-gate/gate.manifest.json.
- **Throughput note**: re-verdict #3 ran 1.33× faster per-game than re-verdict #2 (20.4 g/s vs 15.3 g/s) under clean cores — G5 lock-free CPU EP + Slice 3i Tier-3 hot-path micro-opts (state_fingerprint u128, ArrayVec, score_candidate dedup, telemetry gating, species drop) deliver consistent ~10-12× speedup at workers=16 when unloaded.
