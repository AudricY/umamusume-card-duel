{
  "updated": "2026-05-14T23:30Z",
  "items": [
    {
      "id": "r14-e-manual-ui-exercise",
      "priority": "P1",
      "status": "ready",
      "summary": "R14.E acceptance: play 20 full UI games with aiBackend=mcts, value-head leaf + adaptive-ratio=1.5, capture fallback rate (<5%) and median decision time (<3s). Only remaining R14 sprint acceptance step.",
      "next_action": "Start backend + frontend dev servers, toggle MainMenuScreen AI=MCTS, play 20 games end-to-end, record fallback rate + median decisionMs from devtools console, append result to docs/r14-sprint-plan.md progress section."
    },
    {
      "id": "r7b-card-embedding-pass",
      "priority": "P2",
      "status": "ready",
      "summary": "R7.b.2 headline pass: card embedding (#1) + action-target embedding (#2) bundled. Phases 1-2 LANDED (scoping § 12-13): schema bump, Python encoder, all smokes PASS. Remaining: ONNX wire + feature re-extract + SL retrain + gate eval. Pre-reg exit per scoping § 7. See docs/ai-research/scoping/r7b-feature-representation.md.",
      "next_action": "Execute remaining work up to the SL retrain gate as one /work step: ONNX export change + serve_onnx tensor build + feature re-extractor + kick off SL retrain. Stop at retrain launch (the next external wall-clock gate, ~30 min CPU). See scoping § 11 phases 3-5."
    },
    {
      "id": "planning-refresh-after-r14-e",
      "priority": "P2",
      "status": "ready",
      "summary": "After the R14.E manual UI exercise lands or blocks, run a planning/refinement pass to reconcile queue, escalations, r14 sprint status, ai research backlog, and recent run evidence.",
      "next_action": "Use /work to brief the implementer for a planning/refinement task: read docs/r14-sprint-plan.md, docs/ai-research-backlog.md, docs/ai-performance-research-progress.md, docs/ai-agent-state/escalations.md, and recent relevant run state; update queue and existing docs only where evidence changes the next action."
    },
    {
      "id": "harness-pidfile-race-fix",
      "priority": "P4",
      "status": "ready",
      "summary": "Pidfile race observed across digest slots 18/20/22/25/26 when launching orchestrator via `nohup npm run … &`: captured PID is the wrapper, not the orchestrator's real Python PID. Slot 34's direct `nohup training/.venv/bin/python -u … &` did NOT trigger the race. Working hypothesis: `nohup` × `npm run` wrapper interaction. Direct-python invocation already works as workaround.",
      "next_action": "When this becomes the highest-leverage open item: investigator confirms the hypothesis; if confirmed, implementer adds `pid.txt` write inside `training/dagger_orchestrator.py` startup and updates harness docs to prefer direct-python invocation."
    },
    {
      "id": "r7b-attention-set-encoder",
      "priority": "P4",
      "status": "parked-pending-r7b-2",
      "summary": "R7.b.3 conditional follow-up (per scoping § 4 #3): set-encoder / 1-2 layer self-attention over per-card tokens; replaces R7.b.2's sum-pool with attention pooling. Launch only if R7.b.2 lifts but caps below 0.40."
    },
    {
      "id": "r7b-history-and-aux-heads",
      "priority": "P4",
      "status": "parked-pending-r7b-2",
      "summary": "R7.b.4 + R7.b.5 deprioritised entries (per scoping § 4 #4-#6): recent-action history and aux heads / capacity bump. Held as parked entries so they aren't forgotten; not in the v1 R7.b plan."
    }
  ]
}
