[package]
name = "golden-replay"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# Replays the Phase 0 JSONL golden traces against the Rust engine and
# asserts bit-identity. This is the Phase 1h gate.

[[bin]]
name = "golden-replay"
path = "src/main.rs"

[dependencies]
engine = { path = "../engine" }
serde.workspace = true
serde_json.workspace = true
clap.workspace = true
anyhow.workspace = true
